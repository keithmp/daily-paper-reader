---
title: Contact-Guided 3D Genome Structure Generation of E. coli via Diffusion Transformers
title_zh: 基于扩散 Transformer 的接触引导大肠杆菌三维基因组结构生成
authors: "Mingxin Zhang, Xiaofeng Dai, Yu Yao, Ziqi Yin"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07472v1"
tags: ["query:q7"]
score: 9.0
evidence: 用于3D基因组结构生成的扩散Transformer
tldr: 本研究提出了一种基于扩散Transformer的条件生成框架，旨在利用Hi-C接触图引导生成大肠杆菌的三维基因组构象系综。不同于传统的确定性建模，该方法将基因组重建视为条件生成问题，通过潜空间扩散模型和流匹配目标，从Hi-C数据中采样出具有异质性且符合物理约束的结构系综。研究利用粗粒度分子动力学模拟生成的合成数据集进行训练，证明了该模型在保持构象多样性的同时，能准确还原Hi-C特征，为三维基因组学研究提供了新工具。
selection_source: fresh_fetch
motivation: 传统的基因组结构重建方法往往只生成单一确定性结构，难以捕捉生物体内基因组构象的高度异质性和系综特性。
method: 采用基于变分自编码器（VAE）和扩散Transformer的潜空间生成模型，结合流匹配目标，将Hi-C信息作为条件引导结构生成。
result: 生成的结构系综在保持高度多样性的同时，准确复现了输入Hi-C数据的距离衰减特性和结构相关性指标。
conclusion: 扩散生成模型是实现系综级三维基因组重建的有效手段，能够平衡结构的物理一致性与生物多样性。
---

## 摘要
在本研究中，我们提出了一种条件扩散 Transformer 框架，用于在 Hi-C 接触图的引导下生成大肠杆菌（Escherichia coli）三维基因组构象系综。与生成单一确定性结构不同，我们将基因组重建建模为一个条件生成建模问题，通过采样异质构象，使其系综平均接触与输入的 Hi-C 数据保持一致。我们利用粗粒度分子动力学模拟构建了一个合成数据集，用于生成环状拓扑下的染色质系综及其对应的 Hi-C 图。我们的模型在潜空间扩散设置下运行，并结合了一个变分自编码器，该自编码器能够保持 bin 对齐并支持复制感知表征。Hi-C 信息通过基于 Transformer 的编码器和交叉注意力机制注入，从而施加了从 Hi-C 到结构的物理可解释单向约束。模型采用流匹配目标进行训练，以实现稳定优化。在预留的系综上，生成的结构重现了输入 Hi-C 的距离衰减和结构相关性指标，同时保持了显著的构象多样性，证明了基于扩散的生成建模在系综级三维基因组重建中的有效性。

## Abstract
In this study, we present a conditional diffusion-transformer framework for generating ensembles of three-dimensional Escherichia coli genome conformations guided by Hi-C contact maps. Instead of producing a single deterministic structure, we formulate genome reconstruction as a conditional generative modeling problem that samples heterogeneous conformations whose ensemble-averaged contacts are consistent with the input Hi-C data. A synthetic dataset is constructed using coarse-grained molecular dynamics simulations to generate chromatin ensembles and corresponding Hi-C maps under circular topology. Our models operate in a latent diffusion setting with a variational autoencoder that preserves per-bin alignment and supports replication-aware representations. Hi-C information is injected through a transformer-based encoder and cross-attention, enforcing a physically interpretable one-way constraint from Hi-C to structure. The model is trained using a flow-matching objective for stable optimization. On held-out ensembles, generated structures reproduce the input Hi-C distance-decay and structural correlation metrics while maintaining substantial conformational diversity, demonstrating the effectiveness of diffusion-based generative modeling for ensemble-level 3D genome reconstruction.

---

## 论文详细总结（自动生成）

以下是对论文《Contact-Guided 3D Genome Structure Generation of E. coli via Diffusion Transformers》的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：Hi-C 技术通过测量基因组片段间的接触频率来推断三维结构，但 Hi-C 图谱是群体平均的结果，反映的是数百万个细胞的综合状态。
*   **核心问题**：传统的重建方法（如优化算法或确定性深度学习模型）通常只生成单一的“共识”结构，忽略了基因组在活细胞中高度的异质性和动态性。
*   **研究动机**：将基因组重建视为**条件生成建模问题**。给定一个 Hi-C 图谱，模型应能采样出一系列物理上合理且多样化的三维构象，这些构象的系综平均接触特征需与输入的 Hi-C 数据一致。

### 2. 方法论
*   **核心思想**：结合潜空间扩散模型（Latent Diffusion）与 Transformer 架构，利用 Hi-C 信息作为条件引导三维坐标序列的生成。
*   **关键技术细节**：
    *   **ResNet VAE**：使用 1D ResNet18 结构的变分自编码器将三维坐标序列压缩至潜空间。为了处理大肠杆菌 DNA 复制过程中的分支结构，引入了**复制掩码（Replication Mask）**，使模型能同时表征亲本链和新合成链。
    *   **CrossDiT 架构**：基于扩散 Transformer（DiT），通过**交叉注意力机制（Cross-Attention）**注入 Hi-C 条件。这种设计确保了从 Hi-C 到结构的单向物理约束（即 Hi-C 引导结构，而非互影响）。
    *   **流匹配（Flow-Matching）**：采用流匹配目标函数代替传统的 DDPM 扩散公式，以实现更稳定、更高效的生成动力学优化。
    *   **数据表示**：将基因组建模为 928 个 bin 的序列，每个 bin 包含坐标信息和复制状态，总维度为 $928 \times 16$。

### 3. 实验设计
*   **数据集**：由于缺乏真实的单细胞三维基因组地面真值，研究者利用 **GROMACS** 进行粗粒度分子动力学（MD）模拟，生成了符合物理约束的大肠杆菌染色体构象。
    *   **训练集**：65 个系综，每个系综包含 500 个结构（共 32,500 个样本）。
    *   **测试集**：10 个系综（共 5,000 个样本）。
*   **评估指标（Benchmark）**：
    *   **P(s) 曲线**：衡量接触频率随基因组距离增加而衰减的趋势。
    *   **SCC（地层调节相关系数）**：评估生成的系综平均 Hi-C 图与输入图之间的相似性。
    *   **dRMSD（距离均方根偏差）**：衡量生成构象之间的多样性，防止模型坍缩至单一结构。
*   **对比方法**：对比了不同规模的模型变体（CrossDiT-S, 45M 参数；CrossDiT-L, 634M 参数）。

### 4. 资源与算力
*   **硬件**：使用了单台 **NVIDIA GH200 GPU**。
*   **训练时长**：VAE 训练了 50 个 epoch（Batch Size 32）；CrossDiT 训练了 50 个 epoch（Batch Size 8）。
*   **推理**：使用整流流采样（Rectified Flow Sampling），步数设置为 50 步。

### 5. 实验数量与充分性
*   **实验规模**：共使用了 75 个模拟系综，涵盖了不同的复制阶段和物理约束。
*   **充分性评价**：实验设计较为科学，通过 P(s) 验证了宏观聚合物特性，通过 SCC 验证了局部接触准确性，通过 dRMSD 验证了生成模型的核心优势——多样性。然而，实验目前仅限于合成数据集和大肠杆菌（环状基因组），尚未在真实实验数据或更复杂的真核生物基因组上进行大规模验证。

### 6. 主要结论与发现
*   **系综一致性**：生成的结构系综在聚合后能够高度还原输入 Hi-C 图谱的特征，CrossDiT-L 的 SCC 得分达到 0.962。
*   **多样性保持**：模型生成的结构展现了显著的异质性，其 dRMSD 远高于简单的噪声扰动基准，证明模型捕捉到了构象空间的分布。
*   **规模效应**：模型性能与参数量正相关，较大的模型（CrossDiT-L）在还原 Hi-C 约束和保持多样性方面均优于较小的模型（CrossDiT-S）。

### 7. 优点
*   **范式创新**：从“结构重建”转向“系综生成”，更符合染色质组织的生物物理本质。
*   **物理可解释性**：通过交叉注意力机制实现了 Hi-C 对结构的单向引导，符合实验观测引导结构推断的逻辑。
*   **处理复制状态**：创新的复制掩码设计使其能够处理细菌基因组特有的 DNA 复制分支问题。

### 8. 不足与局限
*   **数据依赖**：高度依赖 MD 模拟生成的合成数据，模拟过程中的物理假设可能与真实生物环境存在偏差。
*   **计算开销**：由于采用 bin 对齐的序列表示，随着基因组长度增加（如真核生物），Transformer 的二次方复杂度会导致巨大的计算和内存压力。
*   **拓扑局限**：目前仅针对大肠杆菌的环状拓扑结构，对于具有多个线性染色体的复杂生物系统的适用性有待验证。

（完）
