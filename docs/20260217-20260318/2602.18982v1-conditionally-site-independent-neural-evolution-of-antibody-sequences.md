---
title: Conditionally Site-Independent Neural Evolution of Antibody Sequences
title_zh: 抗体序列的条件位点独立神经演化
authors: "Stephen Zhewen Lu, Aakarsh Vermani, Kohei Sanno, Jiarui Lu, Frederick A Matsen, Milind Jagota, Yun S. Song"
date: 2026-02-21
pdf: "https://arxiv.org/pdf/2602.18982v1"
tags: ["query:q7"]
score: 9.0
evidence: 利用深度学习和系统发育模型进行抗体序列的神经演化研究
tldr: 本研究提出CoSiNE框架，利用神经网络参数化的连续时间马尔可夫链模拟抗体进化动态。它弥补了深度学习忽略进化信息与传统模型缺乏表达力的不足，能有效捕捉上位性效应。实验证明CoSiNE在零样本变异预测中优于现有语言模型，并能通过Guided Gillespie采样实现抗体亲和力的定向优化，为抗体工程提供了新路径。
selection_source: fresh_fetch
motivation: 现有深度学习方法忽略了抗体亲和力成熟过程中的进化信息，而传统系统发育模型又难以捕捉复杂的上位性相互作用。
method: 提出CoSiNE模型，利用深度神经网络参数化的连续时间马尔可夫链来模拟抗体序列的进化动态。
result: CoSiNE在零样本变异效应预测中优于现有语言模型，并能通过Guided Gillespie采样方案有效优化抗体亲和力。
conclusion: 该研究通过结合进化动力学与深度学习，为抗体序列建模和工程化提供了一种更具生物学解释性且高效的新框架。
---

## 摘要
常用的抗体工程深度学习方法侧重于对序列的边缘分布进行建模。然而，通过将序列视为独立样本，这些方法忽略了亲和力成熟过程，而该过程是关于抗体探索底层适应度景观演化过程的丰富且很大程度上尚未开发的资源。相比之下，经典的系统发育模型显式地表示演化动力学，但缺乏捕捉复杂上位性相互作用的表达能力。我们通过 CoSiNE 弥补了这一差距，这是一种由深度神经网络参数化的连续时间马尔可夫链。在数学上，我们证明了 CoSiNE 为难以处理的序列点突变过程提供了一阶近似，并以分支长度的二次方为误差界限捕捉了上位性效应。在实证方面，CoSiNE 通过显式地将选择与上下文相关的体细胞超突变解耦，在零样本变体效应预测中优于最先进的语言模型。最后，我们引入了 Guided Gillespie，这是一种分类器引导的采样方案，在推理时引导 CoSiNE，从而实现针对特定抗原的抗体结合亲和力的高效优化。

## Abstract
Common deep learning approaches for antibody engineering focus on modeling the marginal distribution of sequences. By treating sequences as independent samples, however, these methods overlook affinity maturation as a rich and largely untapped source of information about the evolutionary process by which antibodies explore the underlying fitness landscape. In contrast, classical phylogenetic models explicitly represent evolutionary dynamics but lack the expressivity to capture complex epistatic interactions. We bridge this gap with CoSiNE, a continuous-time Markov chain parameterized by a deep neural network. Mathematically, we prove that CoSiNE provides a first-order approximation to the intractable sequential point mutation process, capturing epistatic effects with an error bound that is quadratic in branch length. Empirically, CoSiNE outperforms state-of-the-art language models in zero-shot variant effect prediction by explicitly disentangling selection from context-dependent somatic hypermutation. Finally, we introduce Guided Gillespie, a classifier-guided sampling scheme that steers CoSiNE at inference time, enabling efficient optimization of antibody binding affinity toward specific antigens.

---

## 论文详细总结（自动生成）

以下是对论文《Conditionally Site-Independent Neural Evolution of Antibody Sequences》（抗体序列的条件位点独立神经演化）的结构化深入总结：

### 1. 核心问题与研究背景
*   **研究动机**：抗体通过“亲和力成熟”（Affinity Maturation）过程在个体内部快速演化。现有的深度学习方法（如蛋白质语言模型）通常将抗体序列视为独立同分布的样本，忽略了其背后的进化动力学；而传统的系统发育模型（如 CTMC）虽然考虑了进化时间，但通常假设位点独立，无法捕捉复杂的“上位性”（Epistasis，即位点间的相互作用）。
*   **核心问题**：如何构建一个既能显式建模进化时间动态，又能捕捉序列内部复杂相互作用（上位性）的抗体生成模型？

### 2. 方法论
*   **核心思想**：提出 **CoSiNE** 模型，这是一种由深度神经网络参数化的连续时间马尔可夫链（CTMC）。它在形式上假设位点在给定当前全序列上下文的情况下是独立的，但通过神经网络动态生成每个位点的速率矩阵。
*   **关键技术细节**：
    *   **条件位点独立性**：转移概率定义为 $p_\theta(y | x, t) = \prod_{l=1}^L \exp(t Q_\theta(x)_l)_{x_l, y_l}$。其中速率矩阵 $Q$ 是输入序列 $x$ 的函数，从而隐式捕捉了位点间的依赖关系。
    *   **数学证明**：论文证明了 CoSiNE 是一阶近似序列点突变过程的合理方案，其误差界限与分支长度（进化时间）呈二次方关系 $O(t^2)$。
    *   **选择压力解耦**：通过计算 CoSiNE 似然与中性突变模型（如 Thrifty）似然的对数比，显式地将“生物选择压力”从“突变偏好”中分离出来，用于预测变异效应。
    *   **Guided Gillespie 采样**：引入了一种分类器引导的采样方案。利用目标属性预测器的梯度来修正速率矩阵 $Q$，从而在推理阶段定向引导抗体向高亲和力方向演化。

### 3. 实验设计
*   **数据集**：
    *   训练集：整合了 5 个公共 BCR 测序来源，包含约 12 万个克隆家族、200 万个进化转换（Parent-Child Pairs）。
    *   验证集：使用 FLAb2 基准测试集，包含 Koenig、Adams、Shanehsazzadeh 等提供的深层突变扫描（DMS）数据。
*   **对比方法**：
    *   抗体/通用语言模型：AbLang-2、ESM-2 (150M/650M)、ProGen2 (Small/Medium)。
    *   进化模型：DASM（及其配套的 Thrifty 突变模型）。
*   **实验场景**：零样本变异效应预测（VEP）、抗体亲和力成熟模拟、局部 CDR 区域优化。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA A100 GPU** 进行训练。
*   **训练时长**：模型在约 **1 天** 内完成收敛。
*   **模型规模**：基于 ESM-2 150M 参数架构进行初始化和微调。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从合成数据（密码子空间）到真实大规模抗体库的验证。
*   **充分性分析**：
    *   **变异预测**：在 7 个不同的 DMS 数据集上进行了测试，涵盖了表达量和结合力两个维度。
    *   **消融实验**：对比了单链与双链上下文的影响，验证了跨链上位性的存在。
    *   **理论验证**：通过合成密码子实验验证了 Gillespie 采样相对于直接矩阵指数运算的优越性。
    *   **客观性**：使用了标准的 Spearman 相关系数作为评价指标，并与多种主流 SOTA 模型进行了公平对比。

### 6. 主要结论与发现
*   **性能领先**：CoSiNE 在零样本变异效应预测（VEP）任务上显著优于现有的语言模型和专门的抗体模型（如 DASM）。
*   **上位性捕捉**：通过分类 Jacobian 矩阵可视化，证明了模型成功学习到了 CDR 区域内部以及重链与轻链之间的相互依赖关系。
*   **定向优化有效**：Guided Gillespie 采样能够生成预测亲和力超过已知生物结合剂的序列，同时保持了极高的人类化程度（OASis 分数）和结构稳定性（pLDDT）。
*   **解耦价值**：证明了通过减去中性突变背景（SHM correction）能显著提升适应度预测的准确性。

### 7. 优点
*   **理论与实证结合**：不仅有深度学习的实证效果，还提供了严谨的随机过程理论证明。
*   **采样效率高**：Guided Gillespie 方案允许在不重新训练模型的情况下，利用外部预测器梯度进行高效的序列优化。
*   **生物学解释性强**：模型结构直接对应抗体的自然进化过程，而非黑盒的序列概率建模。

### 8. 不足与局限
*   **长分支误差**：理论误差随时间 $t$ 二次方增长，意味着在模拟极长距离的进化（如跨物种进化）时可能失效。
*   **不支持 Indel**：目前模型仅处理等长序列或预对齐序列，无法处理进化过程中的插入和缺失（Insertions/Deletions）。
*   **计算开销**：虽然推理比全矩阵 CTMC 快，但相比于简单的语言模型掩码预测，Gillespie 迭代采样仍需多次模型前向计算。
*   **应用范围**：目前主要针对抗体优化，在通用蛋白质工程上的泛化能力尚待验证。

（完）
