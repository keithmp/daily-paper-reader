---
title: "ResGene-T: A Tensor-Based Residual Network Approach for Genomic Prediction"
title_zh: ResGene-T：一种基于张量的残差网络基因组预测方法
authors: "Kuldeep Pathak, Kapil Ahuja, Eric de Sturler"
date: 2026-02-28
pdf: "https://arxiv.org/pdf/2603.00744v1"
tags: ["query:q7"]
score: 9.0
evidence: 深度学习用于基因组预测
tldr: "本研究针对基因组预测（GP）任务，提出了一种名为ResGene-T的新型深度学习模型。该模型突破了传统1D序列或2D图像表示基因型数据的局限，创新性地将基因型转换为3D张量，并结合ResNet-18残差网络进行回归预测。实验表明，ResGene-T在多种作物和表型性状上表现优异，相较于传统统计、机器学习及深度学习模型，预测性能提升了14.51%至41.51%，为高效基因组育种提供了新方案。"
selection_source: fresh_fetch
motivation: 传统的1D或2D基因型表示方法难以充分捕捉复杂的生物相互作用，且训练效率有限。
method: 提出ResGene-T模型，通过将基因型数据转化为3D张量并输入ResNet-18架构，增强了特征提取能力。
result: 在三种作物、十种表型上的测试结果显示，ResGene-T的性能显著优于七种主流的统计、机器学习和深度学习模型。
conclusion: 采用张量化表示与残差网络相结合的方法能大幅提升基因组预测的准确性，具有极高的应用价值。
---

## 摘要
在这项工作中，我们提出了一种用于基因组预测（GP）的新型深度学习模型，该模型涉及将基因型数据与表型数据相关联。基因型通常作为字符序列输入到基础深度学习模型的一维卷积神经网络（1D-CNN）层中。受早期将基因型表示为二维图像进行基因型-表型分类工作的启发，我们将这一想法扩展到属于回归任务的基因组预测中。我们使用 ResNet-18 作为基础架构，并将该模型命名为 ResGene-2D。尽管二维图像表示能够很好地捕捉生物相互作用，但它需要模型的所有层都参与其中，这限制了训练效率。因此，正如早期提出二维图像表示的工作所示，我们的 ResGene-2D 表现与其他模型基本持平（提升 3%）。为了克服这一限制，我们提出了一种新颖的想法，将二维图像转换为三维/张量，并将其输入到 ResNet-18 架构中，将该模型命名为 ResGene-T。我们在具有十个表型性状的三种农作物物种上评估了所提出的模型，并将其与七种最流行的模型（两种统计模型、两种机器学习模型和三种深度学习模型）进行了比较。ResGene-T 在所有这七种方法中表现最佳（提升幅度从 14.51% 到 41.51% 不等）。

## Abstract
In this work, we propose a new deep learning model for Genomic Prediction (GP), which involves correlating genotypic data with phenotypic. The genotypes are typically fed as a sequence of characters to the 1D-Convolution Neural Network layer of the underlying deep learning model. Inspired by earlier work that represented genotype as a 2D-image for genotype-phenotype classification, we extend this idea to GP, which is a regression task. We use a ResNet-18 as the underlying architecture, and term this model as ResGene-2D.   Although the 2D-image representation captures biological interactions well, it requires all the layers of the model to do so. This limits training efficiency. Thus, as seen in the earlier work that proposed a 2D-image representation, our ResGene-2D performs almost the same as other models (3% improvement). To overcome this, we propose a novel idea of converting the 2D-image into a 3D/ tensor and feed this to the ResNet-18 architecture, and term this model as ResGene-T.   We evaluate our proposed models on three crop species having ten phenotypic traits and compare it with seven most popular models (two statistical, two machine learning, and three deep learning). ResGene-T performs the best among all these seven methods (gains from 14.51% to 41.51%).

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ResGene-T** 的新型深度学习模型，旨在提高基因组预测（Genomic Prediction, GP）的准确性。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在基因组预测任务中更有效地捕捉单核苷酸多态性（SNPs）之间复杂的生物相互作用（包括加性效应和上位性效应）。
*   **研究背景**：传统的基因组预测方法（统计学、机器学习或深度学习）通常将基因型视为一维序列。虽然已有研究尝试将其转化为二维图像以利用 2D-CNN 捕捉空间关系，但这些方法往往需要经过模型的所有层才能覆盖整个基因组信息，导致训练效率低下且性能提升有限。
*   **研究动机**：通过改变数据的表征方式，让模型在初始层就能“看到”更广泛的基因组相互作用，从而提升回归预测的精度。

### 2. 论文提出的方法论
*   **核心思想**：将基因型数据从一维序列（1D）转化为二维图像（2D），再进一步转化为**三维张量（3D/Tensor）**。
*   **关键技术细节**：
    *   **数据编码**：将核苷酸（A, T, G, C 等）编码为数值（如 A/T 为 0，G/C 为 2，缺失值为 -1，其他为 1）。
    *   **张量化表征**：将长序列折叠成多个通道（Channels）。这种做法的数学优势在于，相比于 2D 图像，张量表征使模型覆盖整个基因组所需的卷积层数减少了 $\sqrt{C}$ 倍（$C$ 为通道数），从而让远距离的 SNPs 在网络的早期阶段就能产生交互。
    *   **模型架构**：采用 **ResNet-18** 作为骨干网络。由于基因组数据集规模相对较小，作者选择从头训练（Train from scratch）而非使用预训练模型，并将最后的分类层替换为线性回归层。
    *   **ResGene-2D vs ResGene-T**：前者仅使用单通道 2D 图像，后者使用多通道张量。

### 3. 实验设计
*   **数据集**：涵盖三种主要农作物，共 10 个表型性状：
    *   **大豆 (Soybean)**：269 个品种，66,589 个 SNPs，4 个性状（株高、节数、产量、冠层温度）。
    *   **水稻 (Rice)**：327 个品种，57,542 个 SNPs，3 个性状（株高、抽穗期、产量）。
    *   **高粱 (Sorghum)**：451 个品种，56,299 个 SNPs，3 个性状（株高、水分、产量）。
*   **Benchmark（对比方法）**：共 7 种主流模型：
    *   **统计学方法**：rrBLUP, BayesB。
    *   **机器学习方法**：SVR, XGBoost。
    *   **深度学习方法**：DLGWAS, DNNGP, GPFormer（基于 Transformer）。
*   **评估指标**：皮尔逊相关系数（PCC）和 Friedman 排名检验。

### 4. 资源与算力
*   **硬件设施**：实验在一台配备 **Intel Xeon CPU @ 2.00GHz** 和 **128GB RAM** 的机器上运行。
*   **算力说明**：论文提到使用了 PyTorch 框架，但**未明确说明是否使用了 GPU** 以及具体的训练时长。所有深度学习模型均统一训练了 100 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：采用了 **10 折交叉验证（10-fold cross-validation）**，确保了结果的稳健性。
*   **超参数调优**：作者对所有深度学习基准模型进行了系统的超参数搜索（包括 Batch Size、学习率、Dropout 等，共 8-16 种组合），而非使用默认参数，这保证了对比的**公平性**。
*   **充分性评价**：实验覆盖了多种作物和不同遗传力的性状，并进行了统计显著性检验（Friedman test, p-value < 0.05），实验设计较为客观、充分。

### 6. 主要结论与发现
*   **性能卓越**：ResGene-T 在所有测试方法中表现最优，相比其他 7 种基准方法，PCC 提升了 **14.51% 至 41.51%**。
*   **一致性强**：在 10 个性状中，ResGene-T 在 7 个性状上排名第一，平均排名为 1.4，远优于第二名 ResGene-2D（3.5）和第三名 SVR（3.9）。
*   **表征的重要性**：ResGene-T 比 ResGene-2D 平均性能高出 11.85%，证明了张量化表征在捕捉早期生物相互作用方面的有效性。

### 7. 优点
*   **创新表征**：提出将基因组数据张量化，解决了 2D 图像表征在深度网络中信息传递过慢的问题。
*   **架构稳健**：引入残差网络（ResNet）有效缓解了深层网络中的梯度消失问题，且模型结构相对简单、平衡。
*   **对比严谨**：通过详尽的超参数调优和统计检验，增强了实验结论的可信度。

### 8. 不足与局限
*   **样本量限制**：所使用的作物数据集样本量较小（仅几百个品种），虽然 ResNet-18 表现良好，但在更大规模的数据集上（如人类基因组或大型育种群体）的扩展性有待验证。
*   **计算开销**：虽然张量化减少了层数需求，但多通道卷积会增加单层的计算负担，文中缺乏关于推理速度和内存占用的详细对比。
*   **性状关联**：目前仅针对单性状预测，未探讨多性状联合预测（Multi-trait prediction）的潜力。
*   **生物学解释性**：作为黑盒模型，虽然性能提升显著，但对于具体哪些 SNPs 产生了上位性交互缺乏直观的生物学解释。

（完）
