---
title: "Better Models, Faster Training: Sigmoid Attention for single-cell Foundation Models"
title_zh: 更好的模型，更快的训练：用于单细胞基础模型的 Sigmoid 注意力机制
authors: "Vijay Sadashivaiah, Georgios Dasoulas, Judith Mueller, Soumya Ghosh"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.27124v1"
tags: ["query:q7"]
score: 8.0
evidence: 用于单细胞基因组数据的AI基础模型
tldr: "本研究针对单细胞基础模型训练中的稳定性与效率问题，提出用Sigmoid注意力机制替代传统的Softmax。实验表明，Sigmoid注意力不仅能提升25%的细胞类型分离度，还能加快10%的训练速度，并通过全局有界导数解决了Softmax在高参数量下的梯度爆炸问题。此外，团队开源了高效的TritonSigmoid内核，在H100上表现优于FlashAttention-2。"
selection_source: fresh_fetch
motivation: 传统的Softmax注意力机制在训练大规模生物基础模型时存在梯度不稳定、计算开销大以及表示学习效果有限的问题。
method: 提出使用具有全局有界导数和对角雅可比结构的Sigmoid注意力机制，并开发了高性能的TritonSigmoid GPU内核以优化计算效率。
result: "在六个单细胞数据集上，Sigmoid注意力使细胞类型分离度提升25%，训练速度提高10%，且在极端压力测试下表现出极高的数值稳定性。"
conclusion: Sigmoid注意力在理论和实证上均优于Softmax，是构建更稳定、更高效的生物基础模型的理想选择。
---

## 摘要
训练稳定的生物学基础模型需要重新审视注意力机制。我们发现，将 Sigmoid 注意力作为 Softmax 注意力的直接替代方案具有以下优势：a) 产生更好的学习表示：在六个不同的单细胞数据集上，Sigmoid 实现了高出 25% 的细胞类型分离度、更好的细胞类型凝聚力指标以及更低的验证损失；b) 训练速度更快：采用 Sigmoid 注意力的模型比对应的 Softmax 模型训练速度快达 10%；c) 训练更稳定：消除了 Softmax 注意力中固有的不稳定来源。我们证明了 Sigmoid 注意力具有全局有界导数（$\leq 0.25$），而 Softmax 则不然；此外，与 Softmax 的稠密耦合相比，Sigmoid 具有对角雅可比结构，这些特性共同有助于缓解训练不稳定性。在对 160M 参数的双向注意力模型进行的压力测试中（在 8K token 序列上训练且不使用梯度裁剪），Softmax 发生了灾难性的发散，梯度爆炸了四个数量级，而 Sigmoid 保持稳定。最后，我们实现并开源了 TritonSigmoid，这是一个高效的 GPU 内核，在 H100 GPU 上达到了 515 TFLOPS，性能优于 FlashAttention-2 和 FlashSigmoid，并提供对生物序列至关重要的原生填充支持。我们的结果表明，对于生物学基础模型，Sigmoid 注意力在理论上具有依据，在实证上表现优越。代码开源于：https://github.com/MSDLLCpapers/triton-sigmoid

## Abstract
Training stable biological foundation models requires rethinking attention mechanisms: we find that using sigmoid attention as a drop in replacement for softmax attention a) produces better learned representations: on six diverse single-cell datasets, sigmoid achieves 25% higher cell-type separation, better cell-type cohesion metrics, and lower validation loss, b) faster training, models with sigmoid attention train up to 10% faster than their softmax counterparts, and c) more stable training by eliminating inherent sources of instability in softmax attention. We establish that sigmoid attention has globally bounded derivatives ($\leq 0.25$) as opposed to softmax, and a diagonal Jacobian structure in contrast with softmax's dense coupling, which together help alleviate training instabilities. In stress tests on 160M-parameter bidirectional attention models trained without gradient clipping on 8K-token sequences, softmax diverges catastrophically, with gradients exploding by four orders of magnitude, while sigmoid remains stable. Finally, we implement and open-source TritonSigmoid, an efficient GPU kernel that achieves 515 TFLOPS on H100 GPUs, outperforming both FlashAttention-2 and FlashSigmoid, with native padding support, which is essential for biological sequences. Our results establish sigmoid attention as both theoretically grounded and empirically superior for biological foundation models. Code is available at https://github.com/MSDLLCpapers/triton-sigmoid

---

## 论文详细总结（自动生成）

以下是对论文《Better Models, Faster Training: Sigmoid Attention for single-cell Foundation Models》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：单细胞基础模型（scFMs）通过将细胞的基因表达谱视为序列来学习细胞身份和功能。目前主流模型（如 scGPT, Geneformer）默认使用 **Softmax 注意力机制**。
*   **核心痛点**：
    1.  **生物学不匹配**：Softmax 是竞争性的（归一化导致权重此消彼长），而基因调控通常是非竞争性的（多个转录因子可同时作用于一个基因）。
    2.  **训练不稳定**：在处理长序列（生物数据常需 4K-16K token）时，Softmax 易导致梯度爆炸和注意力熵崩溃。
    3.  **计算效率瓶颈**：生物数据序列长度差异极大（Jaggedness），现有的高效内核（如 FlashAttention）对 Padding（填充）支持不足，导致大量算力浪费。
*   **研究动机**：探索 Sigmoid 注意力是否能作为 Softmax 的优选替代方案，以提升生物基础模型的表示质量、训练速度和数值稳定性。

### 2. 方法论
*   **核心思想**：将 Softmax 替换为逐元素的 Sigmoid 激活函数，并引入可学习或固定的偏置项。
*   **关键技术细节**：
    *   **公式定义**：$SigmoidAttn(X) = \sigma\left(\frac{QK^\top}{\sqrt{d}} + b\right)V$，其中偏置 $b = -\log(n)$（$n$ 为序列长度），用于模拟 Softmax 的缩放特性。
    *   **理论稳定性**：证明了 Sigmoid 具有全局有界导数（$\leq 0.25$）和对角雅可比（Jacobian）结构。这使得梯度在 token 间解耦，避免了 Softmax 中常见的局部 Lipschitz 常数随得分指数增长的问题。
    *   **TritonSigmoid 内核**：基于 Triton 开发了高性能 GPU 内核。
        *   **原生 Padding 支持**：通过块稀疏计算跳过全填充块，显著提升处理变长序列的效率。
        *   **算子融合**：融合了缩放、偏置、Sigmoid 和加权求和，并使用硬件优化的 Tanh 近似 Sigmoid。
        *   **反向传播优化**：将反向传播分解为两个内核以消除原子操作，并重新计算激活值以节省显存。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：使用 CellxGene 数据库（1.316 亿个细胞）。
    *   **验证/下游任务**：6 个独立的单细胞数据集，涵盖大脑、外周血（PBMC）、结肠、肺和心脏等组织。
*   **Benchmark 与对比方法**：
    *   **注意力机制**：Softmax (FlexAttention 实现) vs. Sigmoid。
    *   **内核性能**：标准 PyTorch 实现、FlashAttention-2、FlashSigmoid。
*   **评估指标**：验证损失（Masked LM）、scIB 生物学保护指标（NMI, ARI, Silhouette）、最大平均差异（MMD，衡量细胞类型分离度）、训练吞吐量（TFLOPS）。

### 4. 资源与算力
*   **硬件型号**：NVIDIA H100 80GB SXM5 GPU。
*   **算力投入**：
    *   **吞吐量测试**：在 16 台 H100 上运行 10,000 步。
    *   **完整训练**：160M 参数模型在 2K/4K 上下文下训练至收敛，耗时约 650 至 930 GPU 小时。
    *   **模型规模**：测试了 160M、400M、600M 和 1.4B 四种参数规模。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从内核级（算子速度）到模型级（端到端训练时间），再到生物学表征质量（下游指标）的多维度对比。
    *   **压力测试**：在 8K 序列长度、无梯度裁剪的极端条件下对比了 160M 模型，验证了稳定性。
*   **充分性评价**：实验设计非常充分且客观。通过在 6 个完全独立的外部数据集上验证，排除了过拟合风险；通过控制变量（仅改变注意力机制）确保了对比的公平性。

### 6. 主要结论与发现
1.  **表征质量更优**：Sigmoid 在所有验证集上的损失均低于 Softmax，且在细胞类型分离度（MMD）上平均提升了 **25%**。
2.  **训练速度更快**：端到端训练提速达 **10%**。TritonSigmoid 内核在 H100 上达到 **515 TFLOPS**，比 FlashAttention-2 快 43%。
3.  **数值稳定性极强**：在压力测试中，Softmax 模型在 5.5 万步左右因梯度爆炸（增长 4 个数量级）而崩溃，而 Sigmoid 模型全程保持平稳。
4.  **长上下文优势**：4K 上下文模型系统性优于 2K 模型，证明了长序列建模对捕捉复杂基因关系的重要性。

### 7. 优点
*   **软硬结合**：不仅提出了算法改进，还提供了针对现代 GPU 架构深度优化的开源内核，解决了生物序列 Padding 的实际痛点。
*   **理论支撑扎实**：通过雅可比矩阵和 Lipschitz 连续性分析，从数学上解释了 Sigmoid 稳定的原因。
*   **生物学契合度高**：非竞争性的注意力机制更符合基因共表达和多因子调控的生物学逻辑。

### 8. 不足与局限
*   **架构局限性**：实验主要集中在双向 Transformer Encoder（类似 BERT），尚未在 Decoder-only（类似 GPT）的大规模生成式任务中广泛验证。
*   **硬件绑定**：Triton 内核虽然高效，但主要针对 NVIDIA GPU 优化，在其他硬件平台（如 TPU 或 AMD GPU）上的移植性未讨论。
*   **超参数敏感性**：虽然 Sigmoid 偏置 $b$ 有理论推荐值，但在不同规模或领域的模型中是否需要精细调优仍需进一步探索。

（完）
