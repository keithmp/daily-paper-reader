---
title: "Fold-CP: A Context Parallelism Framework for Biomolecular Modeling"
title_zh: Fold-CP：一种用于生物分子建模的上下文并行框架
authors: "Dejun Lin, Simon Chu, Vishanth Iyer, Youhan Lee, John St John, Kevin Boyd, Brian Roland, Xiaowei Ren, Guoqing Zhou, Zhonglin Cao, Polina Binder, Yuliya Zhautouskaya, Jakub Zakrzewski, Maximilian Stadler, Kyle Gion, Yuxing Peng, Xi Chen, Tianjing Zhang, Philipp Junk, Michelle Dimon, Paweł Gniewek, Fabian Ortega, McKinley Polen, Ivan Grubisic, Ali Bashir, Graham Holt, Danny Kovtun, Matthias Grass, Luca Naef, Rui Wang, Jian Peng, Anthony Costa, Saee Paliwal, Eddie Calleja, Timur Rvachov, Neha Tadimeti, Roy Tal, Emine Kucukbenli"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14806v1"
tags: ["query:q7"]
score: 8.0
evidence: 利用AI进行生物分子建模与蛋白质折叠
tldr: NVIDIA推出BioNeMo Fold-CP框架，旨在解决AlphaFold 3等模型在处理大型生物分子组装体时的显存瓶颈。通过在多GPU间分布推理和训练流水线，并优化三角更新与局部注意力机制，该框架实现了显存的线性扩展。它支持超过3万个残基的结构预测，为大规模蛋白质复合物建模和虚拟细胞研究提供了高效的可扩展方案。
selection_source: fresh_fetch
motivation: 现有的生物分子建模工具受限于单GPU显存，难以处理包含数千个以上残基的大型复杂系统。
method: 开发了Fold-CP框架，利用上下文并行技术将计算任务分布至多GPU，并针对三角更新和不规则局部注意力设计了高效的并行原语。
result: "实现了显存随GPU数量增加而线性下降，成功在64块GPU上预测了超过3万个残基的结构，并覆盖了CORUM数据库中90%以上的复合物。"
conclusion: Fold-CP为大规模生物系统建模提供了可扩展的路径，是实现全细胞尺度模拟的重要一步。
---

## 摘要
理解细胞机制需要对大型生物分子复合物进行原子尺度的重建。然而，此类系统的结构预测一直受限于 AlphaFold 3 等模型的硬件显存需求，导致单张 GPU 可处理的残基数量存在几千个的实际上限。在此，我们介绍了 NVIDIA BioNeMo Fold-CP，这是一个上下文并行（context parallelism）框架，通过将协同折叠（co-folding）模型的推理和训练流水线分布到多个 GPU 上来克服这一障碍。我们以 Boltz 模型作为开源参考架构，并实现了自定义的多维原语，从而有效地并行化了密集三角更新（dense triangular updates）以及窗口批处理局部注意力（window-batched local attention）中不规则且依赖于数据的模式。我们的方法实现了高效的显存扩展；对于分布在 P 个 GPU 上的 N 个 token 输入，单设备显存占用呈 $O(N^2/P)$ 比例缩放，使得在 64 张 NVIDIA B300 GPU 上能够预测超过 30,000 个残基的复合物结构。我们通过成功的开发者用例展示了该方法的科学实用性：Fold-CP 实现了对哺乳动物蛋白复合物综合资源（CORUM）数据库中 90% 以上数据的评分，并完成了与内在无序区结合的疾病相关 PI4KA 脂质激酶复合物的折叠，且无需进行裁剪。通过为具有完整全局上下文的大规模系统建模提供可扩展的途径，Fold-CP 代表了迈向实现虚拟细胞的重要一步。

## Abstract
Understanding cellular machinery requires atomic-scale reconstruction of large biomolecular assemblies. However, predicting the structures of these systems has been constrained by hardware memory requirements of models like AlphaFold 3, imposing a practical ceiling of a few thousand residues that can be processed on a single GPU. Here we present NVIDIA BioNeMo Fold-CP, a context parallelism framework that overcomes this barrier by distributing the inference and training pipelines of co-folding models across multiple GPUs. We use the Boltz models as open source reference architectures and implement custom multidimensional primitives that efficiently parallelize both the dense triangular updates and the irregular, data-dependent pattern of window-batched local attention. Our approach achieves efficient memory scaling; for an N-token input distributed across P GPUs, per-device memory scales as $O(N^2/P)$, enabling the structure prediction of assemblies exceeding 30,000 residues on 64 NVIDIA B300 GPUs. We demonstrate the scientific utility of this approach through successful developer use cases: Fold-CP enabled the scoring of over 90% of Comprehensive Resource of Mammalian protein complexes (CORUM) database, as well as folding of disease-relevant PI4KA lipid kinase complex bound to an intrinsically disordered region without cropping. By providing a scalable pathway for modeling massive systems with full global context, Fold-CP represents a significant step toward the realization of a virtual cell.

---

## 论文详细总结（自动生成）

### 论文总结：Fold-CP —— 用于生物分子建模的上下文并行框架

#### 1. 论文的核心问题与整体含义
*   **研究动机**：以 AlphaFold 3 (AF3) 为代表的现代生物分子结构预测模型，虽然在准确性上取得了巨大突破，但面临严峻的**显存瓶颈**。由于模型内部的成对表示（Pair Representation）显存需求随序列长度 $N$ 呈 $O(N^2)$ 增长，计算量呈 $O(N^3)$ 增长，单张 GPU（如 A100 80GB）通常只能处理约 2000 个残基。
*   **核心问题**：现有的解决方法（如序列裁剪、串行分块或线性注意力近似）要么损失了全局上下文，要么降低了预测精度。论文旨在开发一种**上下文并行（Context Parallelism, CP）**框架，在不牺牲精度和不进行近似的前提下，将超大规模生物分子的计算任务分布到多个 GPU 上。

#### 2. 论文提出的方法论
*   **核心思想**：借鉴大语言模型（LLM）的并行经验，针对生物建模特有的多维流形（进化流形 MSA、序列流形、欧几里得流形）设计了 **3D 设备网格**（1D 数据并行 + 2D 上下文并行）。
*   **关键技术细节**：
    *   **多维激活分片（Sharding）**：将 $N \times N$ 的成对张量划分为 $\sqrt{P} \times \sqrt{P}$ 的瓷砖（Tiling），使单设备显存占用降至 $O(N^2/P)$。
    *   **自定义分布式算子**：
        *   **三角注意力/乘法**：采用环形（Ring）通信和 Cannon 风格算法，通过异步 P2P 传输掩盖通信延迟。
        *   **分布式窗口批处理**：利用 **Toeplitz 矩阵结构**优化原子级局部注意力，将索引矩阵显存从 $O(A^2)$ 降至 $O(1)$。
        *   **自定义 Autograd**：手动定义前向和反向传播路径，防止 PyTorch 原生分布式张量在反向传播时因自动执行 All-Gather 而导致显存溢出（OOM）。
    *   **内核融合**：集成 `cuEquivariance` 和 `FlexAttention` 算子，进一步提升计算效率。

#### 3. 实验设计
*   **数据集与场景**：
    *   **CORUM 数据库**：评估对已知哺乳动物蛋白复合物的覆盖能力。
    *   **PI4KA 脂质激酶复合物**（3,605 残基）：测试长序列且不裁剪情况下的预测精度。
    *   **Elongator 复合物**（4,685 残基）：验证多亚基组装的结构完整性。
    *   **5YZO 同源四聚体**：极端规模推理测试。
*   **Benchmark**：以开源的 **Boltz-1** 和 **Boltz-2** 模型作为参考架构。
*   **对比方法**：主要对比单 GPU 基准（DP）和传统的裁剪/分块方法，验证 Fold-CP 在数值上的一致性（Pearson $R=0.97$）和显存扩展的线性度。

#### 4. 资源与算力
*   **GPU 型号与数量**：
    *   使用了 **NVIDIA B300**（多达 64 块）进行推理扩展性测试。
    *   使用了 **NVIDIA H100 80GB**（多达 144 块）进行大规模推理和训练验证。
*   **性能表现**：
    *   在 64 块 B300 上实现了超过 **32,000 个 token** 的结构预测。
    *   在 144 块 H100 上实现了 24,000 token 的推理。
    *   **推理时长**：3,605 残基的 PI4KA 复合物单样本推理仅需约 54 秒。

#### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从数值一致性验证（Pearson 相关性分析）、训练收敛曲线对比，到多个真实科研案例（涉及 Rezo, Proxima, Earendil Labs 等多家机构的实际用例）。
*   **充分性与客观性**：实验设计较为充分，不仅验证了计算性能（显存、速度），还通过结构生物学指标（TM-score, lDDT, DockQ）验证了科学有效性。通过对比 DP 和 CP 的训练轨迹，证明了该框架在分布式环境下能保持模型原有的学习能力。

#### 6. 主要结论与发现
*   **显存线性扩展**：Fold-CP 成功将显存占用从 $O(N^2)$ 优化为 $O(N^2/P)$，打破了单机建模的尺寸天花板。
*   **科学发现**：在不裁剪的情况下，模型能够发现之前被忽略的远端相互作用（如 PI4KA 案例中的新结合位点）。
*   **长上下文挑战**：发现仅在短序列上训练的模型在长序列推理时会出现“链重叠/碰撞”现象，这表明**长上下文微调**是未来解决物理不合理性的必要手段。
*   **数据库覆盖**：Fold-CP 使 Boltz 模型能够处理 CORUM 数据库中 90% 以上的复合物，而此前仅能处理不到 30%。

#### 7. 优点
*   **无损扩展**：不依赖注意力近似，保持了 AF3 类模型的全全局上下文能力。
*   **系统级优化**：通过 Toeplitz 优化和自定义 Autograd，极大地提升了显存利用率和通信效率。
*   **高度集成**：与 NVIDIA 的 cuEquivariance 等底层库深度结合，充分发挥了 Hopper 和 Blackwell 架构的硬件性能。

#### 8. 不足与局限
*   **拓扑限制**：目前要求 GPU 数量为**平方数**（如 4, 16, 64），这在某些非标准集群配置下可能不够灵活。
*   **模型伪影**：虽然解决了计算问题，但模型在超长序列下仍可能预测出物理上不可能的结构（如原子重叠），这属于算法泛化问题而非并行框架问题。
*   **数据稀缺**：PDB 数据库中超大规模结构的真值数据较少，限制了长上下文训练的效果，未来可能需要依赖合成数据。

（完）
