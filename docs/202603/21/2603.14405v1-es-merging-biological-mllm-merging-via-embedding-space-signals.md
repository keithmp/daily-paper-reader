---
title: "ES-Merging: Biological MLLM Merging via Embedding Space Signals"
title_zh: ES-Merging：基于嵌入空间信号的生物多模态大语言模型合并
authors: "Wonbin Lee, Dongki Kim, Sung Ju Hwang"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14405v1"
tags: ["query:q7"]
score: 8.0
evidence: 生物多模态大语言模型
tldr: 针对生物多模态大语言模型（MLLM）在处理跨模态科学问题时受限于单一模态专业化的问题，本文提出 ES-Merging 框架。该方法摒弃了传统的参数空间启发式合并，转而利用嵌入空间信号，通过探测输入获取层级和元素级的响应来动态估计合并系数。实验证明，该方法在交互效应预测任务中表现优异，超越了现有合并技术及特定任务微调模型，为跨模态模型融合提供了新路径。
selection_source: fresh_fetch
motivation: 现有的模型合并方法多依赖于与输入无关的参数空间启发式规则，难以准确捕捉生物模型中的模态专业化特征。
method: 通过设计包含不同模态标记的探测输入，从嵌入空间中提取层级和元素级的细粒度信号来联合估计合并系数。
result: 在交互效应预测基准上，ES-Merging 的性能不仅优于传统合并方法，还超越了经过专门微调的模型。
conclusion: 研究表明嵌入空间信号是实现跨模态 MLLM 高效、稳健合并的有效基础。
---

## 摘要
生物多模态大语言模型（MLLMs）已成为科学发现的强大基础模型。然而，现有模型通常专注于单一模态，限制了它们解决固有跨模态科学问题的能力。虽然模型合并是将不同模态组合成统一 MLLM 的有效方法，但现有方法依赖于与输入无关的参数空间启发式算法，无法准确捕捉模态专业化。为了克服这一局限性，我们提出了一种表示感知的合并框架，该框架通过嵌入空间信号来估计合并系数。我们首先设计了一个由不同模态 token 组成的探测输入，并将其通过每个专业化的 MLLM，以获得反映模态特定表示变化的逐层嵌入响应。然后，我们从嵌入空间中估计两个粒度的互补合并系数：来自粗粒度信号的逐层系数和来自细粒度信号的逐元素系数，并将两者结合以进行鲁棒的系数估计。在相互作用效应预测基准上的实验表明，我们的方法优于现有的合并方法，甚至超过了特定任务的微调模型，证明了嵌入空间信号为跨模态 MLLM 合并提供了原则性且有效的基础。

## Abstract
Biological multimodal large language models (MLLMs) have emerged as powerful foundation models for scientific discovery. However, existing models are specialized to a single modality, limiting their ability to solve inherently cross-modal scientific problems. While model merging is an efficient method to combine the different modalities into a unified MLLM, existing methods rely on input-agnostic parameter space heuristics that fail to faithfully capture modality specialization. To overcome this limitation, we propose a representation-aware merging framework that estimates merging coefficients from embedding space signals. We first design a probe input that consists of different modality tokens and forward it through each specialized MLLM to obtain layer-wise embedding responses that reflect modality-specific representation changes. We then estimate complementary merging coefficients at two granularities from the embedding space: layer-wise coefficients from coarse-grained signals and element-wise coefficients from fine-grained signals, which are jointly combined for robust coefficient estimation. Experiments on interactive effect prediction benchmarks show that our method outperforms existing merging methods and even surpasses task-specific fine-tuned models, establishing that embedding space signals provide a principled and effective foundation for cross-modal MLLM merging.

---

## 论文详细总结（自动生成）

### ES-Merging：基于嵌入空间信号的生物多模态大语言模型合并总结

#### 1. 核心问题与整体含义（研究动机和背景）
生物科学发现日益依赖多模态大语言模型（MLLM），但现有的生物 MLLM 通常仅针对单一模态（如分子、蛋白质或细胞）进行专业化训练。然而，许多关键科学问题（如药物-靶点相互作用、药物对特定细胞的有效性）本质上是跨模态的。
*   **研究动机**：现有的模型合并方法（如 TIES-Merging, AdaMerging 等）主要依赖于参数空间的启发式信号（如参数的大小、符号或方向），这些信号与输入无关，难以准确捕捉模型对特定模态的专业化理解。
*   **核心问题**：如何有效地将多个单模态专业化的生物 MLLM 合并为一个统一的模型，使其能够处理复杂的跨模态交互任务。

#### 2. 方法论：核心思想与技术细节
论文提出了 **ES-Merging** 框架，将合并范式从“参数空间”转向“嵌入空间（Embedding Space）”。
*   **核心思想**：利用输入感知的嵌入空间信号来衡量模型的模态专业化程度。
*   **关键技术流程**：
    1.  **探测输入（Probe Input）**：设计一个包含多种模态 token（分子、蛋白质、细胞）的统一 Prompt。
    2.  **获取嵌入响应**：将探测输入分别输入基础 LLM 和各个专业化 MLLM，提取每一层的隐藏层表示（Embeddings）。
    3.  **双粒度系数估计**：
        *   **层级全局系数（Layer-wise）**：使用切片瓦瑟斯坦距离（Sliced Wasserstein Distance, SWD）计算专业化模型相对于基础模型在每一层产生的分布偏移，识别哪些层对特定模态最重要。
        *   **元素级局部系数（Element-wise）**：通过计算嵌入空间距离对参数的梯度，衡量每个参数元素对表示变化的敏感度。
    4.  **系数集成**：将层级和元素级系数结合并进行归一化，得到最终的 LoRA 合并权重，用于加权融合各模型的参数。

#### 3. 实验设计
*   **数据集与场景**：
    *   **实例变化交互预测**：分子-蛋白质相互作用（BindingDB, BioSNAP, Human）、分子-细胞相互作用（DrugComb, GDSC2）。
    *   **目标固定功能预测**：CYP 酶抑制预测（5 个子任务）、CYP 酶底物预测（3 个子任务）。
*   **基准模型（Baselines）**：
    *   **基础与专业模型**：LLaMA-3.1-8B-Instruct、Mol-LLaMA（分子）、Prot2Text-V2（蛋白）、Cell-o1（细胞）。
    *   **合并方法**：Average Merging、TIES-Merging、EMR-Merging、AdaMerging、PCB-Merging、Consensus Merging、LS-Merge。
    *   **微调对比**：平均合并后进行特定任务微调（Avg. Merging + FT）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号（如 A100 或 H100）及具体训练/合并的总时长。
*   **计算效率**：论文在表 4 中对比了总浮点运算量（FLOPs）。ES-Merging 的计算成本显著低于 AdaMerging（约 1/3.4）和特定任务微调（约 1/6.1），因为它仅需一次梯度计算，而无需迭代更新参数。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 10 多个生物学子数据集，涉及多种跨模态组合。
*   **消融实验**：针对层级系数、元素级系数及其组合进行了详细的消融研究（表 3 和表 8）。
*   **充分性评价**：实验设计较为充分，不仅对比了多种最先进的合并算法，还通过可视化（嵌入空间分布、系数热力图）和案例研究（Case Study）验证了模型推理的逻辑性。对比实验在相同的指令模板和 Few-shot 设置下进行，体现了公平性。

#### 6. 主要结论与发现
*   **性能领先**：ES-Merging 在所有跨模态交互任务中均优于现有的参数空间合并方法。
*   **超越微调**：在某些任务上，ES-Merging 甚至超过了经过特定任务微调的模型，且保留了更好的生物学推理能力（微调往往导致推理能力退化）。
*   **信号有效性**：实验证明嵌入空间信号比参数空间统计数据更能反映模态专业化，层级和元素级信号具有互补性。

#### 7. 优点（亮点）
*   **表示感知**：首次将嵌入空间信号引入 MLLM 合并，解决了输入无关合并方法的局限性。
*   **多粒度融合**：结合了粗粒度的层级重要性和细粒度的参数敏感性，使合并系数更稳健。
*   **高效且可解释**：计算成本低，且合并后的模型能生成具有生物学意义的推理链，而非仅仅输出标签。

#### 8. 不足与局限
*   **领域局限性**：目前仅在生物化学领域（分子、蛋白、细胞）验证，尚未在通用的视觉-语言或音视频多模态任务中测试。
*   **单模态性能保持**：论文主要关注跨模态融合性能，未深入探讨合并后模型在原始单模态任务上是否存在性能回退（Catastrophic Forgetting）。
*   **超参数依赖**：Softmax 温度参数 $\tau$ 和 SWD 投影维度等超参数可能需要针对不同模型进行微调。

（完）
