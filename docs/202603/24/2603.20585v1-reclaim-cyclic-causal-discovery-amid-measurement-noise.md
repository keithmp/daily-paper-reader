---
title: "RECLAIM: Cyclic Causal Discovery Amid Measurement Noise"
title_zh: RECLAIM：测量噪声环境下的有环因果发现
authors: "Muralikrishnna G. Sethuraman, Faramarz Fekri"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20585v1"
tags: ["query:q7"]
score: 8.0
evidence: 基因组学中循环调节网络的因果发现
tldr: 本研究针对因果发现中常见的无环假设和无噪声假设在现实场景（如基因组学）中失效的问题，提出了RECLAIM框架。该框架能够同时处理循环因果结构和测量噪声，通过期望最大化（EM）算法和残差归一化流实现似然函数最大化，并提供了理论一致性保证。实验证明其在合成数据和蛋白质信号数据集中均表现优异，为复杂系统建模提供了新工具。
selection_source: fresh_fetch
motivation: 现有的因果发现方法通常假设系统无环且无测量误差，这在存在循环调节和仪器噪声的实际科学领域（如基因组学）中难以满足。
method: 提出RECLAIM框架，结合期望最大化（EM）算法与残差归一化流，在两种不同的高斯噪声测量模型下通过最大化似然来学习因果图。
result: 在合成数据和真实蛋白质信号数据集上的实验验证了该方法的有效性，并为其在两种设置下的统计一致性提供了理论证明。
conclusion: RECLAIM是一个能够同时处理循环结构和测量噪声的因果发现框架，为解决现实世界中复杂的因果推断问题提供了稳健的方案。
---

## 摘要
揭示因果关系是科学和工程领域的一个基本问题。然而，大多数现有的因果发现方法假设无环性并假设可以直接访问系统变量，这些假设在许多现实场景中往往无法成立。例如，在基因组学中，有环调节网络非常普遍，且测量结果往往受到仪器噪声的干扰。为了应对这些挑战，我们提出了 RECLAIM，这是一个能够原生处理环路和测量噪声的因果发现框架。RECLAIM 通过期望最大化（EM）算法最大化观测测量值的似然来学习因果图结构，并利用残差归一化流（residual normalizing flows）进行可计算的似然估计。我们考虑了两种测量模型：(i) 高斯加性噪声，以及 (ii) 带有高斯加性噪声的线性测量系统。我们为这两种设置都提供了理论一致性保证。在合成数据和真实蛋白质信号传导数据集上的实验证明了所提方法的有效性。

## Abstract
Uncovering causal relationships is a fundamental problem across science and engineering. However, most existing causal discovery methods assume acyclicity and direct access to the system variables -- assumptions that fail to hold in many real-world settings. For instance, in genomics, cyclic regulatory networks are common, and measurements are often corrupted by instrumental noise. To address these challenges, we propose RECLAIM, a causal discovery framework that natively handles both cycles and measurement noise. RECLAIM learns the causal graph structure by maximizing the likelihood of the observed measurements via expectation-maximization (EM), using residual normalizing flows for tractable likelihood computation. We consider two measurement models: (i) Gaussian additive noise, and (ii) a linear measurement system with additive Gaussian noise. We provide theoretical consistency guarantees for both the settings. Experiments on synthetic data and real-world protein signaling datasets demonstrate the efficacy of the proposed method.

---

## 论文详细总结（自动生成）

这是一份关于论文《RECLAIM: Cyclic Causal Discovery Amid Measurement Noise》的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：传统的因果发现方法通常依赖两个强假设：(1) 因果图是**无环**的（DAG）；(2) 系统变量是**直接观测**到的。然而在生物学（如基因组学）等现实场景中，反馈回路（环路）普遍存在，且观测数据往往包含仪器噪声或属于间接测量。
*   **核心问题**：如何在存在**测量噪声**且允许**因果环路**的情况下，从干预性数据中准确恢复潜在变量之间的非线性因果结构。
*   **整体含义**：论文提出了 RECLAIM 框架，填补了同时处理“循环结构”与“测量误差”的因果发现技术空白。

### 2. 论文提出的方法论
*   **核心思想**：将因果发现建模为一个含有隐变量的似然最大化问题，通过**期望最大化（EM）算法**迭代优化。
*   **关键技术细节**：
    *   **结构因果模型 (SCM)**：使用收缩性神经网络（Contractive NN）建模非线性因果机制，确保有环系统能够达到唯一的平衡点。
    *   **测量模型**：支持两种模型：(i) 高斯加性噪声；(ii) 线性测量系统（观测维度可大于潜在维度）。
    *   **EM 流程**：
        *   **E步**：利用**采样重要性重采样 (SIR)** 估计给定观测值 $Y$ 时潜在变量 $X$ 的后验分布。
        *   **M步**：最大化期望完整数据对数似然，并加入 $L_0$ 型稀疏惩罚。
    *   **噪声估计**：提出了一种基于干预数据的噪声方差一致性估计方法。通过要求每个节点至少被干预一次（条件2），利用投影向量分离噪声。
    *   **似然计算**：利用残差归一化流（Residual Normalizing Flows）和 Hutchinson 迹估计器、俄罗斯轮盘赌估计器来高效、无偏地计算雅可比行列式的对数。

### 3. 实验设计
*   **数据集/场景**：
    *   **合成数据**：基于 Erdős-Rényi 模型生成有环图，使用 $tanh$ 非线性函数生成数据。
    *   **真实数据**：Sachs 等人 (2005) 的蛋白质信号传导数据集（包含 11 种蛋白质在 13 种干预环境下的测量值）。
*   **Benchmark 与对比方法**：
    *   **NODAGS-Flow**：处理环路但不处理测量噪声。
    *   **DCDI**：处理干预但不处理环路和噪声。
    *   **Anchored-CI**：处理测量噪声但假设无环且仅限观测数据。
*   **评估指标**：AUPRC（准确率-召回率曲线下面积）和 SHD（结构汉明距离）。

### 4. 资源与算力
*   **硬件**：实验使用了 **NVIDIA RTX 6000 GPU**。
*   **训练细节**：论文提到在 Sachs 数据集上训练了 200 个 epoch。虽然未详细列出总算力小时数，但其基于梯度优化的微分框架表明其在大规模图上的扩展性优于传统的约束法。

### 5. 实验数量与充分性
*   **实验规模**：
    *   测试了不同噪声水平（$\sigma_{min}$ 从 0.1 到 0.9）。
    *   测试了不同图规模（节点数 $d$ 从 10 到 50）。
    *   测试了不同测量冗余度（测量数 $p$ 从 10 到 20）。
    *   进行了关于环路数量、非线性程度和图稀疏性的消融/敏感性分析。
*   **充分性评价**：实验设计非常充分且客观。每组实验均进行了 10 次独立重复实验并标注了标准差，覆盖了从简单到复杂的多种参数变化，对比基准涵盖了当前最相关的各类算法。

### 6. 论文的主要结论与发现
*   **性能优势**：在所有含噪声的场景中，RECLAIM 的表现均显著优于不考虑噪声的基准方法（如 NODAGS-Flow）。
*   **环路恢复**：在 Sachs 数据集中，RECLAIM 成功恢复了 Raf $\leftrightarrow$ Mek 环路，这符合已知的生物学反馈机制，而基于 DAG 的方法在结构上无法实现这一点。
*   **冗余获益**：在线性测量系统中，增加测量维度 $p$ 能显著提升恢复精度，证明了该方法能有效利用冗余信息。
*   **理论保证**：证明了在满足干预覆盖条件时，该方法具有统计一致性，能收敛至真实的 I-马尔可夫等价类。

### 7. 优点
*   **综合性强**：首次在统一的微分概率框架下解决了循环因果、非线性关系和测量噪声的共同挑战。
*   **理论严密**：不仅提出了算法，还提供了关于噪声可辨识性和结构恢复一致性的数学证明。
*   **鲁棒性高**：实验表明，随着噪声增加，RECLAIM 的性能下降比其他方法更平缓。

### 8. 不足与局限
*   **干预依赖**：噪声参数的估计高度依赖于干预数据的覆盖范围（每个节点需被干预），在纯观测数据或干预受限的情况下可能失效。
*   **噪声假设**：目前主要假设测量噪声为高斯分布，对于非高斯噪声（如计数数据的泊松噪声）的适用性有待验证。
*   **计算开销**：由于涉及 EM 迭代和 SIR 采样，其计算成本高于直接观测下的因果发现方法。
*   **收缩性限制**：为了保证环路系统的稳定性，对神经网络权重施加了收缩性约束，这可能在一定程度上限制了模型表达极其复杂的非线性动力系统的能力。

（完）
