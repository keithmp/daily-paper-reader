---
title: "Cross-Granularity Representations for Biological Sequences: Insights from ESM and BiGCARP"
title_zh: 生物序列的跨粒度表示：来自 ESM 和 BiGCARP 的启示
authors: "Hanlin Xiao, Rainer Breitling, Eriko Takano, Mauricio A. Álvarez"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20825v1"
tags: ["query:q7"]
score: 8.0
evidence: 用于核苷酸和基因等生物序列的AI模型
tldr: 本研究探讨了生物序列模型中跨粒度知识的整合，以BiGCARP（结构域级）和ESM（氨基酸级）为案例。研究发现深层嵌入能捕捉更丰富的上下文信息，且不同粒度的表示具有互补性。通过整合跨粒度特征，显著提升了中间层级预测任务的性能，为增强生物基础模型的性能和可解释性提供了有效策略。
selection_source: fresh_fetch
motivation: 探索如何有效整合生物序列中不同层级（如氨基酸和蛋白质结构域）的粒度信息以提升模型能力。
method: 利用表征分析和探测任务，对比研究了BiGCARP和ESM模型在不同层级的嵌入特征及其融合效果。
result: 证实了深层嵌入包含更丰富的生物学上下文，且跨粒度表示的结合在中间层级任务中表现出明显的性能增益。
conclusion: 跨粒度表示整合是优化生物基础模型性能并增强其生物学解释性的重要方向。
---

## 摘要
通用基础模型的最新进展推动了大型生物序列模型的发展。虽然自然语言表现出符号粒度（字符、词、句子），但生物序列呈现出层次化粒度，其各个层级（核苷酸、氨基酸、蛋白质结构域、基因）进一步编码了生物功能信息。在本文中，我们通过对 BiGCARP（一种针对生物合成基因簇的 Pfam 结构域级模型）和 ESM（一种氨基酸级蛋白质语言模型）的案例研究，探讨了模型间跨粒度知识的整合。利用表示分析工具和一系列探测任务，我们首先解释了为什么简单的跨模型嵌入初始化未能提升 BiGCARP 的下游性能，并表明深层嵌入捕捉到了模型所学知识中更具上下文相关性且更真实的表示。此外，我们证明了不同粒度的表示编码了互补的生物学知识，并且将它们结合起来可以在中间层级的预测任务中产生显著的性能提升。我们的研究结果强调，跨粒度整合是提高生物基础模型性能和可解释性的一种极具前景的策略。

## Abstract
Recent advances in general-purpose foundation models have stimulated the development of large biological sequence models. While natural language shows symbolic granularity (characters, words, sentences), biological sequences exhibit hierarchical granularity whose levels (nucleotides, amino acids, protein domains, genes) further encode biologically functional information. In this paper, we investigate the integration of cross-granularity knowledge from models through a case study of BiGCARP, a Pfam domain-level model for biosynthetic gene clusters, and ESM, an amino acid-level protein language model. Using representation analysis tools and a set of probe tasks, we first explain why a straightforward cross-model embedding initialization fails to improve downstream performance in BiGCARP, and show that deeper-layer embeddings capture a more contextual and faithful representation of the model's learned knowledge. Furthermore, we demonstrate that representations at different granularities encode complementary biological knowledge, and that combining them yields measurable performance gains in intermediate-level prediction tasks. Our findings highlight cross-granularity integration as a promising strategy for improving both the performance and interpretability of biological foundation models.

---

## 论文详细总结（自动生成）

这篇论文深入探讨了生物序列模型中不同层级（粒度）信息的整合问题，以下是对该研究的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：生物序列（如DNA、蛋白质、基因簇）具有天然的层次化粒度（核苷酸 $\rightarrow$ 氨基酸 $\rightarrow$ 蛋白质结构域 $\rightarrow$ 基因/基因簇）。现有的生物基础模型通常只专注于单一粒度（如 ESM 关注氨基酸级，BiGCARP 关注结构域级）。
*   **研究动机**：虽然 BiGCARP 尝试通过 ESM 初始化其嵌入矩阵来引入细粒度知识，但效果并不理想。论文旨在探究：
    1. 为什么简单的跨模型初始化会失效？
    2. 不同粒度的表示是否具有互补性？
    3. 如何有效整合这些跨粒度知识以提升模型性能和可解释性？

### 2. 论文提出的方法论
*   **核心思想**：通过表征分析（Representation Analysis）揭示模型内部知识演化，并验证跨粒度特征融合的有效性。
*   **关键技术细节**：
    *   **模型选择**：选取 **ESM**（基于 Transformer 的氨基酸级模型）和 **BiGCARP**（基于空洞卷积的 Pfam 结构域级模型）作为研究对象。
    *   **表征提取策略**：对比了“嵌入层（Embedder layer，静态）”和“最终层（Last-layer，上下文相关）”的表示。
    *   **分析工具**：
        *   **UMAP**：用于高维嵌入的可视化，观察结构域是否按功能聚类。
        *   **CKA（中心核对齐）**：定量测量不同层级、不同初始化方案之间表示的相似性，追踪预训练知识在模型中的保留情况。
    *   **融合方式**：采用简单的特征拼接（Concatenation）将 ESM 的局部细粒度表示与 BiGCARP 的全局粗粒度表示结合。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：使用 antiSMASH-DB（约 14.7 万条序列）。
    *   **下游探测任务**：使用 MIBiG 3.0（人工标注的实验验证数据集）。
*   **探测任务（Probe Tasks）**：
    1.  **BGC 产物类别预测（全局任务）**：预测基因簇合成的天然产物类别（如聚酮、非核糖体肽等）。
    2.  **A-结构域底物预测（局部任务）**：预测腺苷化结构域的具体底物特异性。
    3.  **卤化产物预测（中间层级任务）**：预测含有特定结构域（PF04820）的基因簇是否产生卤化产物。
*   **对比方法（Baselines）**：随机初始化模型、Pfam2Vec + 随机森林、纯 ESM 嵌入、纯 BiGCARP 嵌入。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或训练总时长。但提到 BiGCARP 基于卷积架构，相比 Transformer 在处理长序列（基因簇）时更具效率。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对三个不同层级的生物学任务进行了评估。
    *   使用了分层 5 折交叉验证，并针对每个评估重复 10 个随机种子以确保统计显著性。
    *   在样本量极小的卤化预测任务（46 个样本）中，采用了留一法交叉验证（LOOCV）和 10,000 次 Bootstrap 重采样。
*   **充分性评价**：实验设计较为客观、公平。通过 CKA 分析从理论层面解释了初始化失效的原因，并结合可视化和多尺度任务验证了结论，逻辑链条完整。

### 6. 论文的主要结论与发现
*   **初始化失效的原因**：CKA 分析显示，BiGCARP 的早期层（尤其是第一层空洞卷积）会迅速重塑表示空间，导致 ESM 初始化的先验知识在深层被“覆盖”或丢失。
*   **深层表示更优**：模型的最终层嵌入比初始嵌入层包含更丰富的生物上下文信息，在下游任务中表现更好。
*   **跨粒度互补性**：ESM 擅长捕捉局部结构和序列细节，而 BiGCARP 擅长捕捉全局功能背景。
*   **性能提升**：在中间层级任务（如卤化预测）和全局任务中，简单拼接两种粒度的表示即可获得显著的性能增益（如卤化预测的 AUROC 从 0.659 提升至 0.759）。

### 7. 优点
*   **深入的表征分析**：不仅关注性能指标，还利用 CKA 和 UMAP 深入探讨了模型“学到了什么”以及“知识如何流动”。
*   **多尺度视角**：将生物序列的层次性与 NLP 的 Tokenization 类比，为生物基础模型的设计提供了新的理论视角。
*   **生物学解释性**：通过对 UMAP 聚类的详细分析，证明了模型能够识别出不同尺度的功能模块（如全局调节 vs 局部修饰）。

### 8. 不足与局限
*   **融合策略简单**：仅使用了简单的特征拼接，未探索更高级的融合机制（如交叉注意力机制或联合预训练）。
*   **数据规模限制**：部分探测任务（如卤化预测）样本量极小，虽然使用了统计手段弥补，但结论的普适性仍需在大规模数据集上进一步验证。
*   **应用范围**：研究目前仅局限于生物合成基因簇（BGC）领域，尚未扩展到非编码区、全基因组建模等更广泛的生物学场景。

（完）
