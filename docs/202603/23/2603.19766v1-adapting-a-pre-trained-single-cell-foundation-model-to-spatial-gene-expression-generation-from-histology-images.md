---
title: Adapting a Pre-trained Single-Cell Foundation Model to Spatial Gene Expression Generation from Histology Images
title_zh: 将预训练单细胞基础模型应用于组织学图像的空间基因表达生成
authors: "Donghai Fang, Yongheng Li, Zhen Wang, Yuansong Zeng, Wenwen Min"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19766v1"
tags: ["query:q7"]
score: 8.0
evidence: 利用单细胞基础模型进行空间基因表达生成
tldr: 针对空间转录组学成本高、通量低的问题，本文提出HINGE框架，旨在从HE染色组织图像预测基因表达。HINGE通过改造预训练单细胞基础模型（sc-FM），引入SoftAdaLN调制层注入视觉上下文，并结合掩码扩散目标和预热课程学习，实现了保留基因间依赖关系的条件表达生成。在三个数据集上的实验证明，该方法在预测准确性、空间标志物模式和共表达一致性方面均优于现有技术，为利用基础模型进行空间表达建模提供了新路径。
selection_source: fresh_fetch
motivation: 现有的生成式方法往往忽略了基因间的生物学依赖关系，且难以直接将缺乏视觉通路的单细胞基础模型应用于组织图像驱动的表达预测。
method: 提出HINGE框架，利用SoftAdaLN模块将视觉上下文注入预训练模型，并结合表达空间掩码扩散目标和预热课程进行训练。
result: 在三个空间转录组数据集上的评估显示，该方法在皮尔逊相关系数、空间标志物模式准确度和基因共表达一致性上均优于现有最先进基准。
conclusion: 本研究成功实现了将预训练单细胞基础模型适配为组织图像条件下的空间基因表达生成器，提升了生物学一致性和预测精度。
---

## 摘要
空间转录组学 (ST) 实现了位点级的原位表达谱分析，但其高昂的成本和有限的通量促使人们探索直接从 HE 染色组织学图像预测基因表达。最近的进展探索了使用基于评分 (score-based) 或流 (flow-based) 的生成模型来估计基于组织学图像的基因表达条件分布，为确定性回归方法提供了一种灵活的替代方案。然而，大多数现有的生成方法忽略了对基因间依赖关系的显式建模，从而损害了生物学一致性。在多种细胞群体中预训练的单细胞基础模型 (sc-FMs) 捕捉到了这些仅凭组织学图像无法揭示的关键基因关系。然而，由于缺乏视觉路径、预训练与条件 ST 目标之间的不匹配，以及混合细胞 ST 监督数据的稀缺，将仅针对表达数据的 sc-FMs 应用于组织学条件下的表达建模并非易事。为了应对这些挑战，我们提出了 HINGE (HIstology-coNditioned GEneration)，它将预训练的 sc-FM 改造为条件表达生成器，同时在很大程度上保留了其学习到的基因关系。我们通过引入 SoftAdaLN 来实现这一点，这是一种轻量级的、恒等初始化的调制模块，可将逐层视觉上下文注入骨干网络，并结合表达空间掩码扩散目标和热启动课程学习，以确保目标对齐和训练稳定性。在三个 ST 数据集上的评估结果表明，我们的方法在平均皮尔逊相关系数上优于最先进的基准模型，并产生了更准确的空间标志物表达模式和更高的成对共表达一致性，为将预训练 sc-FMs 应用于组织学条件下的空间表达生成建立了一条切实可行的路径。

## Abstract
Spatial transcriptomics (ST) enables spot-level in situ expression profiling, but its high cost and limited throughput motivate predicting expression directly from HE-stained histology. Recent advances explore using score- or flow-based generative models to estimate the conditional distribution of gene expression from histology, offering a flexible alternative to deterministic regression approaches. However, most existing generative approaches omit explicit modeling of gene-gene dependencies, undermining biological coherence. Single-cell foundation models (sc-FMs), pre-trained across diverse cell populations, capture these critical gene relationships that histology alone cannot reveal. Yet, applying expression-only sc-FMs to histology-conditioned expression modeling is nontrivial due to the absence of a visual pathway, a mismatch between their pre-training and conditional ST objectives, and the scarcity of mixed-cell ST supervision. To address these challenges, we propose HINGE (HIstology-coNditioned GEneration), which retrofits a pre-trained sc-FM into a conditional expression generator while mostly preserving its learned gene relationships. We achieve this by introducing SoftAdaLN, a lightweight, identity-initialized modulation that injects layer-wise visual context into the backbone, coupled with an expression-space masked diffusion objective and a warm-start curriculum to ensure objective alignment and training stability. Evaluated on three ST datasets, ours outperforms state-of-the-art baselines on mean Pearson correlation and yields more accurate spatial marker expression patterns and higher pairwise co-expression consistency, establishing a practical route to adapt pre-trained sc-FMs for histology-conditioned spatial expression generation.

---

## 论文详细总结（自动生成）

以下是对论文《Adapting a Pre-trained Single-Cell Foundation Model to Spatial Gene Expression Generation from Histology Images》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何利用预训练的单细胞基础模型（sc-FM）中蕴含的丰富基因间依赖关系，来提升从组织学图像（H&E 染色切片）预测空间基因表达的准确性和生物学一致性。
*   **研究背景**：空间转录组学（ST）成本高、通量低。虽然现有方法尝试从组织图像预测表达，但大多采用确定性回归，且忽略了基因间的调控关系。
*   **挑战**：
    1.  **模态鸿沟**：sc-FM 仅在表达空间预训练，缺乏视觉通路。
    2.  **目标不匹配**：sc-FM 通常使用掩码自编码（MAE），而 ST 预测多用回归或高斯扩散。
    3.  **成分偏移**：单细胞数据与 ST 的混合细胞位点之间存在表达差异。
    4.  **监督有限**：ST 数据规模小，直接微调易导致基础模型“灾难性遗忘”。

### 2. 方法论
论文提出了 **HINGE**（HIstology-coNditioned GEneration）框架，核心思想是“改造而非重塑”预训练模型：
*   **核心架构**：以冻结的 **CellFM**（一种基于 Transformer 的单细胞基础模型）为骨干，通过轻量级模块引入视觉条件。
*   **关键技术细节**：
    *   **SoftAdaLN（软自适应层归一化）**：在 Transformer 层中插入恒等初始化的调制层。它通过线性层将组织学特征（由 UNI 和 CONCH 编码器提取）和扩散步长注入模型。由于是恒等初始化，训练初期模型行为与预训练时完全一致，有效防止了知识丢失。
    *   **表达空间掩码扩散（Masked Diffusion）**：不同于传统的高斯扩散，HINGE 模拟了一个随机掩码过程。前向过程逐渐遮盖基因，反向过程则根据图像条件逐步预测被遮盖的基因。这在数学形式上与 sc-FM 的掩码自编码预训练目标高度对齐。
    *   **热启动课程学习（Warm-start Curriculum）**：在训练初期仅采样低掩码率的时间步，使模型先适应低难度的重建任务，随后再过渡到全范围扩散，增强了训练稳定性。

### 3. 实验设计
*   **数据集**：
    1.  **cSCC**（皮肤鳞状细胞癌）：12 张切片，4 名患者。
    2.  **Her2ST**（乳腺癌）：36 张切片，8 名患者。
    3.  **Kidney**（人类肾脏 Visium 数据）：23 张切片，22 名患者。
*   **评估协议**：采用“留一法”（Leave-one-slice-out），确保测试集切片在训练中完全不可见。
*   **对比基准（Baselines）**：
    *   **判别式方法**：ST-Net, BLEEP, TRIPLEX, MERGE。
    *   **生成式方法**：Stem, STFlow。
*   **评估指标**：高变基因的皮尔逊相关系数（PCC-50/200）、均方误差（MSE）、平均绝对误差（MAE）以及空间标志物模式的 SSIM。

### 4. 资源与算力
*   **硬件**：使用了 **NVIDIA A800 GPU**。
*   **软件环境**：PyTorch 2.1.0, CUDA 12.1。
*   **训练细节**：
    *   全局 Batch Size 为 32。
    *   使用 AdamW 优化器，学习率为 1e-4。
    *   最大训练 50 个 Epoch，包含早停机制（Early Stopping）。
    *   推理效率：在 T=50 步时，吞吐量约为 2.48 spots/s；若降低步数至 T=5，可提升至 26.46 spots/s，且精度损失极小。

### 5. 实验数量与充分性
*   **实验规模**：在三个不同组织、不同平台（ST 和 Visium）的数据集上进行了全面测试。
*   **消融实验**：非常充分。作者分别验证了：
    1.  预训练权重的必要性（对比从头训练、LoRA 等）。
    2.  掩码扩散相对于高斯扩散的优越性。
    3.  SoftAdaLN 各组件（SoftNorm、恒等初始化）的作用。
    4.  不同视觉编码器（UNI, CONCH）的贡献。
*   **客观性**：使用了三个随机种子取平均值，并进行了配对 Wilcoxon 符号秩检验以验证统计显著性，实验设计严谨、公平。

### 6. 主要结论与发现
*   **性能领先**：HINGE 在所有数据集上的 PCC 指标均达到 SOTA，尤其在 cSCC 数据集上比最强基准提升了约 4%-6%。
*   **生物学保真度**：HINGE 生成的标志物基因（如 KRT6A, GNAS）空间分布与真实值（Ground Truth）最接近，且能更好地还原基因间的共表达矩阵。
*   **知识迁移成功**：实验证明，即使 ST 数据有限，通过合理的调制设计，也能成功将单细胞层面的基因关系迁移到空间组织建模中。

### 7. 优点（亮点）
*   **首创性**：首次提出将仅含表达数据的单细胞基础模型适配为组织学驱动的条件生成模型。
*   **架构优雅**：SoftAdaLN 的设计既实现了跨模态对齐，又通过恒等初始化巧妙解决了灾难性遗忘问题。
*   **目标对齐**：掩码扩散的设计从数学逻辑上统一了生成式建模与掩码自编码预训练，提高了迁移效率。
*   **实用性**：支持推理步数的灵活调整，在精度和速度之间提供了良好的权衡。

### 8. 不足与局限
*   **基因词汇限制**：受限于预训练模型（CellFM）的固定词汇表（约 2.4 万个基因），无法处理该表之外的罕见基因。
*   **计算开销**：作为生成式模型，其推理速度虽可调，但相比于简单的单步回归模型（如 ST-Net），计算成本仍然较高。
*   **跨组学偏移**：虽然引入了视觉条件，但单细胞测序与空间转录组测序之间的技术偏差（Batch Effect）仍可能对预测结果产生潜在影响。

（完）
