---
title: Quantum Pattern Matching in Generalised Degenerate Strings
title_zh: 广义退化字符串中的量子模式匹配
authors: "Massimo Equi, Md Rabiul Islam Khan, Veli Mäkinen"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16297v1"
tags: ["query:pangen"]
score: 6.0
evidence: 基因组背景下的字符串匹配计算方法
tldr: "本研究探讨了广义退化（GD）字符串中的模式匹配问题，GD字符串是字符串集合的序列。针对在GD字符串中寻找模式字符串精确匹配的任务，传统算法的时间复杂度为 $O(mn+N)$。本文提出了一种基于量子计算模型的改进算法，通过量子加速技术，将运行时间优化至 $\\tilde{O}(\\sqrt{mnN})$，为处理复杂字符串结构的匹配问题提供了更高效的量子方案。"
selection_source: fresh_fetch
motivation: 旨在利用量子计算的优势，提升在广义退化字符串中进行精确模式匹配的计算效率。
method: 将现有的经典匹配算法进行量子化改造，使其适用于量子计算模型。
result: "成功将匹配算法的时间复杂度从经典 $O(mn+N)$ 降低到了量子环境下的 $\\tilde{O}(\\sqrt{mnN})$。"
conclusion: 该研究证明了量子算法在处理广义退化字符串匹配任务上具有显著的性能提升潜力。
---

## 摘要
退化字符串是字符集合的序列。广义退化 (GD) 字符串将这一概念扩展到字符串集合的序列，其中同一集合中的字符串长度相等。在 GD 字符串中寻找模式字符串的精确匹配可以在 $O(mn+N)$ 时间内完成 (Ascone 等人，WABI 2024)，其中 $m$ 是模式长度，$n$ 是字符串数量，$N$ 是构成 GD 字符串的字符串总长度。我们修改了该算法以在量子计算模型下运行，实现了 $\tilde{O}(\sqrt{mnN})$ 的运行时间。

## Abstract
A degenerate string is a sequence of sets of characters. A generalized degenerate (GD) string extends this notion to the sequence of sets of strings, where strings of the same set are of equal length. Finding an exact match for a pattern string inside a GD string can be done in $O(mn+N)$ time (Ascone et al., WABI 2024), where $m$ is the pattern length, $n$ is the number of strings and $N$ the total length of strings constituting the GD string. We modify this algorithm to work under a quantum model of computation, achieving running time $\tilde{O}(\sqrt{mnN})$.