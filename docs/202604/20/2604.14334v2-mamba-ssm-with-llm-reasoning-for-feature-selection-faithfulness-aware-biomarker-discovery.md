---
title: "Mamba-SSM with LLM Reasoning for Feature Selection: Faithfulness-Aware Biomarker Discovery"
title_zh: 结合大语言模型推理与 Mamba-SSM 的特征选择：忠实度感知的生物标志物发现
authors: "Pushpa Kumar Balan, Aijing Feng"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.14334v2"
tags: ["query:q7"]
score: 9.0
evidence: 利用AI和LLM推理在RNA-seq数据中发现生物标志物
tldr: 本研究针对深度序列模型在生物标志物发现中易受组织成分干扰的问题，提出结合大语言模型（LLM）推理进行特征选择。通过在TCGA-BRCA数据集上训练Mamba模型并利用DeepSeek-R1的思维链推理过滤梯度显著性基因，成功将特征数减少至17个，且分类性能（AUC 0.927）显著优于原始集合及高方差基准。研究揭示了“选择性忠实度”现象，即针对性剔除干扰项虽未实现全面召回，但能有效提升预测性能。
selection_source: fresh_fetch
motivation: 深度学习模型提取的生物标志物常包含组织成分混淆因素，限制了下游分类器的准确性。
method: 利用Mamba模型提取梯度显著性基因，并结合DeepSeek-R1的思维链推理对候选基因进行结构化过滤。
result: LLM过滤后的17个基因集以极少的特征量实现了0.927的AUC，性能超越了包含5000个基因的方差基准。
conclusion: 针对性剔除混淆因素能显著提升预测性能，证明了在特征选择中“选择性忠实度”比全面召回更具实际价值。
---

## 摘要
深度序列模型的梯度显著性可以高效地识别候选生物标志物，但生成的基因列表可能会受到组织成分混杂因素的污染，从而降低下游分类器的性能。我们研究了大语言模型（LLM）的思维链（CoT）推理是否能够过滤这些混杂因素，以及推理质量是否与下游性能相关。我们在 TCGA-BRCA RNA-seq 数据集上训练了一个 Mamba SSM，并通过梯度显著性提取了前 50 个基因；随后，DeepSeek-R1 利用结构化思维链对每个候选基因进行评估，最终生成了一个包含 17 个基因的集合。在留出测试集上，原始的 50 基因显著性集合（无 LLM）的表现差于 5000 基因方差基准（AUC 0.832 对比 0.903），而 LLM 过滤后的集合则超越了该基准（AUC 0.927），且使用的特征数量减少了 294 倍。忠实度审计（参考 COSMIC CGC、OncoKB、PAM50）显示，选出的 17 个基因中有 6 个（35.3%）是已验证的 BRCA 生物标志物，而输入中存在的 16 个已知 BRCA 基因中有 10 个被遗漏——包括 FOXA1。下游性能与推理忠实度之间的这种背离表明，在这种情况下存在“选择性忠实度”：针对性的混杂因素去除可以在不具备全面召回率的情况下提高预测性能。

## Abstract
Gradient saliency from deep sequence models surfaces candidate biomarkers efficiently, but the resulting gene lists can be contaminated by tissue-composition confounders that degrade downstream classifiers. We study whether LLM chain-of-thought (CoT) reasoning can filter these confounders, and whether reasoning quality is associated with downstream performance. We train a Mamba SSM on TCGA-BRCA RNA-seq and extract the top-50 genes by gradient saliency; DeepSeek-R1 evaluates every candidate with structured CoT to produce a final 17-gene set. On the held-out test split, the raw 50-gene saliency set (no LLM) performs worse than a 5,000-gene variance baseline (AUC 0.832 vs. 0.903), while the LLM-filtered set surpasses it (AUC 0.927), using 294x fewer features. A faithfulness audit (COSMIC CGC, OncoKB, PAM50) shows that 6 of 17 selected genes (35.3%) are validated BRCA biomarkers, while 10 of 16 known BRCA genes present in the input were missed - including FOXA1. This divergence between downstream performance and reasoning faithfulness suggests selective faithfulness in this setting: targeted confounder removal can improve predictive performance without comprehensive recall.

---

## 论文详细总结（自动生成）

这是一份关于论文《Mamba-SSM with LLM Reasoning for Feature Selection: Faithfulness-Aware Biomarker Discovery》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在高维 RNA-seq 数据（>20,000 基因）中，深度学习模型（如 Mamba）通过梯度显著性识别出的“重要特征”往往包含大量混淆因素（如免疫浸润、组织纯度、批次效应），而非真正的生物学驱动因子。
*   **研究动机**：探索大语言模型（LLM）的思维链（CoT）推理是否能利用其内置的生物医学知识，从模型提取的候选特征中过滤掉这些混淆因素，并评估 LLM 的推理质量（忠实度）与下游分类性能之间的关系。

### 2. 方法论
*   **核心思想**：采用“神经-符号”混合流水线，先用深度学习模型缩小搜索范围，再用 LLM 进行逻辑过滤。
*   **关键技术流程**：
    1.  **Mamba SSM 训练**：在 TCGA-BRCA 数据集上训练 Mamba 状态空间模型，进行肿瘤与正常样本的二分类任务。
    2.  **梯度显著性提取**：计算输入基因对预测结果的梯度，取平均梯度最大的前 50 个基因作为候选池（$G_{50}$）。
    3.  **结构化 CoT 推理**：将 $G_{50}$ 输入 DeepSeek-R1（7B），通过结构化提示词要求其根据 5 个拒绝标准（如：肌肉/神经特异性基因、非特异性免疫标志物）和 3 个保留标准进行评估。
    4.  **忠实度审计**：将 LLM 选出的基因与 COSMIC、OncoKB、PAM50 等权威生物标志物数据库进行交叉比对，分析其推理的准确性。

### 3. 实验设计
*   **数据集**：TCGA-BRCA RNA-seq 数据（1,095 个肿瘤样本，113 个正常样本）。
*   **对比基准（Benchmark）**：
    *   **B1 (Variance baseline)**：基于方差选择的前 5,000 个基因。
    *   **B2 (Mamba saliency only)**：仅基于 Mamba 梯度显著性选择的前 50 个基因（无 LLM 过滤）。
    *   **B3 (Mamba + LLM CoT)**：经 LLM 过滤后的 17 个基因集合。
*   **对比方法**：在相同的 Mamba 架构下，仅改变输入特征集，对比分类准确率、F1 分数和 AUC。

### 4. 资源与算力
*   **模型**：使用了本地运行的 **DeepSeek-R1 (7B)**（通过 Ollama 部署）。
*   **算力支持**：文中提到获得了 Cohere Labs 的计算积分支持以及中央密苏里大学（UCM）的研究基金。
*   **缺失信息**：文中**未明确说明**具体的 GPU 型号、数量以及 Mamba 模型训练的具体耗时。

### 5. 实验数量与充分性
*   **实验规模**：主要针对 TCGA-BRCA 数据集进行了完整的端到端验证。
*   **充分性评估**：
    *   **优点**：进行了详细的生物学忠实度审计，对比了 LLM 选择与已知金标准的重合度。
    *   **不足**：实验仅限于单一癌症类型（乳腺癌），且仅使用了一组随机种子和固定的训练-测试划分，缺乏多数据集交叉验证和统计显著性检验（如 P 值分析）。

### 6. 主要结论与发现
*   **性能超越**：LLM 过滤后的 17 基因集（B3）AUC 达到 **0.927**，优于 5,000 基因基准（0.903）和 50 基因显著性基准（0.832），特征数量减少了 294 倍。
*   **选择性忠实度（Selective Faithfulness）**：LLM 的生物学召回率并不高（仅 37.5%，漏掉了关键基因 FOXA1），但其通过**精准剔除混淆因素**（Precision-oriented）显著提升了下游任务的泛化能力。
*   **推理风险**：LLM 存在“自信地犯错”的情况，例如会使用虚假的路径解释来保留无关基因，或因显著性排名靠后而拒绝已知的关键驱动基因。

### 7. 优点
*   **特征压缩效率极高**：证明了 LLM 可以作为极低维度的特征选择器，显著降低生物信息学任务的计算负担。
*   **可解释性强**：通过 CoT 过程，研究者可以审计模型选择每一个基因的生物学理由，而非仅仅依赖黑盒评分。
*   **揭示了新现象**：提出了“选择性忠实度”概念，即在特征选择中，剔除噪声比找全所有真阳性对性能提升更重要。

### 8. 不足与局限
*   **知识幻觉**：LLM 在推理过程中仍会产生生物学幻觉，调用错误的信号通路解释。
*   **召回率缺陷**：对已知关键生物标志物的遗漏（如 FOXA1）限制了该方法在临床发现中的可靠性。
*   **覆盖范围有限**：目前仅在乳腺癌数据上验证，其逻辑规则和推理能力在其他复杂疾病中的通用性尚待考察。

（完）
