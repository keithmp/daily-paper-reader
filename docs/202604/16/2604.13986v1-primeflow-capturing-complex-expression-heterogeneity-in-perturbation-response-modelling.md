---
title: "PRiMeFlow: Capturing Complex Expression Heterogeneity in Perturbation Response Modelling"
title_zh: PRiMeFlow：在扰动响应建模中捕捉复杂的表达异质性
authors: "Zichao Yan, Yan Wu, Mica Xu Ji, Chaitra Agrahar, Esther Wershof, Marcel Nassar, Mehrshad Sadria, Ridvan Eksi, Vladimir Trifonov, Ignacio Ibarra, Telmo Felgueira, Błażej Osiński, Rory Stark"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13986v1"
tags: ["query:q7"]
score: 8.0
evidence: 在基因表达空间中模拟遗传扰动的影响
tldr: PRiMeFlow是一个基于流匹配（Flow Matching）的端到端模型，旨在预测基因和药物扰动对单细胞基因表达的影响。针对单细胞数据的高异质性和复杂基因依赖性，该模型直接在基因表达空间建模，并采用U-Net架构参数化速度场。在PerturBench基准测试中表现优异，并获得了首届ARC虚拟细胞挑战赛的通用奖，证明了其在捕捉复杂细胞状态分布方面的强大能力。
selection_source: fresh_fetch
motivation: 旨在解决单细胞基因表达中固有的异质性以及复杂的潜在基因依赖性给扰动响应建模带来的挑战。
method: 采用基于流匹配的端到端分布拟合方法，直接在基因表达空间操作并使用U-Net架构参数化速度场。
result: 在PerturBench基准测试中展现了对单细胞经验分布的精确拟合能力，并赢得了首届ARC虚拟细胞挑战赛的通用奖。
conclusion: PRiMeFlow通过创新的架构设计有效捕捉了复杂的表达异质性，为大规模药物发现和细胞行为研究提供了高效的预测工具。
---

## 摘要
通过计算机模拟预测扰动对细胞状态的影响，可以大规模识别细胞行为的驱动因素并加速药物研发。然而，由于单细胞基因表达固有的异质性以及复杂的潜在基因依赖关系，建模仍面临挑战。在本文中，我们提出了 PRiMeFlow，这是一种基于端到端流匹配（flow matching）的方法，用于直接在基因表达空间中对遗传和小分子扰动的影响进行建模。PRiMeFlow 采用的分布拟合方法使其能够准确逼近单细胞基因表达的经验分布，我们通过在 PerturBench 中进行的广泛基准测试证明了这一点。通过消融实验，我们还验证了重要的模型设计选择，例如在基因表达空间中运行以及使用 U-Net 架构对速度场进行参数化。PRiMeFlow 架构被用作在首届 ARC 虚拟细胞挑战赛中获得通用奖（Generalist Prize）的模型基础。

## Abstract
Predicting the effects of perturbations in-silico on cell state can identify drivers of cell behavior at scale and accelerate drug discovery. However, modeling challenges remain due to the inherent heterogeneity of single cell gene expression and the complex, latent gene dependencies. Here, we present PRiMeFlow, an end-to-end flow matching based approach to directly model the effects of genetic and small molecule perturbations in the gene expression space. The distribution-fitting approach taken by PRiMeFlow enables it to accurately approximate the empirical distribution of single-cell gene expression, which we demonstrate through extensive benchmarking inside PerturBench. Through ablation studies, we also validate important model design choices such as operating in gene expression space and parameterizing the velocity field with a U-Net architecture. The PRiMeFlow architecture was used as the basis for the model that won the Generalist Prize in the first ARC Virtual Cell Challenge.

---

## 论文详细总结（自动生成）

以下是对论文《PRiMeFlow: Capturing Complex Expression Heterogeneity in Perturbation Response Modelling》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在药物发现和生物学研究中，预测遗传或小分子扰动对细胞状态的影响至关重要。然而，单细胞基因表达具有高度的**异质性**（Heterogeneity）和复杂的**潜在基因依赖性**。
*   **核心挑战**：现有的模型（如 CPA, SAMS-VAE）通常在压缩的潜在空间（如 PCA 或 VAE 的隐空间）中建模，这可能导致关键生物信号的丢失或多阶段推理中的误差累积。
*   **整体含义**：本文提出了 **PRiMeFlow**，这是首个采用端到端**流匹配（Flow Matching）**技术直接在原始基因表达空间建模扰动响应的方法，旨在更准确地捕捉细胞群体的分布变化而非仅仅是平均值。

### 2. 方法论
*   **核心思想**：利用流匹配框架学习一个随时间变化的**速度场（Velocity Field）**，将已知的源分布（如标准高斯分布）变换为未知的目标数据分布（受扰动的细胞群体表达分布）。
*   **关键技术细节**：
    *   **直接空间建模**：不依赖预训练的嵌入，直接在 log1p 归一化的基因表达空间操作。
    *   **U-Net 架构**：使用 U-Net 来参数化速度场 $v_{\theta,t}$。尽管基因顺序在理论上是任意的，但实验证明 U-Net 的归纳偏置在捕捉复杂依赖关系上优于 MLP。
    *   **输入构成**：模型输入包括基因表达谱（正弦编码）、基因身份嵌入、扰动条件（组合编码）和时间 $t$。
    *   **独立耦合（Independent Coupling）**：训练时采用简单的独立耦合路径，而非复杂的优化传输（OT）耦合，简化了训练过程。
*   **算法流程**：通过最小化条件流匹配损失（Conditional Flow Matching Loss），训练神经网络预测从噪声到真实表达的演化速度。推理时，通过求解常微分方程（ODE）生成单细胞样本。

### 3. 实验设计
*   **数据集**：
    1.  **Srivatsan20**：小分子扰动，涉及 3 种细胞系，约 17.8 万个细胞。
    2.  **Jiang24**：遗传扰动，涉及 30 种生物状态，约 162.8 万个细胞。
    3.  **Norman19**：遗传扰动（含双重扰动组合预测），约 9.1 万个细胞。
*   **Benchmark**：使用 **PerturBench** 平台，采用其定义的协变量迁移（Covariate Transfer）和组合预测任务。
*   **对比方法**：包括 CPA、SAMS-VAE、Biolord、Latent Additive、Linear Additive 以及基于 PCA 的流匹配变体（FM PCA）。

### 4. 资源与算力
*   **硬件配置**：论文提到 PRiMeFlow 的训练使用了 **4 张 GPU**（具体型号未明确说明，通常为 A100 或 V100 级别）。
*   **训练细节**：
    *   使用了梯度累积（4 步）和梯度裁剪。
    *   超参数通过 Optuna 进行了 60 次试验的 HPO（超参数优化）搜索。
    *   推理时使用 100 步 Euler 积分。

### 5. 实验数量与充分性
*   **实验规模**：在三个大规模数据集上进行了测试，涵盖了小分子、单基因和组合遗传扰动。
*   **消融实验**：非常充分。对比了：
    1.  **架构**：U-Net vs. MLP。
    2.  **空间**：基因表达空间 vs. PCA 空间。
    3.  **耦合方式**：独立耦合 vs. 优化传输（OT）。
*   **客观性**：所有模型均使用 5 个随机种子运行并报告均值和标准差。使用了 PerturBench 的标准化指标，确保了对比的公平性。

### 6. 主要结论与发现
*   **分布拟合领先**：PRiMeFlow 在 MMD（最大均值差异）和 **DEG Recall（差异表达基因召回率）** 等分布指标上显著优于所有基线模型。
*   **捕捉异质性**：模型能更好地模拟受扰动细胞群体的方差和复杂结构，有效避免了传统 VAE 类模型的“模式崩溃”（Mode Collapse）问题。
*   **权衡关系**：虽然在分布拟合上表现卓越，但在预测平均表达值（如 LogFC, RMSE）方面，PRiMeFlow 有时略逊于专门优化均值的简单模型（如 CPA），这反映了分布精度与均值精度之间的权衡。
*   **竞赛验证**：该架构是赢得首届 **ARC 虚拟细胞挑战赛（Generalist Prize）** 冠军模型的核心基础。

### 7. 优点
*   **端到端设计**：避免了降维带来的信息损失，保留了细微的生物信号。
*   **强大的分布建模**：通过流匹配生成的单细胞群体在生物学特征（如 DEG）上更接近真实观测。
*   **架构创新**：成功将 U-Net 引入非图像的基因表达建模，并证明了其有效性。
*   **灵活性**：支持分类器自由引导（CFG），允许在生成时调节扰动效应的强度。

### 8. 不足与局限
*   **计算成本**：由于在全基因空间操作且使用 U-Net，其计算开销显著高于基于 MLP 或潜在空间的模型。
*   **架构偏置**：U-Net 假设输入具有序列/空间顺序，而基因顺序是人为定义的。虽然实验效果好，但理论上并非最优（未来可能转向 Transformer 或 Perceiver 架构）。
*   **均值预测**：在传统的伪批量（Pseudobulk）指标上并未全面超越现有模型，说明其优势主要集中在单细胞层面的异质性模拟。

（完）
