---
title: Counterfactual Peptide Editing for Causal TCR--pMHC Binding Inference
title_zh: 用于因果 TCR--pMHC 结合推理的反事实肽编辑
authors: "Sanjar Khudoyberdiev, Arman Bekov"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.13256v1"
tags: ["query:xeno"]
score: 8.0
evidence: TCR-pMHC结合与免疫识别的因果推断
tldr: 针对TCR-pMHC结合预测模型易受捷径学习（如肽链长度、V基因共现）影响而导致泛化能力差的问题，本文提出反事实不变预测（CIP）框架。该框架通过生成受生物约束的反事实肽段编辑，在非锚定位置强制预测不变性，并在MHC锚定残基处放大敏感性。实验表明，CIP在具有挑战性的族系留出测试中显著提升了预测性能和一致性，有效减少了捷径学习。
selection_source: fresh_fetch
motivation: 现有的TCR-pMHC预测模型往往依赖训练数据中的虚假相关性而非物理结合界面，导致在分布外评估中表现脆弱。
method: 提出CIP框架，利用反事实编辑生成技术，结合非锚定位置的不变性损失和锚定位置的对比损失来引导模型学习因果特征。
result: "在VDJdb-IEDB基准测试中，CIP在族系留出协议下达到0.831的AUROC，并将捷径指数相对于基准模型降低了39.7%。"
conclusion: 锚定感知的编辑生成是提升模型分布外泛化能力的关键，为因果驱动的TCR特异性建模提供了实用方案。
---

## 摘要
用于 TCR-pMHC 结合预测的神经模型容易受到捷径学习的影响：它们利用训练数据中的虚假相关性（例如肽长度偏差或 V 基因共现），而非物理结合界面。这使得预测在家族留出（family-held-out）和距离感知评估下变得脆弱，因为此类捷径在这些情况下无法迁移。我们引入了反事实不变预测（Counterfactual Invariant Prediction, CIP），这是一个生成受生物学约束的反事实肽编辑的训练框架，它在增强对 MHC 锚定残基敏感性的同时，强制模型对非锚定位置的编辑保持不变性。CIP 通过两个辅助目标增强了基础分类器：(1) 不变性损失，用于惩罚保守非锚定替换下的预测变化；(2) 对比损失，用于鼓励锚定位置破坏下的显著预测变化。在经过整理的 VDJdb-IEDB 基准测试中，CIP 在具有挑战性的家族留出协议下实现了 0.831 的 AUROC 和 0.724 的反事实一致性（CFC），相比于无约束基线，其捷径指数降低了 39.7%。消融实验证实，锚定感知的编辑生成是分布外（OOD）增益的主要驱动因素，为具有因果基础的 TCR 特异性建模提供了一种实用的方案。

## Abstract
Neural models for TCR-pMHC binding prediction are susceptible to shortcut learning: they exploit spurious correlations in training data -- such as peptide length bias or V-gene co-occurrence -- rather than the physical binding interface. This renders predictions brittle under family-held-out and distance-aware evaluation, where such shortcuts do not transfer. We introduce \emph{Counterfactual Invariant Prediction} (CIP), a training framework that generates biologically constrained counterfactual peptide edits and enforces invariance to edits at non-anchor positions while amplifying sensitivity at MHC anchor residues. CIP augments the base classifier with two auxiliary objectives: (1) an invariance loss penalizing prediction changes under conservative non-anchor substitutions, and (2) a contrastive loss encouraging large prediction changes under anchor-position disruptions. Evaluated on a curated VDJdb-IEDB benchmark under family-held-out, distance-aware, and random splits, CIP achieves AUROC 0.831 and counterfactual consistency (CFC) 0.724 under the challenging family-held-out protocol -- a 39.7\% reduction in shortcut index relative to the unconstrained baseline. Ablations confirm that anchor-aware edit generation is the dominant driver of OOD gains, providing a practical recipe for causally-grounded TCR specificity modeling.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **CIP (Counterfactual Invariant Prediction)** 的框架，旨在解决 T 细胞受体（TCR）与肽-主要组织相容性复合体（pMHC）结合预测中的“捷径学习”问题。

以下是对该论文的结构化深入总结：

### 1. 核心问题与研究背景
*   **核心问题**：现有的深度学习模型在预测 TCR-pMHC 结合时，往往依赖于训练数据中的**虚假相关性（捷径）**，例如 V 基因的出现频率、肽链长度或位置偏差，而不是学习真正的物理结合界面。
*   **后果**：这导致模型在处理“未见过的”TCR 家族或距离较远的序列时（即分布外 OOD 情况），预测性能大幅下降。
*   **研究动机**：从因果推断的角度出发，结合事件是由物理互补性决定的。其中，肽段的“锚定残基”（Anchor residues）对结合起决定性作用，而非锚定位置则更像“样式变量”。模型应当对非锚定位置的微调保持鲁棒，而对锚定位置的破坏保持敏感。

### 2. 方法论：反事实不变预测 (CIP)
CIP 框架的核心思想是通过生物学约束的肽段编辑来引导模型学习因果特征：
*   **反事实肽编辑生成**：
    *   **非锚定编辑（样式扰动）**：在保持锚定位置（如 HLA-A*02:01 的 P2 和 P$\Omega$）不变的前提下，利用 BLOSUM62 矩阵进行保守替换。
    *   **锚定编辑（因果破坏）**：专门针对锚定位置进行破坏性替换（BLOSUM62 得分 < 0），模拟会导致结合失效的突变。
*   **多目标损失函数**：
    *   **$L_{BCE}$**：基础的加权二元交叉熵损失，用于分类。
    *   **$L_{inv}$ (不变性正则化)**：惩罚模型在非锚定编辑下的预测波动，强制模型忽略非因果特征。
    *   **$L_{sens}$ (锚定敏感性对比损失)**：通过边际损失（Margin Loss）确保锚定位置被破坏后，预测置信度显著下降。
*   **基础架构**：采用双编码器结构，使用预训练的 **ESM-2 (650M)** 模型作为特征提取器，后接多层感知机（MLP）生成结合评分。

### 3. 实验设计
*   **数据集**：从 VDJdb 和 IEDB 数据库整理的 HLA-A*02:01 限制性人源 TCR $\alpha\beta$ 记录。
*   **评估协议（Benchmark）**：
    1.  **Random Split**：随机划分（性能上限参考）。
    2.  **Family-held-out (FHO)**：留出特定的 V 基因家族，测试跨家族泛化力。
    3.  **Distance-aware (DA)**：测试集序列与训练集 Levenshtein 距离 > 30%，测试跨序列空间泛化力。
*   **对比方法**：
    *   **Baseline**：仅使用 BCE 损失的常规训练。
    *   **+Edit Aug**：仅将反事实样本作为数据增强加入训练，不使用辅助损失。
*   **因果诊断指标**：除了 AUROC/AUPRC，还提出了 **捷径指数 (SI)**、**反事实一致性 (CFC)** 和 **锚定翻转率 (AFR)**。

### 4. 资源与算力
*   **模型规模**：使用了 ESM-2 650M 参数版本的蛋白质语言模型。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。但考虑到 650M 参数模型的微调，通常需要 A100 或同级别显卡支持。

### 5. 实验数量与充分性
*   **实验规模**：在三个不同的划分协议下进行了测试，每个实验运行了 **5 个随机种子**并取平均值。
*   **消融实验**：非常充分。分别验证了 $L_{sens}$、$L_{inv}$、锚定掩码以及 BLOSUM 约束对最终性能的贡献。
*   **客观性**：通过引入专门的因果诊断指标（如 SI 和 AFR），从不同维度量化了模型是否真的克服了捷径学习，而不仅仅是看 AUROC 的提升，实验设计较为严谨。

### 6. 主要结论与发现
*   **性能提升**：在最困难的 FHO 协议下，CIP 达到了 **0.831 的 AUROC**，比基线提升了 5.2%。
*   **克服捷径**：CIP 将捷径指数（SI）降低了 **39.7%**，证明模型减少了对 V 基因等非因果特征的依赖。
*   **因果敏感性**：锚定翻转率（AFR）从 0.231 提升至 0.401，表明模型真正识别出了锚定残基在结合中的关键作用。
*   **关键驱动力**：消融实验显示，**锚定感知的编辑生成**是 OOD 性能增益的主要来源。

### 7. 优点与亮点
*   **生物学启发**：将免疫学中已知的锚定残基知识转化为机器学习的归纳偏置（Inductive Bias），而非盲目增加数据。
*   **因果视角**：引入反事实推理框架，为生物序列建模提供了一种可解释且鲁棒的训练新思路。
*   **诊断工具**：新提出的 SI、CFC、AFR 指标为评估 TCR 模型的可靠性提供了比标准指标更深层的视角。

### 8. 不足与局限
*   **等位基因局限性**：目前的锚定规则是针对 HLA-A*02:01 特异性设计的，若要推广到其他 HLA 等位基因，需要重新定义锚定位置规则。
*   **标签假设风险**：反事实编辑的标签是基于生物学统计推断的（假设锚定破坏即不结合），虽然概率上成立，但缺乏湿实验（Wet-lab）的逐一验证。
*   **负样本定义**：不变性损失目前仅定义在正样本上，如何更有效地利用负样本进行反事实学习仍有待探索。

（完）
