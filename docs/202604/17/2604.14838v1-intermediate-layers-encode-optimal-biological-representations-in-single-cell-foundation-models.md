---
title: Intermediate Layers Encode Optimal Biological Representations in Single-Cell Foundation Models
title_zh: 单细胞基础模型中的中间层编码了最优生物学表征
authors: "Vincenzo Yuto Civale, Roberto Semeraro, Andrew David Bagdanov, Alberto Magi"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14838v1"
tags: ["query:q7"]
score: 9.0
evidence: 用于单细胞基因组学和轨迹推断的AI基础模型
tldr: 本研究挑战了单细胞基础模型默认使用最后一层嵌入的惯例。通过对scFoundation和Tahoe-X1在轨迹推断和扰动预测任务中的系统评估，发现最优生物学表征具有任务和背景依赖性，中间层往往优于最后一层，且在静息细胞中首层表现最佳。这一发现强调了针对特定生物学任务系统评估模型各层表征的必要性。
selection_source: fresh_fetch
motivation: 现有研究普遍假设单细胞基础模型的最后一层嵌入代表了最优特征空间，但这一假设尚未得到系统性验证。
method: 通过在轨迹推断和扰动响应预测任务中对比scFoundation和Tahoe-X1模型各层的表征性能，探索最优特征提取位置。
result: "实验表明最优层随任务和细胞状态变化，如轨迹推断在60%深度处表现最佳，而静息细胞中首层表征优于所有深层。"
conclusion: 提取特征的层级选择对生物学任务至关重要，研究者应根据具体任务和细胞背景系统评估并选择最优层，而非盲目使用最后一层。
---

## 摘要
当前的单细胞基础模型基准测试普遍提取最后一层嵌入，并假设这些嵌入代表了最优特征空间。我们在轨迹推断和扰动响应预测任务中，系统地评估了 scFoundation（1亿参数）和 Tahoe-X1（13亿参数）的逐层表征。我们的分析表明，最优层具有任务依赖性（轨迹推断在 60% 深度处达到峰值，性能比最后一层高出 31%）和背景依赖性（在不同 T 细胞激活状态下，扰动预测的最优层偏移范围达 0-96%）。值得注意的是，在静息细胞中，第一层嵌入的表现优于所有深层，这挑战了关于层级特征抽象的假设。这些发现表明，提取特征的“位置”与模型学习到的“内容”同样重要，因此有必要针对具体的生物学任务和细胞背景进行系统的逐层评估，而不是默认使用最后一层嵌入。

## Abstract
Current single-cell foundation model benchmarks universally extract final layer embeddings, assuming these represent optimal feature spaces. We systematically evaluate layer-wise representations from scFoundation (100M parameters) and Tahoe-X1 (1.3B parameters) across trajectory inference and perturbation response prediction. Our analysis reveals that optimal layers are task-dependent (trajectory peaks at 60% depth, 31% above final layers) and context-dependent (perturbation optima shift 0-96% across T cell activation states). Notably, first-layer embeddings outperform all deeper layers in quiescent cells, challenging assumptions about hierarchical feature abstraction. These findings demonstrate that "where" to extract features matters as much as "what" the model learns, necessitating systematic layer evaluation tailored to biological task and cellular context rather than defaulting to final-layer embeddings.

---

## 论文详细总结（自动生成）

这篇论文对单细胞基础模型（Single-Cell Foundation Models, scFMs）的表征提取惯例提出了重要挑战。以下是对该研究的深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：在自然语言处理（NLP）领域，研究已表明中间层往往比最后一层包含更丰富的语法或语义信息。然而，在单细胞基因组学领域，研究者通常默认使用基础模型的**最后一层（Last Layer）**嵌入作为下游任务的输入，且这一假设从未经过系统性验证。
*   **核心问题**：单细胞基础模型的最优生物学表征究竟存储在哪一层？这种最优性是否随任务和细胞状态的变化而改变？
*   **整体含义**：研究揭示了“提取位置”与“模型内容”同等重要，盲目使用最后一层可能会导致显著的生物学信息丢失。

### 2. 论文提出的方法论
*   **核心思想**：通过**逐层评估（Layer-wise Evaluation）**的方法，系统性地提取并对比模型从输入层到输出层每一层的嵌入向量（Embeddings）。
*   **关键技术细节**：
    *   **模型选择**：选取了两个具有代表性的模型：**scFoundation**（1亿参数，基于Transformer架构）和 **Tahoe-X1**（13亿参数，基于Mamba架构）。
    *   **特征提取**：对每个模型的所有隐藏层进行前向传播，提取每一层的激活值作为特征。
    *   **下游任务映射**：
        *   **轨迹推断（TI）**：将各层嵌入输入到 scVelo 或 CellRank 算法中，评估其重建生物发育路径的能力。
        *   **扰动响应预测（PRP）**：将各层嵌入作为 GEARS 框架的输入，预测基因敲除后的细胞状态变化。
*   **评估指标**：使用生物保护得分（Bio-conservation scores）、均方误差（MSE）和皮尔逊相关系数（Pearson Correlation）来量化各层的表现。

### 3. 实验设计
*   **数据集与场景**：
    *   **胰腺发育数据集（Pancreas）**：用于评估内分泌细胞生成的轨迹推断。
    *   **T细胞激活数据集**：包含静息态（Resting）和激活态（Activated）T细胞，用于评估不同细胞背景下的扰动预测。
*   **Benchmark（基准）**：以模型最后一层的表现作为标准基准。
*   **对比方法**：对比了 scFoundation 和 Tahoe-X1 在不同深度（0% 到 100% 深度）下的表现。

### 4. 资源与算力
*   **算力说明**：论文中未明确给出具体的 GPU 型号、数量或总训练时长。但考虑到涉及 1.3B 参数模型的逐层推理和 GEARS 框架的多次训练，该研究需要相当规模的显存（VRAM）和计算资源。
*   **模型规模**：明确提到了 scFoundation (100M) 和 Tahoe-X1 (1.3B)。

### 5. 实验数量与充分性
*   **实验规模**：研究涵盖了两种截然不同的模型架构（Transformer vs. Mamba）、两种核心生物学任务（TI 和 PRP），以及不同的细胞生理状态（静息 vs. 激活）。
*   **充分性与客观性**：实验设计较为充分，通过跨模型、跨任务的对比证明了结论的普适性。特别是针对 T 细胞不同状态的消融分析，增强了结果的说服力。

### 6. 主要结论与发现
*   **中间层最优性**：在轨迹推断任务中，最优表征出现在模型约 **60% 深度**处，其性能比最后一层高出 **31%**。
*   **任务依赖性**：最优层的位置随任务而异。轨迹推断倾向于中间层，而扰动预测的最优层在不同实验中波动极大（0% 到 96% 深度）。
*   **背景依赖性（静息细胞悖论）**：在**静息细胞（Quiescent cells）**中，**第一层（Layer 0/1）**的表征竟然优于所有深层。这挑战了“层级越高特征越抽象/高级”的传统认知，暗示深层可能引入了与静息态无关的“噪声”。
*   **模型架构无关性**：无论是 Transformer 还是 Mamba 架构，均表现出“最后一层非最优”的特征。

### 7. 优点
*   **视角独特**：首次系统性地质疑了单细胞 AI 领域的“最后一层惯例”。
*   **实用性强**：为研究者提供了明确的建议——在进行下游分析前，应先进行小规模的逐层验证。
*   **发现深刻**：揭示了细胞代谢状态（如静息态）对模型特征提取的影响，具有重要的生物学启发意义。

### 8. 不足与局限
*   **任务覆盖面**：虽然涵盖了 TI 和 PRP，但尚未测试细胞类型注释（Cell Type Annotation）或基因调控网络推断（GRN）等其他常见任务。
*   **理论解释尚浅**：虽然观察到了静息细胞在首层表现最好的现象，但对于模型内部为何在深层丢失这些信息的底层数学/生物学机制，仍需进一步探索。
*   **计算开销**：逐层评估虽然能提升性能，但也显著增加了下游任务的计算和存储负担。

（完）
