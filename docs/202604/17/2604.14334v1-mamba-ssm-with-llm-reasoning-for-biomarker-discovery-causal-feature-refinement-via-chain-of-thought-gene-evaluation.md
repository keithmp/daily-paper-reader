---
title: "Mamba-SSM with LLM Reasoning for Biomarker Discovery: Causal Feature Refinement via Chain-of-Thought Gene Evaluation"
title_zh: 结合大语言模型推理的 Mamba-SSM 用于生物标志物发现：通过思维链基因评估进行因果特征精炼
authors: "Pushpa Kumar Balan, Aijing Feng"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.14334v1"
tags: ["query:q7"]
score: 9.0
evidence: 利用大语言模型推理进行生物标志物发现和RNA-seq数据中的基因评估
tldr: 本研究针对深度序列模型在生物标志物发现中易受组织成分混杂因素干扰的问题，提出了一种结合Mamba状态空间模型与大语言模型（LLM）推理的新框架。通过在TCGA-BRCA数据集上训练Mamba模型并提取梯度显著性基因，利用DeepSeek-R1的思维链（CoT）能力对候选基因进行因果筛选。结果显示，过滤后的17个基因集在分类性能上显著优于原始50个基因及5000个高方差基因基准，证明了LLM在精简特征和提升模型鲁棒性方面的潜力。
selection_source: fresh_fetch
motivation: 深度学习模型提取的生物标志物候选列表常受组织成分等混杂因素污染，导致下游分类器性能下降。
method: 利用Mamba模型提取梯度显著性基因，并结合DeepSeek-R1的思维链推理对候选基因进行结构化因果过滤。
result: LLM过滤后的17个基因集在乳腺癌分类中达到0.927的AUC，远超原始显著性基因集并优于高方差基准。
conclusion: 研究揭示了“选择性忠实度”现象，即针对性地剔除混杂因素比全面召回已知生物标志物对提升性能更为关键。
---

## 摘要
深度序列模型的梯度显著性能够高效地筛选出候选生物标志物，但生成的基因列表常受到组织成分混杂因素的污染，从而降低下游分类器的性能。我们研究了大语言模型（LLM）的思维链（CoT）推理是否能忠实地过滤这些混杂因素，以及推理质量是否会驱动下游性能。我们在 TCGA-BRCA RNA-seq 数据集上训练了一个 Mamba SSM，并通过梯度显著性提取了前 50 个基因；DeepSeek-R1 使用结构化思维链对每个候选基因进行评估，最终生成了一个包含 17 个基因的集合。原始的 50 基因显著性集合（无 LLM）的表现差于 5000 基因方差基准（AUC 0.832 对比 0.903），而经过 LLM 过滤的集合则超越了该基准（AUC 0.927），且使用的特征数量减少了 294 倍。忠实度审计（基于 COSMIC CGC、OncoKB、PAM50）显示，选出的 17 个基因中仅有 6 个（35.3%）是经过验证的 BRCA 生物标志物，且输入中 16 个已知 BRCA 基因中有 10 个被遗漏，包括 FOXA1。下游性能与推理忠实度之间的这种差距表明了“选择性忠实度”：即使没有全面的召回率，有针对性地去除混杂因素也足以提升性能。

## Abstract
Gradient saliency from deep sequence models surfaces candidate biomarkers efficiently, but the resulting gene lists are contaminated by tissue-composition confounders that degrade downstream classifiers. We study whether LLM chain-of-thought (CoT) reasoning can faithfully filter these confounders, and whether reasoning quality drives downstream performance. We train a Mamba SSM on TCGA-BRCA RNA-seq and extract the top-50 genes by gradient saliency; DeepSeek-R1 evaluates every candidate with structured CoT to produce a final 17-gene set. The raw 50-gene saliency set (no LLM) performs worse than a 5,000-gene variance baseline (AUC 0.832 vs. 0.903), while the LLM-filtered set surpasses it (AUC 0.927), using 294x fewer features. A faithfulness audit (COSMIC CGC, OncoKB, PAM50) reveals only 6 of 17 selected genes (35.3%) are validated BRCA biomarkers, yet 10 of 16 known BRCA genes in the input were missed - including FOXA1. This gap between downstream performance and reasoning faithfulness suggests selective faithfulness: targeted confounder removal is sufficient for performance gains even without comprehensive recall.

---

## 论文详细总结（自动生成）

这篇论文探讨了如何结合状态空间模型（Mamba）与大语言模型（LLM）的推理能力，从高维基因表达数据中更准确地发现生物标志物。

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在高维 RNA-seq 数据（>20,000 基因）中，深度学习模型（如 Mamba）虽然能通过梯度显著性（Gradient Saliency）识别预测性强的基因，但这些基因往往包含大量“混杂因素”（如免疫浸润、组织纯度、批次效应），而非真正的疾病驱动因子。
*   **研究动机**：传统的特征选择方法依赖统计显著性，缺乏生物学因果逻辑。作者试图研究 LLM 的思维链（CoT）推理是否能利用其内置的生物医学知识，像专家一样过滤掉这些混杂因素，从而提升模型的泛化能力和生物学解释性。

### 2. 论文提出的方法论：神经符号管道（Neuro-Symbolic Pipeline）
该方法分为四个阶段：
1.  **Mamba SSM 训练**：在 TCGA-BRCA 数据集上训练一个 Mamba 状态空间模型，用于区分肿瘤和正常样本。Mamba 的线性缩放特性使其能够处理长序列基因数据。
2.  **梯度显著性提取**：计算每个基因对模型预测结果的贡献度（梯度大小），筛选出前 50 个最重要的候选基因（$G_{50}$）。
3.  **结构化 CoT 推理**：将这 50 个基因交给 **DeepSeek-R1 (7B)** 模型。通过结构化提示词（Prompt），要求 LLM 根据 5 条拒绝标准（如：仅在肌肉表达、无乳腺癌文献支持等）和 3 条保留标准进行评估，最终精炼出 17 个基因。
4.  **忠实度审计**：将 LLM 选择的基因与权威数据库（COSMIC CGC、OncoKB、PAM50）进行交叉比对，评估其生物学准确性。

### 3. 实验设计
*   **数据集**：TCGA-BRCA（乳腺癌）RNA-seq 数据，包含 1,095 个肿瘤样本和 113 个正常样本。
*   **基准方法（Benchmarks）**：
    *   **B1 (方差基准)**：选取方差最大的 5,000 个基因。
    *   **B2 (原始显著性)**：仅使用 Mamba 提取的前 50 个显著性基因（无 LLM 过滤）。
    *   **B3 (神经符号法)**：使用 LLM 过滤后的 17 个基因。
*   **对比指标**：准确率（Accuracy）、F1 分数、AUC（曲线下面积）。

### 4. 资源与算力
*   **算力说明**：文中提到使用了 Cohere Labs 研究资助的计算额度以及中央密苏里大学（UCM）的研究基金。
*   **具体细节**：**未明确说明**具体的 GPU 型号、数量或训练时长。仅提到使用了本地运行的 DeepSeek-R1 (7B) 模型（通过 Ollama 部署，温度系数 0.3）。

### 5. 实验数量与充分性
*   **实验规模**：主要针对 TCGA-BRCA 单一数据集进行了分类性能对比和生物学忠实度审计。
*   **充分性评价**：
    *   **优点**：对比了高维基准（5,000 基因）和低维显著性基准，证明了 LLM 过滤的必要性。
    *   **不足**：实验仅限于一种癌症类型（乳腺癌），且未与 LASSO 或 ElasticNet 等经典统计特征选择方法进行横向对比。样本划分仅使用了一组随机种子，缺乏多轮交叉验证的统计显著性分析。

### 6. 主要结论与发现
*   **性能超越基准**：仅使用 17 个基因（比基准少 294 倍特征），AUC 达到 **0.927**，优于 5,000 基因基准（0.903）和 50 基因显著性基准（0.832）。
*   **选择性忠实度（Selective Faithfulness）**：这是一个重要发现。LLM 对已知生物标志物的召回率并不高（仅 37.5%），甚至漏掉了关键的 FOXA1 基因，但它通过**精准剔除混杂因素**（如肌肉特异性基因），显著提升了下游分类器的泛化性能。
*   **LLM 的局限**：LLM 在推理过程中存在“自信地犯错”现象，会使用幻觉出的生物学路径来解释其选择。

### 7. 优点
*   **特征大幅压缩**：证明了极小规模的特征集（17 个基因）在经过生物学逻辑过滤后，可以比大规模特征集具有更强的预测力。
*   **神经符号结合**：将深度学习的统计能力与 LLM 的知识推理结合，为生物信息学提供了一种可解释的特征精炼路径。
*   **揭示推理机制**：通过 CoT 审计，发现了 LLM 在生物医学领域“剔除噪声比发现真理更擅长”的特性。

### 8. 不足与局限
*   **领域知识局限**：LLM 漏掉了如 FOXA1 这样的核心驱动基因，说明其在专业生物医学领域的知识召回仍有待加强。
*   **泛化性验证不足**：研究仅限于乳腺癌，尚未在其他癌症或多组学数据上验证。
*   **推理幻觉风险**：LLM 可能会为了符合逻辑而编造生物学理由（如错误地将某些基因归类为 NF-κB 通路）。
*   **缺乏经典方法对比**：未证明该方法是否优于传统的正则化统计模型（如 LASSO）。

（完）
