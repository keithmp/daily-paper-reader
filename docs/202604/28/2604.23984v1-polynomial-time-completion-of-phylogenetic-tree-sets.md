---
title: Polynomial-time completion of phylogenetic tree sets
authors: "Aleksandr Koshkarov, Nadia Tahiri"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.23984v1"
tags: ["query:q3"]
score: 10.0
evidence: 用于完成具有部分分类群重叠的系统发育树的多项式时间算法
tldr: 引入了一种多项式时间算法，通过保留分类群和分支信息来完成系统发育树的构建。
selection_source: fresh_fetch
motivation: 用于完成具有部分分类群重叠的系统发育树的多项式时间算法。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
Comparative analyses of phylogenetic trees typically require identical taxon sets, however, in practice, trees often include distinct but overlapping taxa. Pruning non-shared leaves discards phylogenetic signal, whereas tree completion can preserve both taxa and branch-length information. This work introduces a polynomial-time algorithm for set-wide completion of phylogenetic trees with partial taxon overlap. The proposed method identifies and extracts maximal completion subtrees that frequently appear across the source trees and constructs a weighted majority-rule consensus. Branch lengths are scaled using rates derived from common leaves. Each consensus subtree is inserted at the position that minimizes the quadratic distance error measured against information from the source trees, with candidate positions restricted to the original branches of the target tree. We demonstrate that the algorithm runs in polynomial time and preserves distances among the original taxa, yielding a unique completion that is order-independent with respect to the processing order of target trees. An experimental evaluation on amphibians, mammals, sharks, and squamates shows that the proposed method consistently achieves the lowest distance to the subset reference trees across subsets among all methods, in both topology and branch lengths.   An open-source Python implementation of the proposed algorithm and the biological datasets utilized in this study are publicly available at: https://github.com/tahiri-lab/overlap-treeset-completion/.

---

## 论文详细总结（自动生成）

这是一份关于论文《Polynomial-time completion of phylogenetic tree sets》（系统发育树集的多项式时间补全）的结构化总结：

### 1. 核心问题与整体含义
在进化生物学研究中，从不同数据源推断出的系统发育树往往包含不同但相互重叠的分类群（Taxa）。传统的处理方法通常是“修剪”掉非共有分类群（导致信息丢失）或构建单一的“超树”（Supertree）。
**本文的核心问题**是如何在保留原始树拓扑结构和分支长度信息的前提下，将缺失的分类群补全到树集中。该研究旨在开发一种高效的算法，能够利用树集中的共有信号，为每个不完整的树提供一个在拓扑和时间尺度上都具有生物学意义的完整版本。

### 2. 方法论
论文提出了一种基于多项式时间复杂度的迭代补全算法，核心步骤如下：
*   **最大补全子树（MCS）提取**：在源树集中识别频繁出现的、包含目标树缺失分类群的子树。
*   **加权多数规则共识**：根据源树与目标树的重叠程度分配权重，构建 MCS 的共识版本。这确保了与目标树亲缘关系更近的源树在补全过程中具有更大的发言权。
*   **分支长度缩放**：利用共有叶子节点之间的距离比率（Rate），对共识子树的分支长度进行调整，使其与目标树的时间尺度对齐。
*   **最优位置插入**：通过最小化一个**二次距离误差目标函数**（Equation 10），在目标树的原始分支上寻找最佳插入点。该函数衡量了插入点到共有叶子的观测距离与推断距离之间的差异。
*   **多项式时间保证**：算法复杂度为 $O(n^3 m^2 + n m^3)$（$n$ 为树的数量，$m$ 为分类群数量），并证明了补全结果具有**顺序无关性**（即处理目标树的先后顺序不影响最终结果）。

### 3. 实验设计
*   **数据集**：使用了来自 VertLife 资源的四个真实生物类群：两栖动物（Amphibians）、哺乳动物（Mammals）、鲨鱼（Sharks）和有鳞目动物（Squamates）。
*   **实验场景**：通过随机采样生成 20 个不同规模（50 到 145 个物种）的子集，并引入 NNI 移动和分支长度缩放来模拟噪声。
*   **Benchmark**：以未修剪的原始 VertLife 树作为地面真值（Ground Truth）。
*   **对比方法**：
    1.  **Root Attach**：将子树简单附加在根部（基准）。
    2.  **Nearest Parent**：离散位置插入基准。
    3.  **No MCS**：逐个插入单个叶子（消融实验，验证子树分组的有效性）。
    4.  **Supertree-based**：基于 PluMiST 构建多数规则超树进行补全。

### 4. 资源与算力
*   论文**未明确说明**具体的硬件配置（如 GPU 型号或 CPU 核心数）。
*   由于该算法被证明是多项式时间的，且主要涉及图论操作和简单的二次函数求解，其对算力的需求远低于深度学习模型。作者提供了开源的 Python 实现，暗示其可以在标准的个人电脑或工作站上高效运行。

### 5. 实验数量与充分性
*   **实验规模**：总共涵盖了 4 个大类生物数据集，每个数据集包含 20 个不同规模的子集，每个子集包含 30 棵输入树。
*   **充分性**：实验设计较为充分。作者不仅评估了拓扑准确性（归一化 Robinson-Foulds 距离），还评估了分支长度的准确性（归一化 Branch Score Distance）。
*   **客观性**：引入了统计显著性检验（Wilcoxon 符号秩检验）和 Holm 校正，证明了所提方法在绝大多数情况下显著优于基准方法。

### 6. 主要结论与发现
*   **性能领先**：在所有四个生物数据集中，该方法在拓扑一致性和分支长度准确性方面均一致优于所有基准方法。
*   **拓扑优势显著**：相比于表现第二的超树方法，该方法在归一化 RF 距离上平均降低了约 60%。
*   **关键因素**：消融实验表明，**MCS 分组策略**和**基于距离的目标函数优化**是提升补全质量的关键。
*   **信号保留**：补全后的树集能够很好地保留全局系统发育信号，其共识树与参考树非常接近。

### 7. 优点
*   **理论严谨**：提供了多项式时间复杂度证明、唯一性证明和顺序无关性证明。
*   **生物学意义**：不仅补全了分类群，还通过速率缩放保留了分支长度（进化时间）信息。
*   **灵活性**：支持多歧分（Multifurcation）的确定性解析，使其输出可兼容各种下游分析工具。
*   **开源友好**：提供了完整的 Python 实现和生物数据集。

### 8. 不足与局限
*   **贪心策略**：在子树选择步骤采用了局部最优的贪心策略，虽然提供了 FPT（固定参数可解）的全局优化方案，但在超大规模数据下可能存在计算权衡。
*   **有根树限制**：目前算法主要针对有根树（Rooted trees）设计，对于无根树的适用性尚未讨论。
*   **依赖共有叶子**：算法要求树集之间存在一定数量的共有叶子（至少 2 个）以进行距离缩放，对于极度稀疏的重叠数据可能受限。

（完）
