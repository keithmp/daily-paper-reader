---
title: annbatch unlocks terabyte-scale training of biological data in anndata
title_zh: annbatch 实现了 anndata 中太字节级生物数据的训练
authors: "Ilan Gold, Felix Fischer, Lucas Arnoldt, F. Alexander Wolf, Fabian J. Theis"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01949v1"
tags: ["query:q7"]
score: 8.0
evidence: 利用AI和机器学习处理全基因组测序和生物数据
tldr: 随着生物数据集规模突破内存限制，数据读取成为机器学习训练的瓶颈。本文推出 annbatch，这是一款原生支持 anndata 的小批量加载器，支持直接在磁盘存储的数据集上进行核外训练。它在单细胞转录组、显微成像和全基因组测序等任务中表现优异，将加载吞吐量提升了一个数量级，并将训练时间从数天缩短至数小时，同时保持了与 scverse 生态系统的完全兼容，为大规模生物 AI 提供了实用的基础设施。
selection_source: fresh_fetch
motivation: 生物数据集规模日益庞大且格式复杂，导致数据访问而非模型计算成为训练大规模生物模型的主要瓶颈。
method: 开发了原生于 anndata 的 mini-batch 加载器 annbatch，支持直接对磁盘备份的数据集进行高效的核外训练。
result: 在多项生物学基准测试中，annbatch 将数据加载吞吐量提升了高达一个数量级，显著缩短了模型训练时间。
conclusion: annbatch 为可扩展的生物 AI 提供了关键的数据加载基础设施，使用户无需放弃标准数据格式即可处理超大规模数据集。
---

## 摘要
生物数据集的规模目前通常已超过系统内存，使得数据访问而非模型计算成为训练机器学习模型的主要瓶颈。这一瓶颈在生物学领域尤为突出，因为广泛使用的社区数据格式必须支持异构元数据、稀疏和密集测定，以及在已建立的计算生态系统内进行下游分析。在此，我们介绍了 annbatch，这是一个原生于 anndata 的小批量加载器，能够直接在基于磁盘的数据集上进行核外（out-of-core）训练。在单细胞转录组学、显微镜成像和全基因组测序的基准测试中，annbatch 将加载吞吐量提高了一个数量级，并将训练时间从数天缩短至数小时，同时保持与 scverse 生态系统的完全兼容。Annbatch 为可扩展的生物人工智能建立了实用的数据加载基础设施，使得在不放弃标准生物数据格式的情况下，能够利用日益增长且多样化的数据集。Github: https://github.com/scverse/annbatch

## Abstract
The scale of biological datasets now routinely exceeds system memory, making data access rather than model computation the primary bottleneck in training machine-learning models. This bottleneck is particularly acute in biology, where widely used community data formats must support heterogeneous metadata, sparse and dense assays, and downstream analysis within established computational ecosystems. Here we present annbatch, a mini-batch loader native to anndata that enables out-of-core training directly on disk-backed datasets. Across single-cell transcriptomics, microscopy and whole-genome sequencing benchmarks, annbatch increases loading throughput by up to an order of magnitude and shortens training from days to hours, while remaining fully compatible with the scverse ecosystem. Annbatch establishes a practical data-loading infrastructure for scalable biological AI, allowing increasingly large and diverse datasets to be used without abandoning standard biological data formats. Github: https://github.com/scverse/annbatch

---

## 论文详细总结（自动生成）

以下是对论文《annbatch unlocks terabyte-scale training of biological data in anndata》的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
随着生物学数据集（如单细胞测序、高分辨率成像）规模达到太字节（TB）级别，传统的深度学习训练瓶颈已从**模型计算**转移到了**数据加载（I/O）**。
*   **痛点**：现有的高性能加载框架（如 NVIDIA Merlin, Petastorm）通常需要转换文件格式，这破坏了生物学界通用的 `anndata` 标准及其关联的 `scverse` 生态系统（如 Scanpy 等工具的兼容性）。
*   **目标**：开发一种原生支持 `anndata` 格式、无需格式转换、且能实现核外（out-of-core）高效训练的小批量加载器，将训练时间从“天”缩短至“小时”。

### 2. 论文提出的方法论：核心思想与关键技术
`annbatch` 提供了一个端到端的框架，包含两个核心组件：
*   **预洗牌器（Pre-shuffler）**：
    *   **核心思想**：解决磁盘随机访问慢的问题。它将磁盘上的多个 `anndata` 文件以“连续块”形式读入内存，在内存中洗牌后重新写回磁盘（存为 Zarr 格式）。
    *   **意义**：确保了后续加载时可以进行大块连续读取，同时保证了训练所需的随机性。
*   **高性能加载器（Data Loader）**：
    *   **关键技术**：
        *   **Zarr 后端**：利用 Rust 编写的 `zarrs` 库及其 Python 桥接，性能远超传统的 HDF5/h5ad。
        *   **连续磁盘访问**：一次抓取大量连续观测值而非单个样本。
        *   **直接 I/O（Direct I/O）**：绕过操作系统缓存，直接从磁盘读取到内存。
        *   **固定内存（Pinned Memory）与 GPU 加速**：优化 CPU 到 GPU 的数据传输，并在 GPU 上进行批次内的进一步洗牌。

### 3. 实验设计
论文在三种主流生物学场景下进行了基准测试：
*   **单细胞转录组学**：使用 **Tahoe-100M** 数据集（约 1 亿个细胞）。
*   **单细胞显微成像**：处理 128x128x5 的裁剪图像。
*   **全基因组测序（WGS）**：使用 **1000 Genomes GRCh38** 数据集，并扩展至 50 万个样本的规模。
*   **对比方法**：
    *   `scDataset`：现有的单细胞加载工具。
    *   `MappedCollection`：`scverse` 生态中的标准加载方式。
    *   `numpy memmap`：基因组学中常用的未压缩磁盘映射基准。

### 4. 资源与算力
*   **硬件平台**：
    *   **AWS 实例**：ml.g4dn.4xlarge（配备 NVIDIA T4 GPU, 16 vCPUs, 64GB RAM），用于模拟标准云端环境。
    *   **HPC 环境**：de.NBI 节点（T4 GPU）和 Leibniz Rechenzentrum (LRZ) 的 AI 分区（用于多进程扩展性测试）。
    *   **高端 GPU**：使用 NVIDIA H100 评估大型模型（如 AIDO.Cell, ViT）的吞吐量上限。
*   **训练时长提升**：在 Tahoe-100M 任务中，单轮（epoch）训练时间从 `MappedCollection` 的 **21 小时**缩短至 `annbatch` 的 **17 分钟**。

### 5. 实验数量与充分性
*   **实验覆盖面**：涵盖了稀疏数据（RNA-seq）、密集数据（成像）和超高维度稀疏数据（WGS 稀有变异），证明了方法的通用性。
*   **消融与对比**：对比了 Zarr 与 h5ad 格式的差异、不同批次大小（Batch Size）对 GPU 利用率的影响，以及多进程并行下的扩展性。
*   **客观性**：作者明确指出了预洗牌（Pre-shuffling）带来的额外时间开销（约 5 小时），但证明该开销在第一个训练轮次内即可通过加载速度的提升而收回成本。

### 6. 论文的主要结论与发现
*   **吞吐量飞跃**：`annbatch` 的加载速度比现有工具快 **10 到 40 倍**。
*   **格式优势**：Zarr 格式在处理大规模生物数据时显著优于 HDF5，且 `annbatch` 成功将其整合进 `anndata` 体系。
*   **解除瓶颈**：通过提高 I/O 效率，使得模型训练性能不再受限于磁盘读取，能够充分压榨 H100 等高端 GPU 的算力。
*   **生态兼容**：用户可以在不离开 `scverse` 生态的情况下处理太字节级数据。

### 7. 优点
*   **生态系统友好**：完全兼容 `anndata`，研究人员无需改变现有的分析工作流。
*   **端到端优化**：不仅关注加载，还通过预洗牌解决了随机访问的性能难题。
*   **跨领域通用**：一套代码即可处理测序、成像和基因组数据。
*   **技术先进**：引入了 Rust 驱动的 Zarr 处理和 Linux 直接 I/O 等底层优化。

### 8. 不足与局限
*   **采样策略受限**：由于采用连续读取模式，目前难以实现复杂的加权采样（Weighted Sampling），除非在预洗牌阶段就确定好分布。
*   **多模态支持**：目前尚未直接支持跨多个 `anndata` 对象的原生多模态同步加载（如同时加载同一细胞的 RNA 和蛋白质数据）。
*   **预洗牌开销**：虽然可摊销，但对于仅训练 1-2 个 epoch 的极小任务，预洗牌的时间可能显得较长。

（完）
