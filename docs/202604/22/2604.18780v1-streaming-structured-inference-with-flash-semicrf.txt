Title: Streaming Structured Inference with Flash-SemiCRF

URL Source: https://arxiv.org/pdf/2604.18780v1

Published Time: Wed, 22 Apr 2026 00:11:33 GMT

Number of Pages: 32

Markdown Content:
# Streaming Structured Inference with Flash-SemiCRF 

## Benjamin K. Johnson 1* †, Thomas Goralski 2†, Ayush Semwal 1, Hui Shen 1‡,H. Josh Jang 2* ‡

> 1

Department of Epigenetics, Van Andel Institute, 333 Bostwick Ave. NE, Grand Rapids, 49503, MI, USA. 

> 2

Department of Cell Biology, Van Andel Institute, 333 Bostwick Ave. NE, Grand Rapids, 49503, MI, USA. *Corresponding author(s). E-mail(s): ben.johnson@vai.org; josh.jang@vai.org; 

> †

These authors contributed equally to this work. 

> ‡

Co-senior authors 

Abstract 

Semi-Markov Conditional Random Fields (semi-CRFs) assign labels to segments of a sequence rather than to individual positions, enabling exact inference over segment-level features and principled uncertainty estimates at their boundaries. However, existing implementations must materialize a large edge potential tensor whose size grows with sequence length, maximum segment length, and label count, becoming prohibitive for speech-scale state spaces and intractable at genomic scales where sequences can exceed 100,000 positions. This memory bottleneck has limited the adoption of exact segment-level inference for long sequences and large label sets. We identify that the core inefficiency is materializing edge potentials that can instead be evaluated on-the-fly from a compact prefix-sum array, and make several improvements. First, replacing the stored edge tensor with prefix-sum lookup reduces the memory footprint by a factor proportional to the product of segment length and label count. Second, a streaming forward-backward pass with checkpoint-boundary normalization keeps working memory sublinear in sequence length while preserving exact gradients. Third, zero-centered cumulative scores control numerical drift and induce an adaptive duration prior under label imbalance. We integrate these ideas into Flash-SemiCRF, a fused Triton kernel that enables exact semi-CRF inference on previously intractable problem sizes. Available at https://github.com/ biobenkj/flash-semicrf. 

Keywords: Semi-CRF · Structured prediction · Genomic segmentation · GPU inference · Triton 

1

> arXiv:2604.18780v1 [cs.LG] 20 Apr 2026

# 1 Introduction 

Many sequence types are naturally segmented and prediction tasks need to go beyond per-position labels. Modern neural encoders learn rich cross-position dependencies, but their output layers remain per-position: the loss decomposes over individual positions, no constraint enforces that adjacent predictions form a valid segmentation, segment duration is never explicitly modeled, and there is no direct mechanism to obtain posterior marginals over segment boundaries or segment identities. Semi-Markov conditional random fields (semi-CRFs) [1] formalizes discriminative segment-level prediction by scoring variable-length segments via content, duration, and transition potentials. This fac-torization provides three structural properties that per-position classifiers lack, such as: every position belongs to exactly one labeled segment, explicit duration modeling that captures label-dependent length distributions, and segment-level posterior marginals that quantify uncertainty over boundaries rather than individual positions. The integration of CRFs as neural network output layers [2–5] demonstrated that structured inference and representation learning are complementary, with the CRF layer enforc-ing global coherence over the encoder’s learned representations. Rush [6] provided a general-purpose semi-CRF implementation ( pytorch-struct ), making the algorithm widely accessible as a training and inference layer but remaining bound by the edge tensor at scale. Semi-CRF inference requires materializing a dense edge tensor of size O(T KC 2) ( T positions, K

maximum segment duration, C labels), dominating both time and memory. For NLP tasks with T in the tens to hundreds and small K, this cost is manageable. At genomic scales, modern CRF-based tools have primarily adopted the per-position linear ( K=1) case that avoids this edge tensor and the associated time and memory costs of a standard semi-CRF: Bonito [7] and Dorado [8] decode raw nanopore signal, and Tranquillyzer [9] annotates long-read RNA-seq, all at sequence lengths routinely exceeding 10 5. Earlier semi-CRF applications to gene prediction – CONRAD [10] and CRAIG [11] – relied on hand-crafted features and CPU-bound Viterbi decode rather than serving as differentiable layers under neural encoder training. Extending to the semi-Markov setting multiplies the per-position cost by K; at genomic scales (K ≥ 10 3), materializing the full edge tensor becomes infeasible on GPU long before the sequential sweep over T becomes the bottleneck. Recent work from Zaratiana et al. [12] addresses this problem through pruning. Their Filtered Semi-CRF uses a local span classifier to discard irrelevant segments before running structured inference on the reduced graph. This exploits the sparsity of named entities in text, where most spans are non-entities. In contrast, for dense segmentation problems where every position belongs to a labeled segment such as those in genomics, the sparsity assumption does not typically hold and the full semi-CRF graph must be traversed. FlashAttention [13] demonstrated that for attention, the dominant cost is not arithmetic but memory access: by never materializing the n × n attention matrix and instead recomputing blocks on-chip, it achieves large wall-clock speedups despite higher FLOP counts. We apply the same IO-aware principle to semi-CRF inference. The standard implementation pre-computes and stores the full O(T KC 2) edge tensor in HBM, then runs forward-backward over the stored result. At scale, this tensor dominates memory. We observe that when segment content scores decompose as sums over positions [1], prefix sums reduce per-edge evaluation to O(1), and the edge tensor need never be materialized. Each potential can instead be computed on-the-fly within a streaming dynamic programming (DP) kernel from an O(T C )cumulative score array. We present Flash-SemiCRF , a Triton-accelerated semi-CRF library built on this observation. The key ideas are: 1. On-the-fly edge computation. A prefix-sum decomposition of segment content scores enables O(1)-in-duration edge potential evaluation, reducing inference from memory-bound to compute-bound and dispensing with the O(T KC 2) edge tensor entirely. 2. Ring-buffer streaming. The forward-backward algorithm operates through K-slot and 2 K-slot ring buffers for the forward and backward passes respectively, achieving O(KC ) working memory independent of sequence length T .3. Sublinear checkpointing. Gradient checkpointing at √T K intervals, following the recomputa-tion paradigm of FlashAttention [13] and the chunked scan of Mamba [14], yields O(pT /K · KC )checkpoint memory that grows sublinearly in T .4. Globally-centered emissions. A sequence-level baseline subtraction simultaneously stabilizes prefix sums at large T and induces an adaptive, data-dependent duration prior that regularizes segmentation under label imbalance. 2We validate correctness through training convergence criteria, finite-difference gradient checks, and self-consistency between independent forward-backward implementations. We benchmark against 

pytorch-struct [6] and demonstrate that sparse or banded matrix representations are incompatible with semi-CRF inference due to the triangular, rather than diagonal, sparsity structure of the intermediate operators. We characterize the transition from memory-bound to compute-bound scaling as a function of T , K, and C. Flash-SemiCRF provides a structured decoding approach that can be applied to learned representations to produce segmentations that respect duration constraints and maintain segment-level coherence, properties that per-position classifiers typically discard. 

# 2 Terminological clarification 

A linear CRF (or linear-chain CRF) is a model in which every segment has duration one (K=1), so inference reduces to the standard per-position Viterbi/forward–backward recurrence. A linear scan is an algorithm that implements the semi-CRF dynamic program by sweeping left-to-right over positions, as opposed to tree-structured (parallel-scan) alternatives. The two are orthogonal: a linear scan can execute semi-CRF inference at both K=1 and K ≥ 1, and a linear CRF can in principle be computed by any backend. Throughout this paper, “linear CRF” refers to the K=1 model, while “linear scan” refers to the sequential sweep algorithm. 

# 3 Implementation 

## 3.1 Edge Potential Decomposition 

The prefix-sum decomposition of segment content scores [1] enables O(1)-per-edge evaluation from an O(T C ) cumulative score array, reducing the memory footprint by a factor of KC relative to the pre-materialized O(T KC 2) edge tensor. Existing implementations use this decomposition to efficiently pre-compute the full edge tensor. We instead evaluate each potential on-the-fly within the streaming DP kernel, so the tensor is never materialized. The following subsections define the cumulative score array, the edge potential it supports, and boundary projections that extends the standard three-term decomposition. 

## 3.2 Cumulative Centered Scores 

Let fθ (t, c ) denote the raw projected encoder emissions at token t for label c. Before constructing prefix sums, we subtract a sequence-level emission baseline :

νb,c = 1

LbLb−1X

> u=0

fθ(u, c ), ¯fθ(t, c ) = fθ (t, c ) − νb,c (1) where Lb is the true (unpadded) sequence length for batch element b.The cumulative scores are then defined over the centered emissions: 

S0,c = 0 , St,c =

> t−1

X

> u=0

¯fθ (u, c ) (2) The content score for a segment [ s, s + k) labeled c decomposes as a constant-time lookup: 

Ss+k,c − S s,c =

> s+k−1

X

> u=s

fθ (u, c ) − νb,c · k (3) The data-dependent term −νb,c · k introduced by centering couples label identity to segment duration, inducing an adaptive duration prior described in Section 4. 

Numerical motivation. Without centering, cumulative scores grow as ST,c = O(T ) when encoder emissions have nonzero per-label means. At genomic scale ( T > 10 5), segment scores St+k,c − S t,c risk catastrophic cancellation, when both operands reach magnitude O(T ) while their difference is O(1). Sub-tracting the per-label mean before accumulation produces a zero-mean random walk whose cumulative sums have standard deviation O(√T ), reducing the dynamic range by a factor of √T .33.3 Edge Potentials 

The edge potential for a segment ending at position t with duration k, transitioning from source label c′

to destination label c, is: ˜ψ(t, k, c, c ′) =  St,c − S t−k,c 

| {z }

> centered content

+ Bk,c 

|{z} 

> duration bias

+ Tc′,c 

|{z} 

> transition

+ Pstart  

> t−k,c

+ Pend 

> t−1,c

| {z }

> boundary

(4) where k ∈ { 1, . . . , K } is the segment duration, c is the destination label, and c′ is the source label. All terms in Equation (4) are either precomputed arrays ( S, B, T , P) or scalar indices, so each edge potential is evaluated in O(1) time regardless of duration k. The O(T C ) cumulative score array replaces the O(T KC 2) pre-materialized edge tensor, reducing the memory footprint by a factor of KC .

## 3.4 Boundary Projections 

The content score St,c − S t−k,c aggregates encoder emissions uniformly over all positions in a segment, but carries no information about the positions at which segments begin or end. The transition matrix 

Tc′,c captures label-to-label preferences but is position-independent. For applications where segment boundaries have distinctive local signatures, such as exon-intron transitions in genomic annotation, beat onsets in physiological signals, etc., explicit boundary features can improve segmentation accuracy. The optional boundary projections score segment start and end positions directly from the encoder’s hidden representation via independent linear heads, Pstart  

> t,c

= W start  

> c

· ht and analogously for Pend , with 

ht ∈ Rd the encoder hidden state at position t and W start , W end ∈ RC×d learned projection matrices. A segment [ s, s + k) with label c receives additive boundary contributions Pstart  

> s,c

+ Pend 

> s+k−1,c

.Separate start and end projections reflect a structural property of many segmentation problems: segment entry and exit involve categorically different signals (e.g., the start of a ”person” identity in language has different best indicators, the first name, than the end indicators, the last name). The separable form also has a computational advantage: each endpoint requires only a single lookup into a precomputed ( B, T, C ) array, whereas a joint parameterization ϕ(c, hs, hs+k−1) would require O(T KC )precomputation over all valid endpoint pairs. The projections operate on the full encoder hidden states 

ht rather than the label-space scores fθ(t, c ), allowing boundary features to capture context that may not be linearly decodable in label space. When disabled ( Pstart = Pend = 0), Equation (4) reduces to the standard three-term decomposition. 

Relationship to classical semi-CRF features. 

In the classical formulation [1], segment features have the general form g′

> k

(yj , y j−1, x, t j , u j ), where each feature may depend jointly on the segment label, the preceding label, the input, and both endpoint positions, at the cost of materializing the full O(T KC 2) edge tensor. Our boundary projections are the streaming-compatible factorization of this capability: boundary scores depend on position t and destination label c, but not on segment duration k or source label c′, restricting boundary features to the separable form ϕ(yj , x, t j ) + ϕ′(yj , x, u j ). This is the price of the O(KC ) memory guarantee, Pstart and 

Pend are precomputed before the DP loop and cannot condition on quantities only known at recurrence time. 

# 4 Emission Baseline Centering 

The emission baseline subtraction (Equation 1) serves a dual purpose. Numerically, it controls cumu-lative sum magnitude as described in Section 3.2. But per-label centering is not path-invariant, and it simultaneously induces an adaptive duration prior with useful regularization properties. We analyze this modeling effect here so that the reader enters the algorithm with a complete understanding of what the edge potentials encode. 

## 4.1 The Adaptive Duration Prior 

A path-invariant centering, such as subtracting a per-position scalar bt = max c fθ (t, c ) shared across labels, would cancel in the partition function because every valid segmentation covers each position exactly once. Per-label centering with νb,c does not cancel, because different segmentations assign different labels to different positions and νb,c varies across labels. 4The effective segment score under centering (Equation 3) is: 

Seff (c, s, k ) = 

> s+k−1

X

> u=s

fθ(u, c ) + Bk,c − νb,c · k (5) The term −νb,c · k acts as a data-dependent duration prior : labels with high average emission νb,c incur a larger penalty for long segments, while labels with low average emission receive a relative boost. This is equivalent to a canonical semi-CRF with an effective duration model Beff  

> k,c

= Bk,c − νb,c · k that adapts per-sequence based on the global emission statistics. This decomposition has an empirical Bayes-like feature [15]: Bk,c captures population-level duration priors across training sequences, while νb,c provides per-sequence adjustment. Since Bk,c can absorb the average centering effect across training, the adaptive prior primarily regularizes per-sequence variation under label imbalance. 

## 4.2 Worked Example and Ablation 

Table 1 illustrates the asymmetric regularization under label imbalance using a synthetic sequence with three genomic labels. The duration penalty −νb,c · k grows linearly in k, so it primarily affects long segments of the dominant label, and are often the degenerate segmentations one wishes to suppress. Rare labels with low or negative baselines incur minimal penalty or a slight boost, and their centered emissions at active positions have higher contrast (+8 .12 for Promoter ) than for dominant labels (+0 .75 for 

Intron ), because centering subtracts a baseline close to the active-position emission for prevalent labels.                          

> Label cfθ(active) fθ(inactive) Prevalence νb,c −νb,c ·100
> Intron +4 .0−1.085% +3 .25 −325
> Exon +5 .0−0.514% +0 .27 −27
> Promoter +8 .0−0.21% −0.12 +12

Table 1 : Asymmetric regularization under label imbalance ( T = 10 ,000, 

C = 3). The baseline νb,c is the weighted average of active and inac-tive emissions. The dominant label ( Intron ) receives a strong duration penalty ( −325 at k=100), while the rare label ( Promoter ) receives a slight duration bonus (+12) because its baseline is negative. Centered emissions at active positions ( f (active)  

> θ

− νb,c ) are +0 .75 ( Intron ), +4 .73 (Exon ), +8 .12 (Promoter )–centering amplifies signal-to-background contrast for rare labels. In ablation experiments (Table 2) on synthetic label-imbalanced sequences ( C = 3, T = 2 ,000, proportions 75/15/10%), Viterbi decoding under mean centering recovered 59 segments of the rarest label versus 19 under path-invariant centering, and 64 versus 35 for the second-rarest, while the dominant label’s segment count decreased from 142 to 90. Under balanced proportions (0.33 each), segment counts converge across centering modes. 

Architectural context. When the semi-CRF serves as an output decoder, the encoder can learn rep-resentations calibrated to the centering, and the adaptive prior provides robustness to emission scale miscalibration during early training. When semi-CRF marginals serve as input features for a downstream model, a path-invariant centering ( bt = max c fθ (t, c )) is preferable to preserve the canonical semi-CRF distribution. 

# 5 Streaming Forward-Backward Algorithm 

We now present the streaming forward-backward algorithm that maintains only O(KC ) working memory through ring buffers, with gradient checkpointing at ∆ ≈ √T K intervals. The design follows the tiling and recomputation pattern of FlashAttention [13] and the chunked parallel scan of Mamba [14], adapted to the semi-Markov recurrence structure. 5Duration penalty −ν · k Imbalanced Balanced Label νb,c k=10 k=25 k=50 mean none mean none 

INTRON 2.021 -20.21 -50.53 -101.05 90 142 80 84 EXON -0.413 4.13 10.32 20.64 64 35 80 80 UTR -0.603 6.03 15.07 30.14 59 19 76 77 

Table 2 : Implicit duration prior from mean centering under label imbal-ance. The per-label temporal mean νb,c creates a duration penalty −νb,c · k

that penalizes long segments of prevalent labels. Right columns show decoded segment counts: under imbalance (75/15/10%), mean centering redistributes segments from the dominant to rare labels; under balanced proportions (0.33 each), the effect vanishes. Separately, cumulative score magnitudes at 

T =50 ,000 confirm that mean centering grows as O(√T ) (peak /√T ≈ 1.28) versus O(T ) without centering (peak /√T ≈ 484). 

## 5.1 Forward Scan with Ring Buffer 

Algorithm 1 maintains a ring buffer α ∈ RK×C storing the K most recent log-forward messages (Figure 1). At each position t, the recurrence looks back at most K positions, so a ring buffer indexed by t mod K suffices–the oldest entry is always the one about to be overwritten. At checkpoint boundaries (every ∆ positions), the algorithm normalizes the ring buffer by subtracting the current maximum, accumulating the shift into Naccum . This prevents the log-forward messages from drifting to extreme magnitudes over long sequences [13]. The full ring buffer state Ω i and cumulative normalizer Ni are saved at each checkpoint for use in the backward pass. For variable-length batches, the normalization shift is masked to zero for positions beyond the true sequence length Lb, preventing phantom normalization of padding positions. 

Algorithm 1: Streaming Semi-CRF Forward Scan 

Input: S, T , B, Pstart , Pend (optional), checkpoint interval ∆ 

Output: log Z, checkpoints (Ω , N ) 

> 1

α ← −∞ ; α[0 , :] ← 0; Naccum ← 0;  

> 2

Ω[0] ← α; N [0] ← 0;  

> 3

for t ← 1 to T do  

> 4

vt ← −∞ ∈ RC ; 

> 5

for k ← 1 to min( K, t ) do  

> 6

˜αprev ← α[( t − k) mod K, :];  

> 7

h ←  St, : − S t−k, :

 + Bk, : + Pstart  

> t−k, :

+ Pend 

> t−1,:

; 

> 8

E ← h[: , None] + T ⊤; 

> 9

sk ← LSE( ˜ αprev [None , :] + E, axis = 1);  

> 10

vt ← LSE( vt, sk);  

> 11

α[t mod K, :] ← vt; 

> 12

if t mod ∆ = 0 then // Normalize at checkpoint [13]  

> 13

n ← t/ ∆;  

> 14

sb ←

(

max c vt,b (c) if t ≤ Lb

0 otherwise ; 

> 15

Naccum ← N accum + sb; α ← α − sb; 

> 16

Ω[ n] ← α; N [n] ← N accum ; 

> 17

log Z ← LSE( α[T mod K, :]) + Naccum ; 

> 18

return log Z, (Ω , N ); 65.2 Backward Scan and the 2 K Ring Buffer 

The backward pass processes checkpoint segments in reverse order. For each segment [ i∆, (i+1)∆), the algorithm restores α from the saved checkpoint Ω i, recomputes the local forward messages within the segment, then scans β right-to-left, computing marginals and accumulating gradients at each position (Figure 1b). The saved normalizer Ni restores the true scale of α when computing marginals via log μ =˜α + ˜ψ + ˜β + Ni − log Z. The full backward algorithm is given in Appendix A. While the forward pass uses a ring buffer of size K (sufficient since we only look back K positions), the backward pass requires a ring buffer of size 2 K. This asymmetry arises from a write-before-read hazard in the backward access pattern: at position t we read β values from positions {t+1 , . . . , t +K}

and write β[t], so with K slots the write at t collides with the read at t+K (both map to t mod K), overwriting β[t+K] before it is read. Doubling the buffer to 2 K slots separates write and read indices for the entire lookback window, eliminating the conflict. The additional KC float memory overhead is negligible compared to the O(pT /K · KC ) checkpoint storage. 

Forward scan 

αt ← αt−k

k = 1 . . . K 

Checkpoints 

Ωi = ( α, Ni)every ∆ steps 

Backward scan 

βt ← βt+k

k = 1 . . . K  

> save Ω irestore Ω i

recompute α

K-slot ring          

> αt-3 αt-2 αt-1 αt
> write tmod K
> Ω0,N0
> Ω1,N1
> Ω2,N2

⌈T / ∆⌉ total 

2K-slot ring (no R/W hazard)          

> βt+1 βt+2 βt+3 βt+4
> read: k= 1 . . . K write tmod 2 K

Per-segment cycle: restore Ω i → recompute α[i∆.. (i+1)∆] → emit gradients → free 

Memory: O(KC )

| {z }

> ring buffers

+ O

p T /K · KC 

| {z }

> checkpoints

Fig. 1 : Memory layout for streaming semi-CRF inference. The forward scan maintains a K-slot ring buffer indexed by t mod K, checkpointing the full buffer state Ω i and cumulative log-normalizer Ni every ∆ positions. During the backward pass, each segment is processed independently: α is restored from Ω i

and recomputed locally, while β is scanned right-to-left through a 2 K-slot ring buffer that prevents the write-before-read hazard (Section 5.2). Total memory is O(KC ) for ring buffers plus O(pT /K · KC ) for checkpoints, sublinear in T .

## 5.3 Complexity 

Table 3 summarizes the time and memory complexity of the streaming algorithm. The dominant working memory is the O(KC ) ring buffer, which is independent of sequence length T . Checkpoints contribute 

O(pT /K ·KC ), which grows sublinearly in T . The total time is O(T KC 2), the same as a standard semi-CRF forward-backward pass. The dominant memory reduction is replacing the O(T KC 2) edge tensor with the O(T C ) prefix-sum array (a factor of KC ). The ring buffer further reduces forward-message storage from O(T C ) to O(KC ), independent of sequence length. At T = 10 6, K = 200, C = 6, the edge tensor would require ∼29 GB in float32; the prefix-sum array requires ∼24 MB, and the ring buffer 

∼10 KB. 

## 5.4 Adaptive Loop Tiling 

The marginal computation requires a ( CPAD ×CPAD ) matrix per ( t, k ) pair, which at CPAD = 64 demands approximately 384 registers per thread. On our L40S GPUs (65,536 registers per SM), this exceeds the available register file at >4 warps and causes spilling to slow local memory. We tile the destination label dimension in blocks of size τ , reducing peak register demand to ∼120 per thread and enabling 4–8 warps without spilling. The β reduction across tiles uses the online logsumexp pattern from FlashAttention [13]. 7Component Time Memory                            

> Prefix-sum array O(T C )O(T C )Forward scan O(T KC 2)O(KC ) ring buffer Checkpoints –O(pT /K ·KC )Backward scan O(T KC 2)O(KC ) ring buffer (2 Kslots) Alpha recompute O(T KC 2)O(∆ ·C) per segment Gradient workspace –O(B·Nckpt ·KC 2)
> Total O(T KC 2)O(KC +pT /K ·KC )†

Table 3 : Complexity analysis. The DP working memory (ring buffers) is O(KC ), independent of T ; checkpoint mem-ory grows as O(√T ). The O(T C ) prefix-sum array is the dominant memory term but replaces the O(T KC 2) edge tensor, a reduction by factor KC . Nckpt = ⌈T / ∆⌉, B is batch size. †Working memory only; excludes the O(T C )prefix-sum array (precomputed, not part of the streaming DP) and the O(B · N · KC 2) gradient workspace. The tile size τ is selected adaptively based on C to balance compile time, register pressure, and iteration count (Table 4).                       

> CPAD τIterations Rationale
> ≤842Minimal iteration count
> ≤16 82Minimal iteration count 32 16 2Balanced 64 16 4Moderate register pressure
> ≥128 32 ≤8Bounded compile time

Table 4 : Adaptive tile size selection. The algorithm bounds iteration count to ≤ 8 even at C = 256 while keeping register pressure manageable. 

## 5.5 Correctness validation. 

Because the Triton kernel uses a different floating-point reduction order than the PyTorch reference, bit-wise agreement is not expected. Instead, we validate through three strategies (Table 5). Self-consistency: 

boundary and emission marginals from the backward pass must satisfy the following probabilistic invari-ants, 1) marginals bounded as P (c | t) ∈ [0 , 1], 2) class marginals normalized as P 

> c

P (c | t) = 1 at each position, 3) total mass conserved as P 

> t,c

P (c | t) = T , and 4) zero mass at padded positions. All invari-ants hold at genome scale ( B=142, T =100 ,000, C=24, K=100) with per-position deviation of 1 .6×10 −4.

Finite-difference gradient checking: for each element of S (cumulative scores), T (transition matrix), and B (duration bias), we perturb by ±ε (ε = 10 −3) and compute the central-difference approxima-tion ( f (x+ε) − f (x−ε)) /2ε using only forward-pass evaluations. This numerical gradient, independent of the backward kernel, serves as ground truth against which the Triton autograd output is compared. All parameters achieve cosine similarity ≥ 0.9999 and normalized maximum error < 5 × 10 −5, confirming that the custom backward kernels produce correct gradients. Training convergence comparison: mod-els initialized with the same random seeds were trained with the streaming PyTorch, streaming Triton, and pytorch-struct linear scan backends reaching the same final negative log-likelihood (relative dif-ference < 10 −9) with loss-curve cosine similarity of 1 .0 after 100 epochs, while the Triton backend runs approximately 5 .7× faster. 8(a) Self-consistency – large scale ( B=4 , T =2 , 000 , C=32 , K=50 )

Invariant Observed Boundary marginals ∈ [0 , 1] [0 .9699 , 1.0000] Expected segments P 

> t

p(bdy | t) ∈ [1 , T ] ¯x = 1940 .6, range [1940 .6, 1940 .6] 

P 

> c

P (c | t) = 1 max dev. = 1 .5 × 10 −6

P 

> t,c

P (c | t) ≈ T 2,000 ± 1.5 × 10 −3 (0.0001%) Emission marginals ≥ 0 observed min = 0 .0179 Padding-mask consistency all padded positions = 0 

(b) Self-consistency – genome scale ( B=142 , T =100 , 000 , C=24 , K=100 )

Invariant Observed Boundary marginals ∈ [0 , 1] [0 .9597 , 1.0000] Expected segments P 

> t

p(bdy | t) ∈ [1 , T ] ¯x = 96085 .4, range [96085 .3, 96085 .5] 

P 

> c

P (c | t) = 1 max dev. = 1 .6 × 10 −4

P 

> t,c

P (c | t) ≈ T 100 ,000 ± 8.10 (0.0081%) Emission marginals ≥ 0 observed min = 0 .0208 Padding-mask consistency all padded positions = 0 

(c) Finite-difference gradient validation 

Parameter Elements Cosine sim. Norm. max err Max |∆|

cum scores 1,600 1.000000 2.1 × 10 −5 1.5 × 10 −6

transition 256 1.000000 4.6 × 10 −6 2.3 × 10 −6

duration bias 400 1.000000 2.6 × 10 −6 1.7 × 10 −5

(d) Training convergence 

Backend Final NLL Rel. diff Curve cos. Time (s) 

pytorch 1606.71 – – 810 

triton 1606.71 6.4 × 10 −10 1.000000 141 

linear scan 1606.71 1.2 × 10 −9 1.000000 697 

Table 5: Full correctness validation of the Triton Semi-CRF implementation. 

(a) Self-consistency invariants at large scale. (b) Self-consistency invariants at genome scale. (c) Finite-difference gradient validation: central differences ( ε=1 .0 ×

10 −3) vs. autograd ( B=1, T =100, C=16, K=25). (d) Training convergence across backends (100 epochs, B=4, T =500, C=32, K=50). Performed on an NVIDIA L40S GPU. 96 Experiments 

We evaluate the ability of Flash-SemiCRF to perform exact semi-Markov inference across a range of values of T , K, and C, scaling from NER-sized problems to genome-scale sequences, measuring time, memory, and throughput. We further show that the natural candidates for exploiting bounded K – banded or block-sparse matrix formats – cannot prevent the near-dense intermediates that arise under composition in a parallel-scan formulation. Next, we benchmark linear CRF (K=1) and semi-CRF (K=30) on the DARPA TIMIT speech corpus [16]. Before presenting scaling benchmarks, we address whether tree-structured alternatives could exploit the bounded K via banded kernels. 

## 6.1 Infeasibility of Banded Backends for Tree-Structured Inference 

Bounded segment length K suggests that tree-structured backends could exploit sparsity via banded or block-sparse kernels. Two structural results show they cannot (full proofs in Appendix C). First, at a tree node of span S, the feasible duration constraint d1 + d2 ≤ S is anti-diagonal triangular ,not diagonal-banded; the clique formed by all states with d ≤ ⌊ S/ 2⌋ forces a permutation-independent bandwidth lower bound of C⌊S/ 2⌋ − 1, so the compatibility matrix is near-dense once S ≳ K. Second, even genuinely banded one-step operators fill in under composition: bw( Bm) = min( T, mK ), saturating at dense width after O(log( T /K )) tree levels. Empirically, bandwidth-reducing permutations yield ratios bw best /(n−1) ≥ 0.97 for S ≥ K, and block-triangular formats that match the triangular geometry did not yield speedups due to indirect indexing overhead. Consequently, banded storage cannot prevent the near-dense intermediates that drive the high memory requirements in tree-structured formulations, motivating the streaming approach of Section 5. Similarly, pytorch-struct implements a heuristic of 

KC > 200 state space switch to use the linear scan backend instead of the default tree search approach. 

## 6.2 Benchmarking 

Time and Memory 

We benchmark semi-CRF backends across T , K, and C (Fig. 2). Tree-based methods (binary tree, memory sharded tree) achieve the lowest per-position latency at small state sizes but degrade rapidly as (K+1) C increases (Fig. 2A, B), reflecting the cost of materializing O(T (KC )2) intermediates. Linear and streaming scans exhibit stable, state-size-independent compute scaling; the fused Triton kernel matches this stability while improving throughput over unfused linear scans through on-the-fly edge evaluation (Fig. 2E). The dominant constraint at scale is memory, not compute. Linear and streaming scans maintain a small, constant memory footprint regardless of T , while tree-based and block-structured methods require orders of magnitude more memory (Fig. 2C). This separation is most visible in the out-of-memory (OOM) frontier (Fig. 2D, F): tree-based methods support progressively smaller state sizes as T grows, whereas the fused kernel maintains a nearly constant maximum state size. Eliminating edge tensor materialization fundamentally alters the scaling behavior of semi-CRF inference. To assess performance in regimes relevant to genomics-scale segmentation (large T and K), we further evaluate the fused Triton implementation at extended sequence lengths and segment sizes (2G).In this setup, the tradeoff between memory and per-position runtime for K=2000–8000 is driven by hardware-specific constraints, likely due to L1 cache saturation, and will vary across systems. Further, in this setup where the competing methods are no longer tractable due to memory constraints, the fused kernel maintains near-linear runtime scaling in K and a stable memory footprint, demonstrating practical applicability to kilobase-scale segmentation tasks. 

DARPA TIMIT Speech Corpus 

To evaluate Flash-SemiCRF as a training and inference layer on a real segmentation task, we benchmark on the DARPA TIMIT acoustic-phonetic corpus [16], a standard testbed for structured sequence models in speech recognition. TIMIT is a natural fit for semi-CRFs because phonemes have characteristic, label-dependent durations (vowels persist longer than stops, silence segments are highly variable) that a duration-aware model can exploit. TIMIT provides 630 speakers (462 train, 168 test) with time-aligned phoneme annotations at 61 phone classes, collapsed to the standard 39-phone set ( C = 39, CPAD = 64) following the Lee and Hon convention [17]. Input features are 13 MFCCs with delta and delta-delta coefficients (39 dimensions total) extracted at 10 ms frame shift, z-score normalized using training-set statistics. 10 Fig. 2 : Theoretical Benchmarks . A. Compute time per positions relationship to model state size between backends across different sizes T. B. Compute time for forward and backwards pass between each back-end. C. Peak Memory utilization between backends at T=128, K=4, C=3. D. Mapping the out of memory (OOM) frontier for each backend across different sizes of T. E. Comparing fused Triton kernel opti-mization to original linear scan and binary tree backends (pytorch-struct) for compute time per position across different sizes T F. Demonstrating Triton implementation maintains linear memory requirements across sizes T while Binary tree is able to be used as a viable compute backend. G. Demonstration of triton streaming backend performance at scales T=50,000, C=6, batch size=32 and variable size K. 11 Experimental setup. 

Both models share an identical 3-layer bidirectional LSTM encoder (hidden dimension 256, dropout 0.3) followed by the Flash-SemiCRF structured decoder, trained with AdamW ( η = 10 −3, weight decay 10 −5)and cosine annealing over 50 epochs (batch size 32). CRF parameters receive L2 regularization ( λ = 0 .01) and gradient clipping at norm 5.0. The partition function is computed in float64 for numerical stability. Learnable scalar sequence boundary parameters πstart , π end ∈ RC are folded into the cumulative scores via the prefix-sum trick described in Appendix D.4. The only difference between models is the maximum segment duration: K = 1 (linear CRF) versus K = 30 (semi-CRF). This adds K × C = 1 ,170 duration bias parameters to the semi-CRF, negligible relative to the encoder’s ∼1.6M parameters. At CPAD = 64, the per-checkpoint gradient workspace (Section A.4.4) requires ∼120 MB for a batch of 32, well within the regime where workspace memory is eclipsed by the edge tensor savings. 

Convergence. 

Figure 3 shows training convergence across three metrics. Both models converge to similar final Phone Error Rates (PER = 0 .218 for K=30 vs. 0 .219 for K=1; best PER 0 .2178 vs. 0 .2187 at epoch 43), consistent with the expectation that a strong neural encoder absorbs much of the segmentation structure into its learned representations, compressing the marginal contribution of explicit duration modeling to corpus-level PER. The semi-CRF maintains a small but consistent advantage in boundary F1 (0 .476 vs. 0 .468) and segment F1 (0 .215 vs. 0 .207), reflecting improved segmentation coherence from explicit duration modeling. The semi-CRF starts behind (PER = 0 .95 at epoch 0 vs. 0 .89 for K=1), requiring additional warmup to explore the larger state space, but overtakes the linear CRF by epoch 5 on PER and by epoch 1 on boundary and segment F1. The PER gap peaks near epoch 20 (∆ ≈ 0.005) and compresses to ∼0.001 as both models converge, suggesting that harder tasks with larger T or C may exhibit a more pronounced semi-CRF advantage. Validation loss is consistently higher for the semi-CRF (206 .8 vs. 183 .7 at epoch 50) despite its lower PER. This is expected: the semi-CRF partition function sums over a combinatorially larger space of valid (K, C ) segmentations, distributing probability mass more broadly than the per-frame linear CRF. The discrepancy underscores that the negative log-likelihood and the MAP segmentation accuracy measure distinct quantities.                          

> Metric K=1 (Linear CRF) K=30 (Semi-CRF)
> Phone Error Rate (PER) ↓0.219 0.218
> Boundary F1 ↑0.468 0.476
> Boundary F1 ±2 frames ↑0.914 0.919
> Segment F1 ↑0.207 0.215
> Boundary entropy Hbdy 5.687 (invariant) 5.529
> Position entropy Hpos 3.651 3.651 Validation NLL 183.7 206.8

Table 6 : TIMIT benchmark results ( C = 39, 50 epochs, best check-point by PER at epoch 43). Both models share the same BiLSTM encoder ( ∼1.6M parameters); the semi-CRF adds K × C = 1 ,170 duration bias parameters. Boundary entropy for the linear CRF is structurally fixed: p(boundary | t) = 1 when K=1. 

Throughput. 

Table 7 compares wall-clock training and inference time on a single L40S GPU. The pytorch-struct 

linear scan [6] requires 4,430 s per training epoch and 1,243 s per inference pass at K=30, making a 50-epoch experiment a multi-day commitment. Flash-SemiCRF reduces this to 175 s per epoch (25 ×

speedup) and 7 s per inference pass (178 ×), processing 6.5k frames/s versus 0.3k. Despite the 30 × increase in theoretical O(T KC 2) complexity from K=1 to K=30, training the semi-CRF takes only 6 .5× longer per epoch: 175 s versus 27 s. Inference time for K=1 (32 s) is paradoxically slower than for K=30 (7 s). The K=1 CRF forward pass is fast (as reflected in the 27 s training time, which is dominated by forward-backward); the bottleneck 12 Backend Model K Train (s/ep) Infer (s) utt/s kfr/s                         

> pytorch-struct linear scan Semi-CRF 30 4,430 1,243 0.8 0.3 Flash-SemiCRF (Triton) Semi-CRF 30 175 721.1 6.5 Flash-SemiCRF (PyTorch) Linear CRF 127 32 140 42.8
> Speedup (Flash K=30 vs. pytorch-struct) 25 ×178 ×26 ×22 ×

Table 7 : Wall-clock training and inference time on TIMIT (single NVIDIA L40S GPU, B = 32, C = 39). Flash-SemiCRF achieves a 25 × training speedup and 178 × inference speedup over pytorch-struct’s linear scan, the only existing backend that can run K=30 at this scale without OOM (tree backends exceed memory at (K+1) C = 1 ,209; see Fig. 2D). All three backends compute the same partition func-tion (Table 5d). is the unfused PyTorch Viterbi decode, which runs outside the Triton kernel pipeline. The K≥3 path dispatches to the fused Triton kernel for both forward-backward and Viterbi (Appendix D.9), avoiding this overhead. A fused K=1 Triton Viterbi was not prioritized since the training loop, where kernel fusion provides the dominant speedup, already dispatches efficiently at K=1. 

Boundary entropy and structural differences in uncertainty. 

The clearest separation between models appears in boundary entropy (Figure 3). Complementing the per-position label entropy Hpos (the sequence-averaged form of the frame-level class entropy used by Salvi [18] for phonetic boundary detection), we define a sequence-level diagnostic as the Shannon entropy [19] of the normalized boundary distribution: 

Hbdy = −

> T

X

> t=1

ˆpt ln ˆ pt, ˆpt = p(boundary | t)

P 

> t′

p(boundary | t′) , (6) where p(boundary | t) is the posterior segment-start probability from the forward-backward algorithm, and exp( Hbdy ) gives the effective number of boundary positions. For K=1, every frame is its own segment, so ˆ pt = 1 /T and Hbdy = ln T per utterance regardless of learned parameters; the linear CRF does not represent boundary uncertainty. Averaged over the test corpus, the K=1 invariant is E[ln T ] = 5 .687 (geometric mean length ≈ 295 frames). The semi-CRF reduces Hbdy from 5 .671 at initialization to 5 .529 by epoch 50, concentrating probability mass at true phoneme transitions; the effective boundary count exp( Hbdy ) drops from ∼295 to 

∼252. Per-position label entropy converges to the same value for both models ( Hpos = 3 .651), indicating that the encoder representations are comparably expressive and the difference lies in the structural decoder. 

Per-utterance uncertainty analysis. 

The corpus-level PER gap (∆ = 0 .001) averages over utterances where the models perform comparably and utterances where they diverge. Figure 4 shows posterior marginals p(c | t) and boundary posteriors for DR4 MPCS0 SI729 ( T = 480, 39 ground-truth segments), where the semi-CRF achieves PER = 0 .232 versus the linear CRF’s 0 .411, a 44% relative reduction concentrated in regions of phonetic ambiguity. Both models activate similar label hypotheses, but the semi-CRF concentrates posterior mass more tightly along the correct label rows. The boundary posterior panel makes the structural difference explicit: the linear CRF assigns p(boundary | t) = 1 .0 uniformly (expected at K=1), while the semi-CRF’s boundary posterior rises at true phoneme boundaries (red dashed lines) and falls to p < 0.2 in segment interiors. The semi-CRF thus provides a posterior over segmentation structure that a per-position model cannot represent, useful for downstream tasks requiring boundary uncertainty. 13 Fig. 3 : Training convergence on TIMIT: K=1 linear CRF vs. K=30 semi-CRF. Both models share an identical BiLSTM encoder; the only difference is the maximum segment duration. The semi-CRF main-tains a similar Phone Error Rate (PER) as the linear CRF, but consistent improvement at boundary and segment F1 despite starting from a higher initial PER (larger state space requires additional warmup). 

Fig. 4 : Per-utterance posterior analysis on TIMIT utterance DR4 MPCS0 SI729 ( T = 480 frames). 

Top: Ground-truth (GT) phoneme segmentation. Middle panels: Position-marginal heatmaps p(c | t)for the semi-CRF ( K=30, PER = 0 .232) and linear CRF ( K=1, PER = 0 .411). Both models activate similar label hypotheses, but the semi-CRF concentrates posterior mass more tightly on correct labels. 

Bottom: Boundary posterior p(boundary | t). The semi-CRF (blue) varies across the utterance, peaking at true phoneme boundaries (red dashed lines) and suppressing in segment interiors. The linear CRF (orange) assigns p(boundary | t) = 1 .0 uniformly, the expected behavior when every frame is its own segment ( K=1). 14 7 Discussion 

The central barrier to scaling semi-Markov Conditional Random Fields has not been their asymptotic complexity, but the memory constraints of the standard implementations. Existing approaches materi-alize an O(T KC 2) edge tensor, making inference memory bound and infeasible on long sequences. By eliminating this tensor through prefix-sum decomposition and streaming dynamic programming, Flash-SemiCRF reframes semi-CRF inference as a compute-bound problem. This shift mirrors recent advances such as FlashAttention and scan-based sequence models, where avoiding intermediate materialization, not just changing asymptotics, enables orders of magnitude improvements in scale. This change in computational approach has direct modeling consequences. Semi-CRFs, long limited to short sequences in domains such as natural language processing, now become viable as structured decoders for long-range sequence modeling. This addresses a persistent limitation of modern neural sequence models: while they learn rich contextual representations, they typically operate under a per-position prediction paradigm that does not enforce globally consistent segmentations. In contrast, semi-CRFs ensure coherent partitions of the input and model the label-dependent duration explicitly. With Flash-SemiCRF, these properties can now be applied at scale, enabling segmentation tasks to combine learned representations with explicit structural constraints (such as event detection, temporal action segmentation, or domain parsing). Both linear and semi-CRF models have complementary strengths that make them useful in different parts of a genomics workflow. Linear CRFs are computationally efficient and well-suited for annotating known sequence structures (e.g., Tranquillyzer, Bonito/Dorado). In contrast, semi-CRFs provide modeling of segment structure and boundary uncertainty, which can be advantageous for more complex annotation tasks, albeit at the cost of increased computational overhead due to the larger context space and exact marginals. The edge potential decomposition (3.4) also has consequences for gradient flow and encoder design. Because the boundary projections Pstart , Pend and the content emissions fθ enter as separate additive terms, their gradients are structurally decoupled: the Jacobian is block-diagonal in ( W start , W end ) vs. 

fθ, so parameter updates can adjust boundary and content scoring independently. In a joint edge tensor parameterization these error signals are entangled, which can slow convergence when boundary and interior features have different characteristic scales or noise profiles. The additive decomposition also provides a natural interface for multi-modal encoders. Modalities that produce punctate, position-specific signals (e.g., Tn5 insertion densities from ATAC-seq) can be routed to the boundary projections via modality-specific heads on ht, while broad-domain signals (e.g., aggregate expression counts, histone mark coverage) feed the content term through fθ and accumulate evidence via the prefix sum. This routing is possible precisely because the terms are precomputed independently; it would not be available in a parameterization where boundary and content features are jointly materialized. Beyond computational efficiency, the formulation introduces a subtle but important statistical effect. The sequence-level centering of emissions induces an adaptive, data-dependent duration prior Beff  

> k,c

=

Bk,c − νb,c · k. This acts as an implicit regularizer: labels with high average emissions incur stronger penalties for long segments, while low-prevalence labels receive a relative boost. In imbalanced settings, this mechanism suppresses degenerate segmentations dominated by frequent states and improves recovery of rare classes. At the same time, because this transformation is not path-invariant, it alters the underlying distribution and may be undesirable in settings requiring strict probabilistic interpretation. The choice between centered and uncentered formulations therefore reflects a trade-off between numerical stability, regularization, and model fidelity. We note a limitation regarding the state space size C. To ensure deterministic gradients and eliminate inter-segment atomic contention on the GPU, Flash-SemiCRF allocates a gradient workspace of size 

O(B · Nckpt · K · C2). While this introduces a quadratic dependence on the number of labels, genomic segmentation tasks, parsing promoters, UTRs, exons, introns, and intergenic regions, rarely require C > 

64. Within this regime, the workspace memory remains low and is entirely eclipsed by the savings from avoiding the O(T KC 2) edge tensor materialization. The implementation trades asymptotic scaling in C

for transformative scaling in the dimensions that define genomic workloads ( T and K). A more consequential constraint in practice is the maximum segment duration K. Segments longer than K cannot receive a unified duration or content score and are instead captured through K-position lookback and boundary projections alone, sacrificing the full segment-level modeling that motivates the semi-CRF. In human genomics, median protein-coding exons ( ∼130 bp) and most introns ( ∼1,539 bp median) fall well within practical K values, but the 95th percentile intron length ( ∼22 ,908 bp) approaches the regime where runtime and register pressure costs become significant (Section 5.4). Selecting K to cover 15 the segments of greatest modeling interest is therefore the primary tuning decision, and characterizing how segmentation quality degrades as features extend past K is an important direction for future work. From a systems perspective, the results highlight a broader principle: structured models are often dis-missed as unscalable due to nominal complexity, when in practice the limiting factor is memory traffic and tensor materialization. By converting semi-CRF inference into a fused, streaming kernel, we demonstrate that exact structured inference can achieve high throughput and full hardware utilization at sequence lengths exceeding 10 6. This suggests that other structured models based on dynamic programming over variable-length segments, such as segmental RNNs [4], may admit similar reformulations. 

Acknowledgements. We gratefully acknowledge the feedback on this work from Brad Dickson. Com-putation for the work described in this paper was supported by the High Performance Cluster and Cloud Computing (HPC3) Resource at the Van Andel Research Institute. 

# Declarations 

• Funding: Support for this work was provided by VAI. 

• Conflict of interest: The authors declare no competing interests. 

• Code availability: [https://github.com/biobenkj/flash-semicrf] 

• Author contribution: B.K.J., T.G., A.S., H.S., and H.J.J. conceived the project. B.K.J. and T.G. implemented and bench marked the code base. B.K.J. and T.G. wrote the manuscript with contri-butions from A.S., H.S., and H.J.J.. H.S. and H.J.J. provided co-supervision and funding for this project. 

# Appendix A Streaming Backend Supplement 

## A.1 Notation 

Table A.1 collects all symbols used in the main text and supplement.                                                                    

> Symbol Description Shape
> TPadded sequence length (batch dimension) scalar
> LbTrue (unpadded) length for batch element bscalar
> KMaximum segment duration scalar
> CNumber of labels (states) scalar
> CPAD Padded label count (next power of 2) scalar
> BBatch size scalar
> St,c Cumulative centered scores (B, T + 1 , C )
> fθ(t, c )Raw projected encoder emissions (B, T, C )
> νb,c Sequence-level emission baseline (B, C )¯fθ(t, c )Centered emissions: fθ(t, c )−νb,c (B, T, C )
> Tc′,c Transition scores (source c′to dest c)(C, C )
> Bk,c Duration bias for duration k, label c(K, C )
> Pstart
> t,c Start boundary projection (optional) (B, T, C )
> Pend
> t,c End boundary projection (optional) (B, T, C )˜αt(c)Log-forward message at position t, label c(B, C )˜βt(c)Log-backward message (B, C )
> αForward ring buffer (B, K, C )
> βBackward ring buffer (B, 2K, C )ΩCheckpointed ring buffer states (B, N ckpt , K, C )
> NCumulative log-normalization factors (B, N ckpt )∆Checkpoint interval ( ≈ √T K )scalar
> τTile size for label dimension scalar log ZLog partition function (B, )

Table A.1 : Notation. Tilde (˜ ·) denotes log-domain quantities. 

Nckpt = ⌈T / ∆⌉ is the number of checkpoints. 

Indexing conventions. Positions t ∈ { 0, . . . , T } index boundaries between tokens; cumulative scores 

St,c are defined at boundaries, with S0,c = 0. Emissions fθ (t, c ) are defined at tokens t ∈ { 0, . . . , T −1}

(0-indexed). Segments are half-open intervals [ s, s +k) covering tokens s, s +1 , . . . , s +k−1, so that the content score is Ss+k,c − S s,c . Durations are 1-indexed: k ∈ { 1, . . . , K }, stored at 0-indexed positions 

k−1 in code. 16 Algorithm A.1: Streaming Semi-CRF Backward Pass 

Input: S, T , B, Pstart , Pend (optional), checkpoints (Ω , N ), log Z, upstream ∂L/∂Z 

Output: ∇S , ∇T , ∇B  

> 1

β ← −∞ ∈ R2K×C ; β[T mod 2 K, :] ← 0;  

> 2

Initialize gradient accumulators;  

> 3

for i ← Nckpts − 1 to 0 do  

> 4

tstart ← i · ∆; tend ← min(( i + 1) · ∆, T );  

> 5

Ni ← N [i];  

> 6

αlocal ← RecomputeAlpha (Ω[ i], tstart , tend );  

> 7

for t ← tend − 1 to tstart do  

> 8

˜αt ← αlocal [t − tstart , :];  

> 9

˜βt ← −∞ ; 

> 10

for k ← 1 to min( K, T − t) do  

> 11

˜βnext ← β[( t + k) mod 2 K, :]; // 2K ring buffer  

> 12

˜ψ ← edge potential (main text, Eq. (4)); 

// Log-norm correction restores true α scale  

> 13

log μ ← ˜αt[None , :] + ˜ψ + ˜βnext [: , None] + Ni − log Z; 

> 14

μ ← exp(log μ);  

> 15

Accumulate ∇S , ∇T , ∇B from μ; 

> 16

˜βt ← LSE( ˜βt, LSE( ˜ψ + ˜βnext [: , None] , axis = 0));  

> 17

β[t mod 2 K, :] ← ˜βt; // 2K ring buffer  

> 18

return ∇S , ∇T , ∇B ;

## A.2 Backward Pass with Checkpointing 

The backward pass processes checkpoint segments in reverse order, recomputing α from saved checkpoints following the gradient checkpointing strategy described in the main text. Algorithm A.1 gives the full pseudocode. The saved normalizer Ni restores the true scale of α when computing marginals. 

## A.3 Gradient Computation 

Gradients follow by the standard exponential-family identity: ∇θ log Z equals the expected sufficient statistics under pθ , computed from the joint segment marginals μ(t, k, c, c ′) = exp(˜ αt−k(c′) + ˜ψ + ˜βt(c) + 

Ni − log Z) produced by the forward-backward pass. The implementation challenges lie not in these standard expressions but in accumulating them efficiently on GPU, which we address next. 

## A.4 Reduced Atomics: Full Algorithms 

A.4.1 Reduced Atomics Strategy 

GPU atomic operations are expensive and introduce non-determinism from floating-point non-associativity. For per-position gradients ∇S t,c , we accumulate all k and tile contributions into thread-local registers before a single atomic write, reducing atomics from K × tiles to 1 per position (e.g., 4 ,000 → 1at K=1000, C=64). For shared parameters ∇T and ∇B , we allocate per-checkpoint-segment workspace buffers that eliminate inter-segment atomic contention entirely, with a final deterministic einsum 

reduction on the host. 

A.4.2 Local Accumulation for Per-Position Gradients 

For ∇S t,c , the negative contribution (from segments starting at t) is the same position for all k values. Instead of K × tiles atomic operations, we accumulate locally: 17 Algorithm A.2: Local Accumulation for ∇S t 

> 1

grad cs t local ← 0 ∈ RCPAD ; 

> 2

for k ← 1 to K do  

> 3

for each tile do  

> 4

grad cs t local −= P 

> c′

μtile (t, k, c, c ′); // Register accumulation  

> 5

atomic add (∇S t, grad cs t local ); // Single write 

Speedup : K × tiles → 1 atomic per position (e.g., 1000 × 4 = 4000 → 1 at K = 1000, C = 64). 

A.4.3 Per-Duration Accumulation for Duration Bias 

For ∇B k,c , we accumulate across all tiles for each k, then write once: 

Algorithm A.3: Per-Duration Accumulation for ∇B k 

> 1

for k ← 1 to K do  

> 2

grad db k local ← 0 ∈ RCPAD ; 

> 3

for each tile do  

> 4

grad db k local += P 

> c′

μtile (t, k, c, c ′);  

> 5

atomic add (∇B k, grad db k local ); // Once per k

Speedup : tiles → 1 atomic per ( t, k ) pair. 

A.4.4 Segment-Isolated Workspace Buffers 

For shared parameters ( ∇T , ∇B ), cross-segment atomic contention introduces non-determinism. We allocate per-segment workspace buffers: 

grad tr workspace ∈ RB×Nsegments ×K×CPAD ×CPAD (A.1) (Padded to CPAD to prevent out-of-bounds access from masked Triton threads; sliced back to C before reduction.) Each checkpoint segment writes to its own slice, eliminating inter-segment atomics. The final reduction uses deterministic host-side operations: 

∇T k,c ′,c = einsum (“bskij, b → kij” , workspace.sum(dim=1) , ∇out ) (A.2) 

∇B k,c = einsum (“bskc, b → kc” , workspace.sum(dim=1) , ∇out ) (A.3) 

Determinism : The segment-wise sum is deterministic (fixed order), and einsum performs a single reduction pass. 

Memory cost : O(B · Nckpt · K · C2) workspace, which is acceptable since Nsegments ≈ pT /K is typically small (e.g., 10–100 for T = 100k, K = 1000). 

## A.5 Online Logsumexp for Tiled β Reduction 

The adaptive loop tiling described in the main text tiles the destination label dimension in blocks of size τ . Since the β reduction spans multiple tiles, we use the standard online logsumexp pattern from FlashAttention [13] (tracking a running maximum and rescaled sum of exponentials across tile boundaries) to compute exact logsumexp without materializing the full CPAD × CPAD matrix. 

Two-pass tile normalization for marginals. Marginal computation (Algorithm A.1, line 7) also tiles across destination labels. A naive approach would normalize each tile independently, but different tiles would use different local maxima, causing inconsistent marginals and 10–400% gradient errors when 

C > τ . The implementation uses a two-pass strategy: Pass 1 sweeps all tiles to compute a single global maximum m∗ = max c( ˜ αt + ˜ψ + ˜β); Pass 2 reprocesses all tiles using m∗ as a fixed normalizer, ensuring every tile produces marginals on the same scale. 

## A.6 Additional Numerical Stability Measures 

Beyond emission baseline centering and checkpoint boundary normalization (both described in the main text), the implementation employs several additional stability measures. 18 Log-domain. All forward-backward computations use logsumexp: LSE( x) = max( x) + log P 

> i

exp( xi − max( x)). 

NEG INF guards. When all logsumexp inputs are −10 9, the subtraction x − max( x) = 0 instead of remaining at −∞ . Guards detect this case (max < −10 9 + 1) and return −∞ directly. 

Float64 accumulation. Gradient tensors for shared parameters use float64 to prevent non-determinism from atomic add floating-point non-associativity. Error scales as O(√T × K × C) per operation; float64 reduces this from ∼10 −3 (float32) to ∼10 −10 (negligible). 

Log-scale clamping. The log-scale factor log s = max( α)+ Ni −log Z is clamped to [ −700 , 0] before exp(). The upper bound of 0 reflects that segment marginals are probabilities ( s ≤ 1); the lower bound of −700 prevents float64 underflow (exp( −710) ≈ 0). 

Intermediate clamping. Before computing the joint log-probability ˜αt + ˜ψ + ˜β, each term is clamped to [ −10 6, 10 6]. This prevents float overflow when three large-magnitude terms are summed, without affecting valid values (which are O(√T ) after checkpoint normalization). 

Masking. Invalid positions use −10 9 (not −∞ ) to avoid NaN in gradients. 

Variable-length batches. For sequences ending at L < T , the normalization shift is masked to zero for t > L , freezing Naccum at the correct value. 

## A.7 Emission Baseline: Extended Notes 

Padding semantics. The implementation computes the baseline using a masked mean over valid positions only: 

νb,c = 1

LbLb−1X

> u=0

fθ (u, c ) (A.4) where Lb is the true (unpadded) sequence length for batch element b. Padding tokens ( u ≥ Lb) do not influence the baseline, so the effective model is independent of padding scheme and padded length T .For the primary use case of fixed-length genomic windows ( Lb = T for all batch elements), this reduces to the ordinary mean over the full sequence. 

Semantics-preserving alternative. A semantics-preserving alternative exists: build prefix sums on the centered residuals ¯fθ(t, c ) but reconstruct the original segment score at evaluation time via ( Ss+k,c −Ss,c )+ νb,c ·k. This retains the numerical benefit ( O(√T ) cumulative sum magnitude) without altering the semi-CRF distribution. We use the globally-centered variant instead for three reasons: (1) the adaptive prior provides empirically useful regularization in label-imbalanced genomic sequences; (2) the learned duration bias Bk,c , with K × C free parameters, can absorb the average effect of νb,c , so the prior primarily regularizes per-sample variation rather than imposing a fixed bias (parametric duration models with O(C) parameters cannot represent the linear-in-k correction and likely require the reconstruction variant instead); and (3) the reconstruction variant requires propagating νb,c into the Triton kernel as an additional argument, adding complexity for marginal benefit in the fixed-length-window regime where 

Lb = T .

## A.8 Implementation Correspondence 

Table A.2 maps mathematical notation to code variables for reference. 

# Appendix B Semi-Markov CRF Backend Algorithms 

This supplement summarizes the semi-CRF inference backends benchmarked in the main text, with complexity analysis and provenance for each. 

## B.1 Notation 

All notation follows the main text; see also the notation table in Appendix A. We additionally use 

n = ( K + 1) · C for the expanded state-space size and Wt for the transition matrix at position t in tree-structured backends. 

Important Distinction: Edge Tensor Materialization. 

All backends in this supplement assume the edge potential tensor ψ ∈ RB×(T −1) ×K×C×C is pre-materialized before inference. This O(T KC 2) tensor dominates memory for large T , regardless of which backend processes it. True T -independent memory–where edge potentials are computed on-the-fly from 

O(T C ) cumulative scores–is achieved only in the fused Triton kernel (Appendix A). 19 Math Code Variable Notes 

St,c cum scores[:, t, c] Built from centered emissions 

fθ (t, c ) scores / projected Raw encoder output 

νb,c center scores(scores, lengths) Masked mean over valid Lb

Tc′ ,c transition[c src, c dst] Source-first storage 

Bk,c duration bias[k-1, c] 0-indexed in code 

α ring buffer / alpha ring Size K

β beta ring Size 2 K

Pstart proj start Optional, ( B, T, C )

Pend proj end Optional, ( B, T, C )Ω ring checkpoints Saved at intervals 

N log norm checkpoints Cumulative log-norm ∆ checkpoint interval ≈ √T K CPAD C PAD next power of 2(C) 

τ TILE C Adaptive – grad tr workspace (B, N ckpt , K, C PAD , C PAD )– grad db workspace (B, N ckpt , K, C PAD )

Table A.2 : Mapping between mathematical notation and implemen-tation. Nckpt denotes number of checkpoint segments. 

## B.2 Semiring Abstraction 

All backends use the standard semiring dynamic programming framework [20]: substituting the ( ⊕, ⊗)operations yields the partition function (Log semiring), Viterbi path (Max semiring), k-best lists, sam-ples, or entropy from the same algorithm; see Rush [6], Table 2 for the full catalog of semirings and their gradient interpretations. For large state spaces, we provide checkpointing wrappers that trade 2 ×

compute for reduced memory [21]. 

## B.3 Provenance 

Backend Source 

Binary tree (dense) pytorch-struct [6] Binary tree (sharded) pytorch-struct [6] Linear scan (standard) pytorch-struct [6] Linear scan (vectorized) This work Streaming linear (PyTorch) This work Block-triangular This work Banded (analysis) This work Streaming (Triton kernel) This work 

Table B.1 : Algorithm provenance. The Triton kernel (bottom) is the key con-tribution: it computes edge potentials on-the-fly, achieving true T -independent memory and enabling genome-scale infer-ence. 

## B.4 Backend 1: Binary Tree (Parallel Scan) 

The binary tree algorithm is the default semi-Markov implementation in pytorch-struct [6]. It refor-mulates the forward recursion as semiring matrix products over an expanded state space of size 

n = ( K + 1) · C, where each state ( k, c ) represents “ k time steps remaining in a segment of label c”(see Rush [6] for the full transition matrix construction). The forward pass A1: T = W1 ⊗ · · · ⊗ WT is computed via binary tree reduction in O(log T ) parallel depth. 20 Complexity. 

Time: O(T (KC )3). Space: O(T (KC )2). The quadratic memory in KC causes OOM before the depth advantage is realized. A sharded variant [6] adds gradient checkpointing [21] to reduce memory to 

O(√T (KC )2) with 2 × compute overhead, extending the viable regime from n < 100 to n < 150. 

## B.5 Backend 2: Block-Triangular Sparsity (Experimental) 

At a tree node of span S, the duration constraint k1 + k2 ≤ S means only K(K+1) 2 duration pairs are feasible, yielding block-triangular sparsity. 

Representation. 

Store only blocks ( k1, k 2) satisfying k1 + k2 ≤ S, each block being dense C × C. This halves storage vs. dense at relevant spans. 

Sparse Multiplication. 

For block-triangular matrices C, D:

E[k1, k 3] = M

> k2:k1+k2≤S, k 2+k3≤S

C[k1, k 2] ⊗ D[k2, k 3] (B.1) 

Practical Outcome. 

In PyTorch/CUDA experiments, block-triangular storage did not yield speedups at typical sparsity levels. The overhead of indirect indexing and non-contiguous memory access outweighed savings from skipping ∼50% of blocks. GPUs prefer well-tuned dense kernels unless sparsity exceeds ∼90%. 

## B.6 Backend 3: Linear Scan 

The standard linear scan is the reference semi-Markov implementation in pytorch-struct [6]. It imple-ments the textbook semi-CRF forward recursion ˜ αt(c) = LSE k,c ′ [˜ αt−k(c′)+ ˜ψ(t, k, c, c ′)] sequentially over positions, avoiding the O(( KC )2) intermediate storage of tree backends. A vectorized variant replaces inner loops with batched tensor operations, yielding 2–3 × speedup. 

Complexity. 

Time: O(T KC 2). Space: O(T C ) for forward messages, or O(KC ) with ring buffer; in both cases the pre-materialized edge tensor adds O(T KC 2), which dominates. Depth: O(T ). 

## B.7 Backend 4: Streaming Linear Scan 

The streaming linear scan reduces forward message memory from O(T C ) to O(KC ) via a ring buffer (˜ αt(c) = α[t mod K, c ]). However, this PyTorch implementation still requires the pre-materialized edge tensor as input. 

Complexity. 

Time: O(T KC 2). Space: O(T KC 2) for edge tensor + O(KC ) for ring buffer. True T -independent memory requires computing edges on-the-fly via the prefix-sum decomposition (main text, Eq. (4)), achieved only in the fused Triton kernel (Appendix A). 

## B.8 Complexity Summary B.9 Duration Distributions 

The duration bias Bk,c can be learned as K × C free parameters or constrained to standard parametric forms (Geometric, Negative Binomial, Poisson, Uniform), reducing the parameter count to O(C) and encoding inductive bias about expected segment lengths. 21 Backend Work Memory Depth Notes Binary tree (dense) O(T n 3) O(T n 2) O(log T ) n < 100 Binary tree (sharded) O(T n 3) O(√T n 2) O(√T ) n < 150 Block-triangular O(T n 3) ∼ 12 O(T n 2) O(log T ) Not faster Banded O(T n 3) → O(T n 2) O(log T ) Not viable Linear scan O(T KC 2) O(T KC 2)∗ O(T ) General Streaming linear (PyTorch) O(T KC 2) O(T KC 2)∗ O(T ) Ring buffer for α

Streaming (Triton kernel) O(T KC 2) O(pT /K · KC )† O(T ) Genome-scale 

Table B.2 : Backend comparison. n = (K + 1) · C. ∗Dominated by pre-materialized edge tensor. †Working memory is O(KC ) (ring buffers); checkpoint memory grows sublinearly as O(pT /K · KC ). Edges computed on-the-fly; see Appendix A. 22 Appendix C Infeasibility of Banded Backends for Tree-Structured Semi-CRF Inference 

This appendix analyzes a natural optimization for exact semi-CRF [1] inference: using banded or block-sparse matrix kernels inside a binary-tree (parallel-scan) formulation of the dynamic program. The conclusion is negative: within this formulation, banded storage can reduce cost only at the smallest spans, but cannot prevent near-dense intermediate operators at the levels that dominate runtime and memory. 

Scope. The claims below apply to exact tree-structured implementations that represent node-to-node combination as semiring matrix products over an expanded state space of size Θ( KC ). This does not rule out approximate inference (e.g., pruning) or alternative exact formulations that do not materialize these operators. 

## C.1 Notation 

We consider a sequence of length T , equivalently boundary indices 0 , 1, . . . , T . The maximum segment length is K, and the label/state count is C.Two matrices are used as simplifying models: 

Boundary reachability (global boolean model). Define B ∈ { 0, 1}(T +1) ×(T +1) by 

B[i, j ] = 

(

1 if 1 ≤ j − i ≤ K

0 otherwise. (C.1) This captures the monotone-time constraint “a single segment advances forward by at most K bound-aries.” For instance, with T = 5 and K = 2, the matrix B has B[0 , 1] = B[0 , 2] = B[1 , 2] = B[1 , 3] = 

· · · = 1, but B[0 , 3] = 0 (since 3 − 0 = 3 > K ). 

Duration compatibility at a tree node (local pattern). At a tree node of span length S, partial states are indexed by a duration d ∈ { 1, . . . , D } where D = min( K, S ), and a label y ∈ { 1, . . . , C }. The quantity D caps the duration states at the actual feasible range for the current span: durations cannot exceed the span S, nor can they exceed the global maximum K. Feasible left/right duration pairs satisfy 

d1 + d2 ≤ S. For each feasible duration pair, label interactions are typically dense, yielding block-dense 

sparsity. 

Effective bandwidth. 

For a square matrix M whose indices have been flattened to {1, . . . , n }, define the (structural) bandwidth bw( M ) = max { | i − j| : M [i, j ] is structurally nonzero }. (C.2) For boolean matrices, “nonzero” means 1; for semiring score matrices, it means “not masked / not −∞ .” A banded representation with half-bandwidth b requires bw( M ) ≤ b (after any chosen permutation). 

## C.2 Local Incompatibility Pattern Is Triangular, Not Diagonal-Banded 

At an internal tree node of span S, a standard combine operation must account for left and right partial durations d1, d 2 that together fit inside the span: 

d1 + d2 ≤ S. (C.3) In the ( d1, d 2) plane, this feasible set is an anti-diagonal triangle. A diagonal band, by contrast, corre-sponds to |d1 − d2| ≤ b. These shapes overlap only partially, and the mismatch becomes pronounced as soon as S is a nontrivial fraction of K.To make “not narrow-banded under any ordering” precise, it suffices to lower bound the minimum achievable bandwidth.                    

> Proposition 1 (Bandwidth lower bound from a clique) Fix S, let D= min( K, S ), and define a binary matrix
> MS∈ { 0,1}(DC )×(DC )indexed by pairs (d, y )∈ { 1, . . . , D } × { 1, . . . , C }, with
> MS[( d1, y 1),(d2, y 2)] = 1 ⇐⇒ d1+d2≤S. (C.4)

23 For any simultaneous row/column permutation π,                                      

> bw
> 
> P⊤
> πMSPπ
> 
> ≥C
> jS
> 2
> k
> −1.(C.5)
> Proof Let m=⌊S/ 2⌋. For any d, d ′≤m,d+d′≤2m≤S, so all states in
> U={1, . . . , m } × { 1, . . . , C }(C.6) are mutually adjacent in the undirected graph induced by MS; hence Uis a clique of size |U|=mC . For any linear ordering of a clique on nvertices, the first and last vertices are adjacent, so the bandwidth is at least n−1. □

Consequence. When S is moderate (e.g., S ≈ K) and C is not tiny, the best achievable bandwidth is already a large fraction of the dense width DC . In the extreme case S ≥ 2K–since D = K when 

S ≥ K, the condition d1 + d2 ≤ S with d1, d 2 ≤ K is always satisfied when S ≥ 2K–the matrix MS is structurally dense. 

## C.3 Fill-In Under Composition in a Boolean Reachability Model 

Even if the local operator were genuinely banded, repeated composition widens the support.                                                            

> Lemma 2 (Boolean powers widen linearly) Let Bbe the boundary adjacency matrix defined above, and interpret multiplication over the boolean semiring (OR as addition, AND as multiplication). Then ( Bm)[ i, j ] = 1 if and only if there exists a path of exactly msegments from ito j. For m≥1, (Bm)[ i, j ] = 1 ⇐⇒ m≤j−i≤mK, (C.7) and therefore bw( Bm) = min( T, mK ).
> Proof A path of msegments advances by δ1+· · · +δmwith each δt∈ { 1, . . . , K }, hence m≤j−i≤mK .Conversely, if r=j−isatisfies m≤r≤mK , then rcan be written as a sum of mintegers in [1 , K ] (e.g., start from all ones and distribute the remaining r−munits without exceeding K). □

Implication for a balanced binary tree. 

In a balanced binary tree [22], combining two children corresponds (in this simplified reachability model) to composing reachability over a larger number of segments. The relevant parameter m therefore grows geometrically with tree level, so bw( Bm) increases rapidly and saturates at the dense width on the order of log 2(T /K ) levels. The reachability pattern becomes near-dense well before the root, so a banded container offers little benefit at the levels that dominate the total work. 

## C.4 Why Matrix Reordering Helps Only at Very Small Spans 

A reasonable countermeasure is to apply bandwidth-reducing permutations (e.g., Reverse Cuthill– McKee [23]; see also [24]). Proposition 1 already limits what is possible at a single node: cliques force large bandwidth regardless of ordering. At the global scale, once composition produces near-complete reachability, permutations cannot help substantially. One clean sufficient condition is “undirected completeness”: if for every unordered pair of indices {u, v } at least one of M [u, v ] or M [v, u ] is structurally nonzero, then the induced undirected graph is a clique and the bandwidth is n − 1 under any ordering. 

Empirical bandwidth ratios. 

The accompanying bandwidth report (available on github with the codebase) evaluates several orderings across ( K, C, S ). Summarizing bw best /(n − 1) by span fraction S/K : for small spans ( S ≤ K/ 2), best-case bandwidth ratios ranged from ≈ 0.23 to ≈ 0.68 (mean ≈ 0.44); for moderate spans ( K/ 2 < S ≤ K), ratios ranged from ≈ 0.78 to ≈ 0.99 (mean ≈ 0.90); for large spans ( S ≥ K), ratios were ≥ 0.97 and often exactly 1 .0. Thus, reordering can reduce bandwidth when spans are very small, but offers negligible benefit once spans approach K, the regime that dominates higher tree levels. 24 C.5 Practical Implications for Exact Semi-Markov Backends 

The computation and memory of tree-structured backends are dominated by higher tree levels, where local duration compatibility is near-dense (Proposition 1) and multi-step composition produces near-dense support (Lemma 2). Block-triangular formats that match the triangular geometry reduce storage by roughly half, but in our experiments, indirect indexing overhead negated this at typical sparsity levels, consistent with the well-known GPU performance pattern that moderate block sparsity underperforms dense kernels unless sparsity exceeds ∼90%. If sparsity is essential, it must be introduced by approximation (e.g., segment filtering [12] or hybrid architectures [4, 5]), which enforce value sparsity rather than relying on structural sparsity that does not persist under composition. 

# Appendix D Linear and Near-Linear CRF Implementations 

This supplement documents the specialized K = 1 (linear CRF) and K = 2 (near-linear CRF) implemen-tations in flash-semicrf . These optimized paths eliminate ring buffer overhead for common sequence labeling tasks while maintaining compatibility with the streaming Semi-CRF interface. 

## D.1 Overview 

The streaming Semi-CRF module automatically dispatches to specialized implementations based on the maximum segment duration K:            

> KImplementation Rationale
> K= 1 LinearCRFStreaming Standard linear-chain CRF; no duration loop
> K= 2 SemiCRFK2Streaming Explicit 2-step history; avoids ring buffer edge cases
> K≥3SemiCRFStreamingTriton Full ring buffer architecture

Table D.1 : Automatic dispatch by maximum segment duration. When full boundary projections ( Pstart , Pend ) are active, K = 1 and K = 2 dispatch falls through to the generic K ≥ 3 streaming path. Scalar boundaries ( πstart ,

πend ) are folded into S and do not affect dispatch. 

Triton Kernel Scope. 

The specialized K = 1 and K = 2 paths are implemented only in PyTorch. The Triton streaming kernel requires K ≥ 3 for correct ring buffer operation. This is intentional: the PyTorch implementations are already efficient for small K, and the Triton kernel’s complexity is justified only when ring buffers and checkpointing provide substantial memory savings. 

## D.2 Notation 

Symbol Description Shape 

T Sequence length scalar 

C Number of labels (states) scalar 

B Batch size scalar 

St,c Cumulative projected scores (B, T + 1 , C )

Tc′,c Transition scores (source c′ to dest c) (C, C )

Bk,c Duration bias for duration k, label c (K, C )˜αt(c) Log-forward message at position t, label c (B, C )˜βt(c) Log-backward message (B, C )

πstart  

> c

Learnable scalar start boundary (C, )

πend  

> c

Learnable scalar end boundary (C, )

Pstart  

> t,c

Full start boundary projection (B, T, C )

Pend  

> t,c

Full end boundary projection (B, T, C )25 Table D.2 : Notation. Tilde (˜ ·) denotes log-domain quantities. Durations are 1-indexed in the equations: 

k ∈ { 1, . . . , K }, stored at 0-indexed positions k−1 in the implementation. 

## D.3 Score Centering 

Score centering follows the main text (Section 3.2): the per-sequence baseline νb,c is subtracted from raw projected scores before constructing the cumulative sum S, controlling cumulative magnitude from O(T )to O(√T ). For variable-length batches, the baseline is computed as a masked mean over the Lb valid positions (see Appendix A for padding semantics). 

Invariance of emissions. 

Centering does not change the per-position emission scores: St,c − S t−1,c = fθ(t−1, c ) − νb,c , and the constant νb,c cancels in the forward recurrence (it shifts all labels equally at each timestep). However, centering keeps the absolute magnitude of S bounded, which matters for content scores spanning many positions when K > 1. 

## D.4 Sequence Boundary Handling 

flash-semicrf supports two boundary mechanisms that control how sequence start and end positions are scored. These interact differently with the K = 1 and K = 2 fast paths. 

D.4.1 Scalar Boundaries (Zero-Overhead Folding) 

When use sequence boundaries=True , learnable vectors πstart ∈ RC and πend ∈ RC are activated (initialized to zero, equivalent to pytorch-crf ’s start transitions /end transitions ). These add label-dependent scores at sequence boundaries: πstart  

> c

rewards (or penalizes) label c at the start of a sequence, and πend  

> c

at the end. Rather than threading boundary parameters through the forward and backward kernels, scalar boundaries are folded directly into the cumulative scores via prefix-sum arithmetic: 

Sb, 0,c ← S b, 0,c − πstart  

> c

(D.1) 

Sb,L b,c ← S b,L b,c + πend  

> c

(D.2) 

Why this works. 

The content score for a segment [ s, t ) is St,c − S s,c . By subtracting πstart from position 0: 

• Any segment starting at s = 0 gets content score ( St,c − S 0,c ) + πstart  

> c

.

• Segments not starting at 0 are unaffected (their Ss,c and St,c both remain unchanged). Similarly, adding πend to position Lb adds it to any segment ending at t = Lb.

Consequence for dispatch. 

Because the folding modifies S before the forward/backward functions are called, the K = 1 and K = 2 fast paths remain active. The LinearCRFStreaming and SemiCRFK2Streaming autograd functions are boundary-agnostic–they operate on the already-modified S with zero additional overhead. 

Gradient flow. 

Gradients with respect to πstart and πend flow automatically through the modified cumulative scores via standard autograd: 

∇πstart  

> c

L = −∇ Sb, 0,c L (D.3) 

∇πend  

> c

L = ∇Sb,L b,c L (D.4) 

D.4.2 Full Boundary Projections (Fallthrough to Generic Path) 

When use boundary projections=True , position-dependent boundary scores Pstart  

> t,c

and Pend  

> t,c

are com-puted by linear layers applied to the encoder hidden states. These provide richer expressiveness than scalar boundaries, allowing the model to learn position-specific start/end preferences. 26 When full boundary projections are active, the K = 1 and K = 2 fast paths are skipped . Dispatch falls through to the generic K ≥ 3 streaming path, which handles boundaries via the HAS BOUNDARIES 

flag in the Triton kernel (or the PyTorch reference equivalent). 

Composition with scalar boundaries. 

If both use sequence boundaries and use boundary projections are enabled, the scalar vectors are added to the projection tensors at the boundary positions: 

Pstart  

> b, 0,c

← P start  

> b, 0,c

+ πstart  

> c

(D.5) 

Pend  

> b,L b−1,c

← P end  

> b,L b−1,c

+ πend  

> c

(D.6) In this case the boundary projections trigger the generic path regardless, so the scalars compose additively within that path rather than folding into S.

Rationale for fallthrough. 

The full boundary projections require per-position boundary scores to be threaded through the forward recurrence. The generic kernel already supports this via HAS BOUNDARIES . Duplicating that logic in the specialized K = 1/ K = 2 PyTorch paths would add complexity for a niche use case. 

## D.5 K=1: Linear CRF 

When K = 1, every segment has duration 1, reducing the Semi-CRF to a standard linear-chain CRF. The forward recurrence simplifies to: ˜αt(c) = emit t(c) + log 

> C

X

> c′=1

exp  ˜αt−1(c′) + Tc′,c 

 (D.7) where the emission score is computed via prefix-sum difference: emit t(c) = St,c − S t−1,c + B1,c (D.8) When scalar boundaries are active, the boundary scores are already embedded in S (Section D.4.1), so the recurrence above implicitly includes πstart  

> c

at t = 1 and πend  

> c

at t = Lb without any modification to the algorithm. 

D.5.1 Initialization Convention 

Following the pytorch-crf convention, we use uniform initialization :˜α0(c) = 0 ∀c ∈ { 1, . . . , C } (D.9) This is equivalent to assuming a uniform distribution over initial states. At t = 1: ˜α1(c) = emit 1(c) + log 

> C

X

> c′=1

exp( Tc′,c ) (D.10) 

Interaction with Sequence Boundaries. 

When use sequence boundaries=True , learnable parameters πstart  

> c

and πend  

> c

are activated (initialized to zero). These are not applied by modifying ˜ α0–instead they are folded into S via the prefix-sum trick (Section D.4.1). The forward algorithm still sees ˜ α0(c) = 0; the boundary effect appears in the emission scores. At initialization ( πstart = 0), behavior is identical to the boundary-free case. After training, πstart 

and πend provide independent expressiveness beyond the transition matrix. 

D.5.2 Algorithm 

Algorithm D.1 presents the optimized K = 1 forward pass. 27 Algorithm D.1: Linear CRF Forward ( K = 1) 

Input: Cumulative scores S ∈ RB×(T +1) ×C , Transitions T ∈ RC×C , Duration bias B ∈ R1×C

(optional), Lengths ℓ ∈ ZB

Output: Log partition log Z ∈ RB 

> 1

˜α ← 0 ∈ RB×C ; // Uniform initialization  

> 2

for t ← 1 to T do  

> 3

et ← S [: , t, :] − S [: , t − 1, :] + B1 ; // Emission // Standard linear CRF recurrence  

> 4

˜αnew ← LogSumExp c′ ( ˜ α[: , c ′] + Tc′,:) + et;

// Update only active sequences  

> 5

˜α ← where (t ≤ ℓ, ˜αnew , ˜α);  

> 6

log Z ← LogSumExp c( ˜ α[ℓ]) ; // Final reduction  

> 7

return log Z;

Note. 

When scalar boundaries are active, S has already been modified per Equations (D.1)–(D.2). The algorithm is unchanged; the boundary scores appear implicitly in the emission differences. 

D.5.3 Complexity 

• Time : O(T C 2) – matrix-vector products at each timestep 

• Space : O(BC ) – single α vector per batch element 

• No checkpointing : Full α history stored for backward pass ( O(BT C )) The space overhead for storing full α history is acceptable because K = 1 implies short-to-moderate sequences where the O(BT C ) cost is manageable. 

## D.6 K=2: Near-Linear CRF 

When K = 2, segments can have duration 1 or 2. Rather than using the general ring buffer (which has edge cases at K = 2 due to modular arithmetic with small indices), we use explicit 2-step history variables. 

D.6.1 Forward Recurrence 

At each position t, we combine contributions from both durations: ˜αt(c) = LogSumExp 



score k=1 (t, c )

| {z }

> duration 1

, score k=2 (t, c )

| {z }

> duration 2



(D.11) where: score k=1 (t, c ) = emit t−1: t(c) + log X

> c′

exp(˜ αt−1(c′) + Tc′,c ) (D.12) score k=2 (t, c ) = emit t−2: t(c) + log X

> c′

exp(˜ αt−2(c′) + Tc′,c ) (D.13) The emission scores are: emit t−1: t(c) = St,c − S t−1,c + B1,c (D.14) emit t−2: t(c) = St,c − S t−2,c + B2,c (D.15) 

D.6.2 Algorithm 

Algorithm D.2 presents the optimized K = 2 forward pass using explicit history variables. 28 Algorithm D.2: Semi-CRF Forward ( K = 2) 

Input: Cumulative scores S, Transitions T , Duration bias B ∈ R2×C , Lengths ℓ

Output: Log partition log Z ∈ RB 

> 1

˜α(1) ← 0 ∈ RB×C ; // α[t − 1]  

> 2

˜α(2) ← −∞ ∈ RB×C ; // α[t − 2] (invalid at t = 1 ) 

> 3

for t ← 1 to T do 

// Duration k = 1 : segment from t − 1 to t 

> 4

ek=1 ← S [: , t, :] − S [: , t − 1, :] + B1; 

> 5

sk=1 ← LogSumExp c′ ( ˜ α(1) [: , c ′] + Tc′,:) + ek=1 ; 

> 6

if t ≥ 2 then 

// Duration k = 2 : segment from t − 2 to t 

> 7

ek=2 ← S [: , t, :] − S [: , t − 2, :] + B2; 

> 8

sk=2 ← LogSumExp c′ ( ˜ α(2) [: , c ′] + Tc′,:) + ek=2 ; 

> 9

˜αnew ← LogSumExp( sk=1 , sk=2 );  

> 10

else  

> 11

˜αnew ← sk=1 ;

// Shift history  

> 12

˜α(2) ← ˜α(1) ; 

> 13

˜α(1) ← where (t ≤ ℓ, ˜αnew , ˜α(1) );  

> 14

log Z ← LogSumExp c( ˜ α(1) [ℓ]);  

> 15

return log Z;

D.6.3 Complexity 

• Time : O(T C 2) – two matrix-vector products per timestep 

• Space : O(BC ) – two α vectors per batch element 

• No ring buffer : Explicit variables avoid modular arithmetic overhead 

## D.7 Backward Pass and Gradients 

Both K = 1 and K = 2 implementations use the standard forward-backward algorithm for gradient computation. Because these paths do not use checkpointing, the full α history is stored during the forward pass. 

Boundary transparency. 

When scalar boundaries are active, the backward pass requires no special handling because πstart and 

πend have already been folded into S (Section D.4.1). Gradients with respect to πstart  

> c

and πend  

> c

flow automatically through the modified cumulative scores via standard autograd. 

D.7.1 Marginal Computation 

The marginal probability for a segment spanning [ t − k, t − 1] with transition c′ → c is: 

μ(t, k, c, c ′) = exp  ˜αt−k(c′) + ˜ψ(t, k, c, c ′) + ˜βt(c)

exp(log Z) (D.16) where the edge potential is: ˜ψ(t, k, c, c ′) = ( St,c − S t−k,c ) + Bk,c + Tc′,c (D.17) 

Numerical safeguard. 

In the PyTorch reference implementation, the log marginal ˜ αt−k(c′) + ˜ψ(t, k, c, c ′) + ˜βt(c) − log Z is clamped to [ −80 , 80] before exponentiation. This prevents overflow/underflow when individual terms are 29 large in absolute value but nearly cancel. The clamped range corresponds to exp( ±80) ≈ 5.5 × 10 34 , well within float64 range. 

D.7.2 Gradient Formulas 

Cumulative Scores. 

The gradient for St,c accumulates contributions from segments ending at t (positive) and starting after 

t (negative): 

∇S t,c = X

> k,c ′

μ(t, k, c, c ′) − X

> k,c ′

μ(t + k, k, c, c ′) (D.18) 

Transitions. 

The gradient sums marginals over all positions and durations: 

∇T c′,c = X

> b

∂L

∂Z b

· X

> t,k

μb(t, k, c, c ′) (D.19) 

Duration Bias. 

The gradient for duration k sums over all segments of that duration: 

∇B k,c = X

> b

∂L

∂Z b

· X

> t,c ′

μb(t, k, c, c ′) (D.20) 

## D.8 Viterbi Decoding 

Both K = 1 and K = 2 implementations support Viterbi decoding for MAP inference. The algo-rithms follow the same structure as the forward pass, replacing LogSumExp with max and maintaining backpointers. 

D.8.1 K=1 Viterbi 

Standard linear CRF Viterbi with backpointers for the best previous label at each position. 

D.8.2 K=2 Viterbi 

Extended backpointers track both the best previous label and the best duration (1 or 2) at each position. The traceback reconstructs the segmentation by stepping back by the recorded duration. 

## D.9 Why Triton Kernels Require K ≥ 3

The Triton streaming kernel is designed for the general Semi-CRF case where ring buffers and checkpointing provide substantial memory savings. For small K, ring buffer indexing creates edge cases: 

• K = 1: All positions map to ring index 0. The ring buffer degenerates to a single slot, providing no benefit over a simple variable. 

• K = 2: Indices alternate between 0 and 1. Wraparound logic is fragile and provides minimal memory savings over explicit variables. 

• K ≥ 3: Ring buffer provides meaningful history separation. Checkpointing amortizes over multiple segments, justifying the implementation complexity. The PyTorch implementations for K = 1 and K = 2 are already efficient (no kernel launch overhead, simple control flow), making Triton acceleration unnecessary. 

## D.10 Implementation Summary 

Automatic Dispatch. 

The semi crf streaming forward function automatically routes to the appropriate implementation based on K. Users need not manage dispatch manually. 30 K Time Space Checkpointing Ring Buffer Backend 1 O(T C 2) O(BC ) No No PyTorch 2 O(T C 2) O(BC ) No No PyTorch       

> ≥3O(T KC 2)O(BKC )Yes Yes Triton (GPU) / PyTorch (CPU)

Table D.3 : Implementation characteristics by maximum segment duration. 

## D.11 Functional Equivalence 

All three implementations ( K = 1, K = 2, K ≥ 3) compute the same partition function and gradients for their respective segment duration constraints. Specifically: 

• The K = 1 path produces identical results to the general streaming kernel with K = 1 (verified numerically). 

• The K = 2 path produces identical results to the general streaming kernel with K = 2 (verified numerically). 

• All paths support variable-length batches with proper masking. 

• The equivalence between K = 1/ K = 2 fast paths and the generic kernel holds only when full boundary projections are not active . When Pstart or Pend are provided, dispatch bypasses the spe-cialized paths entirely (Section D.4.2), so the question of equivalence does not arise. Scalar boundaries (πstart , πend ) are folded into S before dispatch and do not affect equivalence. The specialized paths exist purely for performance optimization, not behavioral differences. 

# References 

[1] Sarawagi, S., Cohen, W.W.: Semi-Markov conditional random fields for information extraction. In: Advances in Neural Information Processing Systems, vol. 17 (2004) [2] Lample, G., Ballesteros, M., Subramanian, S., Kawakami, K., Dyer, C.: Neural architectures for named entity recognition. In: Proc. NAACL-HLT, pp. 260–270 (2016). https://doi.org/10.18653/ v1/N16-1030 [3] Ma, X., Hovy, E.: End-to-end sequence labeling via bi-directional LSTM-CNNs-CRF. In: Proc. ACL, pp. 1064–1074 (2016). https://doi.org/10.18653/v1/P16-1101 [4] Kong, L., Dyer, C., Smith, N.A.: Segmental recurrent neural networks. In: Proc. ICLR (2016) [5] Ye, Z., Ling, Z.-H.: Hybrid semi-Markov CRF for neural sequence labeling. In: Proc. ACL, pp. 235–240 (2018). https://doi.org/10.18653/v1/P18-2038 [6] Rush, A.: Torch-struct: Deep structured prediction library. In: Proc. ACL: System Demonstrations, pp. 335–342 (2020). https://doi.org/10.18653/v1/2020.acl-demos.38 [7] Oxford Nanopore Technologies: Bonito: A PyTorch Basecaller for Oxford Nanopore Reads. https: //github.com/nanoporetech/bonito [8] Oxford Nanopore Technologies: Dorado: Oxford Nanopore’s Basecaller. https://github.com/ nanoporetech/dorado [9] Semwal, A., Morrison, J., Beddows, I., Palmer, T., Majewski, M.F., Jang, H.J., Johnson, B.K., Shen, H.: Tranquillyzer: A flexible neural network framework for structural annotation and demultiplexing of long-read transcriptomes. bioRxiv (2025) https://doi.org/10.1101/2025.07.25.666829 [10] DeCaprio, D., Vinson, J.P., Pearson, M.D., Montgomery, P., Doherty, M., Galagan, J.E.: Conrad: Gene prediction using conditional random fields. Genome Research 17 (9), 1389–1398 (2007) https: //doi.org/10.1101/gr.6558107 [11] Bernal, A., Crammer, K., Hatzigeorgiou, A., Pereira, F.: Global discriminative learning for higher-accuracy computational gene prediction. PLoS Computational Biology 3(3), 54 (2007) https://doi. 31 org/10.1371/journal.pcbi.0030054 [12] Zaratiana, U., Holat, P., Tomeh, N., Charnois, T.: Filtered semi-Markov CRF. In: Findings of EMNLP, pp. 13553–13564 (2023) [13] Dao, T., Fu, D., Ermon, S., Rudra, A., R´ e, C.: FlashAttention: Fast and memory-efficient exact attention with IO-awareness. In: Advances in Neural Information Processing Systems, vol. 35 (2022) [14] Gu, A., Dao, T.: Mamba: Linear-time sequence modeling with selective state spaces. In: Proc. ICML (2024). Preprint arXiv:2312.00752, 2023 [15] Efron, B.: Large-Scale Inference: Empirical Bayes Methods for Estimation, Testing, and Prediction. Cambridge University Press, Cambridge (2010) [16] Garofolo, J.S., Lamel, L.F., Fisher, W.M., Fiscus, J.G., Pallett, D.S., Dahlgren, N.L., Zue, V.: DARPA TIMIT Acoustic-Phonetic Continuous Speech Corpus CD-ROM. National Institute of Standards and Technology (NIST), Gaithersburg, MD (1993) [17] Lee, K.-F., Hon, H.-W.: Speaker-independent phone recognition using hidden Markov models. IEEE Transactions on Acoustics, Speech, and Signal Processing 37 (11), 1641–1648 (1989) [18] Salvi, G.: Segment boundary detection via class entropy measurements in connectionist phoneme recognition. Speech Communication 48 (12), 1666–1676 (2006) https://doi.org/10.1016/j.specom. 2006.07.009 . arXiv:2401.05717 [19] Shannon, C.E.: A mathematical theory of communication. Bell System Technical Journal 27 (3), 379–423 (1948) https://doi.org/10.1002/j.1538-7305.1948.tb01338.x [20] Goodman, J.: Semiring parsing. Computational Linguistics 25 (4), 573–605 (1999) [21] Chen, T., Xu, B., Zhang, C., Guestrin, C.: Training deep nets with sublinear memory cost. In: arXiv:1604.06174 (2016) [22] Blelloch, G.E.: Prefix sums and their applications. Technical Report CMU-CS-90-190, Carnegie Mellon University (1990) [23] Cuthill, E., McKee, J.: Reducing the bandwidth of sparse symmetric matrices. In: Proceedings of the 24th National Conference of the ACM, pp. 157–172 (1969) [24] George, A., Liu, J.W.H.: Computer Solution of Large Sparse Positive Definite Systems. Prentice-Hall, Englewood Cliffs, NJ (1981) 32