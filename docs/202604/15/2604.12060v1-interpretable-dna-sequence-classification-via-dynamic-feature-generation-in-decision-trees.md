---
title: Interpretable DNA Sequence Classification via Dynamic Feature Generation in Decision Trees
title_zh: 通过决策树中的动态特征生成实现可解释的 DNA 序列分类
authors: "Nicolas Huynh, Krzysztof Kacprzyk, Ryan Sheridan, David Bentley, Mihaela van der Schaar"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.12060v1"
tags: ["query:q7"]
score: 9.0
evidence: 用于DNA序列分类的AI和决策树
tldr: 针对DNA序列分类中深度学习模型缺乏解释性以及传统决策树表达能力不足的问题，本文提出了DEFT框架。该框架在决策树构建过程中，利用大语言模型（LLM）根据局部序列分布动态生成并迭代优化具有生物学意义的高层特征。实验证明，DEFT在多种基因组任务中不仅保持了高预测性能，还发现了具有高度可解释性的序列特征，实现了模型性能与透明度的平衡。
selection_source: fresh_fetch
motivation: 传统决策树在处理DNA序列时因仅考虑孤立的原始特征而导致树结构过深，难以兼顾解释性与泛化性能。
method: 提出DEFT框架，利用大语言模型在决策树节点处动态生成生物学特征，并通过反思机制进行迭代优化。
result: 在多项基因组任务中，DEFT成功发现了人类可理解且具有强预测能力的序列特征。
conclusion: DEFT通过动态特征生成有效提升了决策树在复杂生物序列分析中的表现，为可解释的基因组学研究提供了新途径。
---

## 摘要
DNA 序列分析在从进化生物学到理解基因调节和疾病机制的众多领域中已变得至关重要。虽然深度神经网络可以实现卓越的预测性能，但它们通常作为黑盒运行。与这些黑盒相比，轴对齐决策树为可解释的 DNA 序列分析提供了一个充满前景的方向，但它们面临一个根本性的局限：在每个分裂点孤立地考虑单个原始特征限制了其表达能力，导致树深度过大，从而损害了可解释性和泛化性能。我们通过引入 DEFT 来应对这一挑战，这是一个在树构建过程中自适应生成高级序列特征的新型框架。DEFT 利用大语言模型，针对每个节点的局部序列分布提出具有生物学意义的特征，并通过反思机制对其进行迭代优化。实验表明，DEFT 在各种基因组任务中发现了具有人类可解释性且高度预测性的序列特征。

## Abstract
The analysis of DNA sequences has become critical in numerous fields, from evolutionary biology to understanding gene regulation and disease mechanisms. While deep neural networks can achieve remarkable predictive performance, they typically operate as black boxes. Contrasting these black boxes, axis-aligned decision trees offer a promising direction for interpretable DNA sequence analysis, yet they suffer from a fundamental limitation: considering individual raw features in isolation at each split limits their expressivity, which results in prohibitive tree depths that hinder both interpretability and generalization performance. We address this challenge by introducing DEFT, a novel framework that adaptively generates high-level sequence features during tree construction. DEFT leverages large language models to propose biologically-informed features tailored to the local sequence distributions at each node and to iteratively refine them with a reflection mechanism. Empirically, we demonstrate that DEFT discovers human-interpretable and highly predictive sequence features across a diverse range of genomic tasks.

---

## 论文详细总结（自动生成）

以下是对论文《Interpretable DNA Sequence Classification via Dynamic Feature Generation in Decision Trees》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：DNA序列分析在生物学和医学中至关重要，但现有的深度学习模型（如CNN、Transformer）通常是“黑盒”，缺乏透明度，难以验证其预测是否符合生物学原理。
*   **核心问题**：传统的决策树（如CART）虽然具有可解释性，但在处理DNA序列时存在根本局限：它们在每个分裂点只能考虑单个核苷酸位置（轴对齐分裂）。为了捕捉复杂的序列模式，树必须长得非常深，这不仅破坏了可解释性，还导致泛化性能差。
*   **整体含义**：本文旨在设计一种既具有决策树的透明性，又具备深度学习表达能力的模型，通过在树构建过程中动态生成高级生物特征来解决上述矛盾。

### 2. 方法论：核心思想与关键技术
DEFT（Dynamic Engineering of Features in Trees）框架的核心思想是将**大语言模型（LLM）**作为自适应特征生成器，嵌入到决策树的归纳过程中。
*   **自适应特征生成**：在决策树的每个内部节点，DEFT不只是搜索原始特征，而是利用LLM根据该节点的局部数据分布、任务元数据和从根到该节点的路径上下文，生成新的高级特征。
*   **两步生成流程**：
    1.  **语义表示**：LLM首先生成特征的“理由”（Rationale）、“名称”和“描述”。
    2.  **可执行代码**：LLM将语义描述转化为具体的Python代码，用于计算该特征值。
*   **迭代优化（反思机制）**：采用受进化算法启发的优化方案。LLM会根据当前候选特征的判别得分（如基尼指数）进行“自我反思”，通过“探索”（提出新想法）和“利用”（改进高分特征）来迭代精炼特征，直到找到最优分裂特征。
*   **推理过程**：推理时，新样本沿着树向下遍历，在每个节点运行生成的Python代码计算特征值，并与学习到的阈值进行比较。

### 3. 实验设计
*   **数据集**：
    1.  **Pol II Pausing**：预测RNA聚合酶II在DNA序列中的停顿。
    2.  **Promoters**：识别非TATA框的人类启动子区域。
    3.  **Enhancers**：分类增强子活性。
*   **对比方法（Baselines）**：
    *   **可解释模型**：标准CART（原始序列）、OC1（斜决策树）、基于2-mer特征的CART、逻辑回归。
    *   **黑盒模型**：CNN、Transformer、XGBoost（2-mer）、Nucleotide Transformer（预训练模型）。
*   **评估指标**：准确率（Accuracy）、AUPRC、F1分数、精确率、召回率。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **18核 Intel Core i9-10980XE CPU** 和 **NVIDIA GeForce RTX 3080 GPU** 的机器上运行。
*   **计算开销**：由于涉及LLM推理，DEFT的计算成本高于传统树模型。文中提到在Pol II任务中，每个分裂点的平均耗时约为 **353秒**。
*   **LLM模型**：主要使用 **GPT-4o (version 1001)**，同时也测试了开源模型（gpt-oss）。

### 5. 实验数量与充分性
*   **实验规模**：在3个不同的基因组任务上进行了测试，每个实验运行 **5个随机种子** 并报告了95%置信区间。
*   **消融实验**：非常充分。作者分别验证了“先验知识”、“自适应性”和“反思机制”对性能的影响。
*   **深度分析**：探讨了树深度（1到6层）对性能的影响，并进行了专家评估（由生物学家对生成的特征进行评分），确保了实验的客观性和生物学相关性。

### 6. 主要结论与发现
*   **性能卓越**：DEFT在所有任务中均显著优于传统的树基模型，并缩小了与黑盒深度学习模型之间的性能差距。
*   **特征发现**：DEFT能够发现多种类型的生物特征，包括位置检查、组成窗口、基序（Motifs）、布局/间距以及物理/表观遗传特征（如堆叠能量）。
*   **可控平衡**：通过调整提示词中的约束，DEFT可以在“极致性能”和“极致可解释性”之间进行权衡。
*   **泛化能力**：相比于在原始特征上过度拟合的深层CART，DEFT通过浅层树和高级特征实现了更好的测试集泛化。

### 7. 优点
*   **原生透明**：模型本身就是决策树，每个分裂逻辑都有人类可读的理由和代码，满足可模拟性和可分解性。
*   **自适应性强**：特征生成是“局部感知”的，能根据树的不同分支动态调整。
*   **生物学对齐**：利用LLM的预训练知识，生成的特征往往具有明确的生物学意义，有助于科学发现。

### 8. 不足与局限
*   **计算成本高**：每个节点的LLM调用和代码执行显著增加了训练时间，限制了其在大规模数据集或极深树构建上的应用。
*   **随机性风险**：LLM生成的代码可能存在随机性，虽然通过反思机制有所缓解，但仍可能导致不同运行之间的树结构差异。
*   **代码安全性**：自动执行LLM生成的Python代码在生产环境中可能存在安全隐患（尽管在受控的科研环境中风险较小）。
*   **对LLM的依赖**：模型性能高度依赖于底层LLM的推理能力和生物学知识储备。

（完）
