---
title: annbatch unlocks terabyte-scale training of biological data in anndata
title_zh: annbatch 实现了 anndata 中太字节级生物数据的训练
authors: "Ilan Gold, Felix Fischer, Lucas Arnoldt, F. Alexander Wolf, Fabian J. Theis"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01949v2"
tags: ["query:q7"]
score: 8.0
evidence: 全基因组测序数据的AI训练
tldr: 针对生物数据集规模超出内存限制导致的训练瓶颈，本文推出了 annbatch，一个原生于 anndata 的微批次加载器。它支持直接在磁盘备份的数据集上进行核外训练，在单细胞、显微镜和基因组学等任务中将加载吞吐量提升了高达一个数量级，显著缩短了训练时间，同时保持了与 scverse 生态系统的完全兼容，为大规模生物 AI 提供了高效的基础设施。
selection_source: fresh_fetch
motivation: 生物数据集规模的急剧增长使得数据访问而非模型计算成为机器学习训练的主要瓶颈。
method: 开发了原生支持 anndata 格式的 annbatch 加载器，实现了直接基于磁盘存储数据的核外微批次训练。
result: 在多项生物学基准测试中，annbatch 将加载吞吐量提升了 10 倍，使训练时间从数天缩短至数小时。
conclusion: annbatch 为大规模生物 AI 提供了实用的数据加载方案，在不放弃标准生物数据格式的前提下实现了 TB 级数据的训练。
---

## 摘要
生物数据集的规模目前通常已超过系统内存，使得数据访问而非模型计算成为训练机器学习模型的主要瓶颈。这一瓶颈在生物学领域尤为突出，因为广泛使用的社区数据格式必须支持异构元数据、稀疏和密集测定，以及在已建立的计算生态系统内进行下游分析。在此，我们介绍了 annbatch，这是一个原生于 anndata 的小批量加载器，能够直接在基于磁盘的数据集上进行核外（out-of-core）训练。在单细胞转录组学、显微镜成像和全基因组测序的基准测试中，annbatch 将加载吞吐量提高了一个数量级，并将训练时间从数天缩短至数小时，同时保持与 scverse 生态系统的完全兼容。Annbatch 为可扩展的生物人工智能建立了实用的数据加载基础设施，使得在不放弃标准生物数据格式的情况下，能够利用日益增长且多样化的数据集。Github: https://github.com/scverse/annbatch

## Abstract
The scale of biological datasets now routinely exceeds system memory, making data access rather than model computation the primary bottleneck in training machine-learning models. This bottleneck is particularly acute in biology, where widely used community data formats must support heterogeneous metadata, sparse and dense assays, and downstream analysis within established computational ecosystems. Here we present annbatch, a mini-batch loader native to anndata that enables out-of-core training directly on disk-backed datasets. Across single-cell transcriptomics, microscopy and whole-genome sequencing benchmarks, annbatch increases loading throughput by up to an order of magnitude and shortens training from days to hours, while remaining fully compatible with the scverse ecosystem. Annbatch establishes a practical data-loading infrastructure for scalable biological AI, allowing increasingly large and diverse datasets to be used without abandoning standard biological data formats. Github: https://github.com/scverse/annbatch

---

## 论文详细总结（自动生成）

以下是对论文《annbatch: Unlocks terabyte-scale training of biological data in anndata》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：随着生物数据集（如单细胞转录组、显微成像、全基因组测序）规模达到太字节（TB）级别，系统内存（RAM）已无法容纳全部数据。
*   **核心瓶颈**：在训练深度学习模型时，瓶颈已从“模型计算”转移到了“数据加载（I/O）”。
*   **现有痛点**：现有的高性能加载框架（如 NVIDIA Merlin, WebDataset）通常需要转换文件格式，这破坏了生物学界通用的 `anndata` 格式及其 `scverse` 生态系统的兼容性。此外，现有的单细胞加载器（如 scDataset）在处理大规模数据时无法达到饱和 GPU 吞吐量的速度。

### 2. 论文提出的方法论
`annbatch` 是一个原生支持 `anndata` 的端到端高性能微批次加载框架，其核心思想是通过**预洗牌（Pre-shuffling）**将随机访问转化为伪随机的**连续块访问**。
*   **预洗牌器（Preshuffler）**：
    *   采用延迟加载（Lazy loading）方式读取磁盘上的多个 `anndata` 文件。
    *   将数据划分为用户定义的连续块，在内存中进行洗牌，然后以 Zarr 格式重新写回磁盘。
    *   这保证了在训练时可以通过读取大块连续磁盘区域来获得极高的 I/O 速度，同时不牺牲批次间的随机性。
*   **高性能加载器（Data Loader）**：
    *   基于 Rust 开发的 `zarrs-python` 桥接库，实现极速 Zarr 读取。
    *   **关键技术**：自定义非均匀切片索引、Linux Direct I/O（绕过内核缓存）、读取至固定内存（Pinned Memory）以及 GPU 加速洗牌。
    *   **框架无关性**：可无缝对接 PyTorch 等主流深度学习框架。

### 3. 实验设计
*   **数据集与场景**：
    1.  **单细胞转录组**：Tahoe-100M 数据集（约 1 亿个细胞）。
    2.  **单细胞显微成像**：scPortrait 提供的裁剪图像（128x128x5 维度）。
    3.  **全基因组测序 (WGS)**：1000 Genomes GRCh38 数据集（扩展至 50 万样本），涵盖稀疏（罕见变异）和密集（常见变异）两种模式。
*   **对比方法（Benchmark）**：
    *   `scDataset`
    *   `MappedCollection` (Lamin Labs)
    *   `numpy memmap`（针对密集基因组数据的基准）
    *   对比了 `h5ad` (HDF5) 与 `Zarr` 存储格式的性能差异。

### 4. 资源与算力
*   **GPU 型号**：主要在 AWS `ml.g4dn.4xlarge`（配备 T4 GPU）和 NVIDIA H100 上进行测试。
*   **硬件环境**：包括 AWS 云环境、de.NBI（德国生物信息学基础设施）高性能计算节点、以及 LRZ AI 分区。
*   **训练时长示例**：在 Tahoe-100M 数据集上，`annbatch` 跑完一个 epoch 仅需约 **17 分钟**，而 `scDataset` 需 7.5 小时，`MappedCollection` 需 21 小时。
*   **预处理开销**：预洗牌过程约需 5.5 小时（非 GPU 任务），但在第一个训练 epoch 内即可通过节省的时间完成成本摊销。

### 5. 实验数量与充分性
*   **实验覆盖面**：涵盖了生物学 AI 的三大典型场景：超大规模样本量（单细胞）、高特征维度（基因组）、大单样本载荷（成像）。
*   **客观性**：实验特别强调了在**超出内存容量**的数据集上进行测试，以避免操作系统缓存（OS Caching）导致的性能虚高（如文中指出 BioNemo 在小数据集上第二轮速度暴增的现象）。
*   **公平性**：在标准化的 AWS 实例上对比了多种现有工具，并测试了不同硬件（从笔记本到 HPC）的扩展性，实验设计较为严谨、客观。

### 6. 论文的主要结论与发现
*   **性能飞跃**：`annbatch` 的吞吐量比现有工具高出多达 **40 倍**。
*   **格式优势**：采用 Zarr 作为后端比传统的 `h5ad` (HDF5) 显著提高了预洗牌速度和加载吞吐量。
*   **I/O 解耦**：通过极高的加载效率，`annbatch` 成功将训练瓶颈从 I/O 重新移回到了 GPU 计算能力上，使得 TB 级数据的训练变得可行。
*   **生态兼容**：证明了在不放弃 `anndata` 标准格式的前提下，可以实现工业级的训练性能。

### 7. 优点
*   **生态融合**：完全兼容 `scverse` 生态，用户无需改变数据处理习惯或转换格式。
*   **端到端方案**：不仅提供加载器，还解决了大规模数据在磁盘上高效洗牌的难题。
*   **跨领域通用**：一套框架同时解决了组学、成像和遗传学三大领域的大规模数据加载问题。
*   **技术先进**：引入了 Rust 优化、Direct I/O 等底层技术提升 Python 环境下的 I/O 表现。

### 8. 不足与局限
*   **预洗牌开销**：虽然可以摊销，但对于仅训练 1-2 个 epoch 的极小模型，预洗牌的时间成本仍显突出。
*   **采样策略受限**：由于采用连续块读取，目前难以直接实现复杂的加权采样（Weighted Sampling），需通过特定的磁盘写入逻辑来间接实现。
*   **多模态支持**：目前尚未直接支持多模态数据的联合加载（虽可通过扩展实现，但非当前版本核心）。
*   **存储空间**：预洗牌会生成一份数据的副本，对于存储空间极其紧张的环境可能存在压力。

（完）
