---
title: Zero-inflated modeling with smoothing on counting tensors
title_zh: 计数张量上的零膨胀平滑建模
authors: "Elena Tuzhilina, Yaoming Zhen"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.22088v1"
tags: ["query:q7"]
score: 8.0
evidence: 针对单细胞Hi-C基因组数据的概率框架
tldr: 针对单细胞 Hi-C 数据中存在的高稀疏性、零膨胀和细胞异质性问题，本文提出了一种零膨胀泊松张量模型。该模型结合了低秩 CP 结构、聚类特定潜嵌入和基因组位点的平滑性，能够有效捕捉多维依赖关系。通过贝叶斯最优程序区分结构零和技术零，并在高维环境下证明了参数的一致性。实验表明，该方法在零检测、结构恢复及 3D 染色质推断等任务上表现优异。
selection_source: fresh_fetch
motivation: 旨在解决单细胞 Hi-C 等多维计数数据中由于技术限制和生物特性导致的极端稀疏性、零膨胀及细胞间异质性挑战。
method: 提出一种集成低秩 CP 分解、平滑约束和聚类潜变量的零膨胀泊松张量框架，并开发了区分结构零与技术零的贝叶斯最优推断方法。
result: 理论上证明了模型的参数可识别性和一致性，实验上在零检测、细胞聚类和 3D 染色质组织推断方面均优于现有方法。
conclusion: 该框架为具有复杂依赖关系的零膨胀多维计数数据提供了灵活的建模方案，并为大规模单细胞组学分析奠定了理论基础。
---

## 摘要
我们针对具有超额零值的稀疏计数张量提出了一个统一的概率框架，其动机源于单细胞 Hi-C 数据。观测数据自然地表示为由基因组位点对和细胞索引的三路张量，表现出显著的稀疏性、零膨胀和细胞间的异质性。我们引入了一种零膨胀泊松张量模型，该模型集成了低秩 CP 结构、聚类特有的潜在嵌入以及沿有序基因组位点的平滑性，从而共同捕捉多维依赖性、异质性和结构化变异。我们开发了一种贝叶斯最优程序来区分结构性零值和技术性零值，从而实现有原则的推断和不确定性量化。我们确立了模型参数的可辨识性，并推导了所提估计量在高维情形下的收敛速度。仿真研究和单细胞 Hi-C 数据分析表明，该方法在零值检测、潜在结构恢复以及聚类和 3D 染色质结构推断等下游任务中表现出更优的性能。所提出的框架为具有超额零值和结构化依赖的多维计数数据提供了一种灵活的方法，并为未来的工作指明了几个方向，包括基于混合模型的细胞群体建模以及针对大规模应用的可扩展计算。

## Abstract
We propose a unified probabilistic framework for sparse count tensors with excess zeros, motivated by single-cell Hi-C data. The observed data are naturally represented as a three-way tensor indexed by genomic loci pairs and cells, exhibiting pronounced sparsity, zero inflation, and cell-to-cell heterogeneity. We introduce a zero-inflated Poisson tensor model that integrates low-rank CP structure, cluster-specific latent embeddings, and smoothness along ordered genomic loci, thereby jointly capturing multiway dependence, heterogeneity, and structured variation.   We develop a Bayes-optimal procedure for distinguishing structural from technical zeros, enabling principled inference and uncertainty quantification. We establish identifiability of the model parameters and derive consistency rates for the proposed estimators in a high-dimensional regime. Simulation studies and analyses of single-cell Hi-C data demonstrate improved performance in zero detection, latent structure recovery, and downstream tasks such as clustering and 3D chromatin organization inference.   The proposed framework provides a flexible approach for multiway count data with excess zeros and structured dependencies, and suggests several directions for future work, including mixture-based modeling of cell populations and scalable computation for large-scale applications.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **ZITS**（Zero-Inflated Tensor model with Smoothing）的统一概率框架，旨在解决单细胞 Hi-C 数据分析中的高维计数张量建模难题。

### 1. 核心问题与研究动机
*   **研究背景**：单细胞 Hi-C 技术能够测量单个细胞内基因组位点间的空间接触频率，生成“基因组位点 × 基因组位点 × 细胞”的三路张量。
*   **核心挑战**：
    1.  **极端稀疏性与零膨胀**：观测到的零值既包括生物学上的真实缺失（结构零），也包括技术限制导致的采样失败（技术零/掉库）。
    2.  **细胞异质性**：不同细胞类型的染色质结构存在显著差异。
    3.  **基因组平滑性**：相邻基因组位点由于物理连接，其接触模式具有连续性和平滑性。
*   **研究动机**：现有的张量分解方法通常不处理零膨胀，而传统的零膨胀模型又难以扩展到具有复杂多维依赖和结构化变异的张量数据。

### 2. 方法论：ZITS 框架
*   **核心思想**：将零膨胀泊松（ZIP）分布与低秩张量分解、聚类潜变量及函数平滑技术相结合。
*   **关键技术细节**：
    *   **ZIP 模型**：将每个观测值建模为泊松强度（$\Lambda$）和掩码概率（$P$）的乘积。
    *   **双重低秩 CP 分解**：对强度张量和掩码张量同时进行 CP 分解，共享空间嵌入，但允许不同的细胞簇权重。
    *   **函数平滑（Smoothing）**：利用正交基函数（如样条基）对基因组位点的嵌入进行展开，强制模型捕捉染色质作为连续曲线的物理特性。
    *   **聚类结构**：在细胞维度引入块结构，以捕捉不同细胞亚群的异质性。
    *   **算法流程**：采用基于似然函数的交替梯度下降算法进行参数估计，并推导出贝叶斯最优分类器，用于区分结构零和技术零。

### 3. 实验设计
*   **数据集**：
    *   **仿真数据**：模拟了不同稀疏度（高、中、低）、不同细胞数量（25 到 500）和不同聚类数量（2 到 6）的场景。
    *   **真实数据**：人类单细胞 Hi-C 数据（Ramani et al., 2017），涵盖 HAP1、HeLa 和 K562 三种细胞系，分辨率为 10 Mb。
*   **Benchmark 与对比方法**：
    *   **零膨胀处理类**：HiCImpute（贝叶斯层次模型）、scHiCSRS（自表示平滑）。
    *   **插补与聚类类**：scHiCluster（基于随机游走）、标准 CP 张量分解。
*   **评估指标**：相对误差（参数估计）、调整兰德指数（ARI，用于聚类准确度）、精确率/召回率（用于零值检测）。

### 4. 资源与算力
*   **说明**：论文中**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **实现细节**：作者提到开发了基于似然的估计算法，并在 R 语言环境下运行了对比方法。由于涉及高维张量运算和梯度下降，通常需要一定的内存支持，但其低秩假设显著减少了参数量（从 $O(N^2K)$ 降至 $O(K)$ 级别增长）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验针对每个设置重复了 50 次，涵盖了秩（Rank）选择、初始化策略对比、稀疏度影响等多个维度。
    *   真实数据分析重复了 10 次随机子采样，以消除采样偏差。
*   **充分性与公平性**：实验设计较为全面，不仅验证了参数估计的准确性，还通过下游任务（细胞聚类）验证了实用性。对比方法涵盖了目前该领域的主流工具，实验过程考虑了超参数（如嵌入维度 $L$）的鲁棒性。

### 6. 主要结论
*   **理论层面**：确立了模型的参数可识别性，并证明了在高维情形下估计量的一致性收敛速度。
*   **实验层面**：
    *   ZITS 在区分结构零和技术零方面表现优异，尤其在高稀疏场景下具有“稀疏性红利”（Blessing of Sparsity）。
    *   通过 ZITS 进行插补后的数据，其细胞类型聚类准确度（ARI）显著高于原始数据及其他竞争方法。
    *   模型能够有效提取具有生物学意义的细胞潜嵌入。

### 7. 优点
*   **统一性**：首个将零膨胀、低秩分解和平滑约束整合进统一概率框架的工作。
*   **理论支撑**：提供了严谨的可识别性和一致性证明，这在生物信息学启发的方法中较为少见。
*   **物理启发**：通过基函数引入平滑性，契合染色质作为物理实体的生物学本质。

### 8. 不足与局限
*   **计算扩展性**：实验主要在 10 Mb 的粗糙分辨率下进行。对于更高分辨率（如 10 kb 或 100 kb）的数据，张量维度将呈指数级增长，目前的梯度下降算法可能面临内存和计算时间瓶颈。
*   **聚类假设**：目前模型假设细胞簇是预先存在或通过后验聚类得到的，未来若能整合进混合模型（Mixture Model）进行端到端学习会更理想。
*   **应用限制**：模型高度依赖于“有序维度”的平滑性假设，对于不具备此类结构特征的其他计数张量数据，其优势可能减弱。

（完）
