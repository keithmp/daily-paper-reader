---
title: Graph Transformer-Based Pathway Embedding for Cancer Prognosis
title_zh: 基于图变换器的癌症预后通路嵌入
authors: "Koushik Howlader, Md Tauhidul Islam, Wei Le"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.16685v1"
tags: ["query:q7"]
score: 8.0
evidence: 基于AI的分子组学数据通路嵌入
tldr: 针对癌症预后预测中分子组学数据的高异质性，本文提出PATH模型。该模型采用基于调制的患者条件基因嵌入策略，通过共享的基础嵌入结合患者特有的CNV和突变信号，动态生成基因表示。结合图变换器框架建模通路间相互作用，在泛癌转移预测中显著提升了性能，并揭示了疾病状态相关的通路重构机制。
selection_source: fresh_fetch
motivation: 现有模型在构建通路表示时，往往缺乏对基因共享基础特征的显式学习，难以平衡个体分子差异与生物学一致性。
method: 提出一种基于调制的基因嵌入策略，通过患者特有的组学信号动态调整共享的基因基础表示，并利用图变换器捕捉通路间的相互作用。
result: "在泛癌转移预测任务中，该方法取得了0.8766的F1分数，相比现有最先进的多组学基准模型提升了8.8%。"
conclusion: 该研究不仅显著提高了癌症预后预测的准确性，还通过揭示疾病状态下的通路重构为癌症进展机制提供了新见解。
---

## 摘要
由于患者间分子组学数据的高度异质性，准确预测癌症进展仍是一项挑战。虽然生物启发模型提高了这些预测的可解释性，但一个持续存在的局限性在于它们如何编码单个基因以构建通路表示。现有的分层模型通常通过直接映射原始分子输入来获取基因特征，而集成框架往往依赖于患者层面信号的简单统计聚合。这些方法通常无法为每个基因显式学习共享的基础表示，从而限制了下游通路嵌入的表达能力和生物学准确性。为了解决这一问题，我们引入了 PATH，这是一种基于调制且以患者为条件的基因嵌入策略。PATH 代表了一种范式转变：它从每个基因的共享基础嵌入开始，在群体中保持稳定的生物学身份，然后利用患者特异性的拷贝数变异 (CNV) 和突变信号对其进行动态调整。这使得模型能够捕捉细微的个体分子差异，同时保持对基因本身一致的潜在理解。我们将 PATH 集成到一个图变换器框架中，该框架通过通路引导的注意力机制对生物学相关的通路间相互作用进行建模。在泛癌转移预测中，PATH 达到了 0.8766 的 F1 分数，相比当前的 SOTA 多组学基准提升了 8.8%。除了卓越的预测准确性外，我们的方法还识别了具有生物学意义的通路，并且至关重要地揭示了疾病状态特异性的通路重连，为驱动癌症进展的演化通路间相互作用提供了新的见解。

## Abstract
Accurate prediction of cancer progression remains a challenge due to the high heterogeneity of molecular omics data across patients. While biologically informed models have improved the interpretability of these predictions, a persistent limitation lies in how they encode individual genes to construct pathway representations. Existing hierarchical models typically derive gene features by directly mapping raw molecular inputs, whereas integration frameworks often rely on simple statistical aggregations of patient-level signals. These approaches often fail to explicitly learn a shared base representation for each gene, thereby limiting the expressiveness and biological accuracy of downstream pathway embeddings. To address this, we introduce PATH, a modulation-based, patient-conditioned gene embedding strategy. PATH represents a paradigm shift by starting from a shared base embedding for each gene, preserving a stable biological identity across the population, and then dynamically adapting it using patient-specific copy number variation (CNV) and mutation signals. This allows the model to capture subtle individual molecular variations while maintaining a consistent latent understanding of the gene itself. We integrate PATH into a graph transformer framework that models interactions among biologically connected pathways through pathway-guided attention. Across pancancer metastasis prediction, PATH achieves an F1 score of 0.8766, representing an 8.8 percent improvement over the current SOTA multi-omics benchmarks. Beyond superior predictive accuracy, our approach identifies biologically meaningful pathways and, crucially, reveals disease-state-specific pathway rewiring, offering new insights into the evolving pathway-pathway interactions that drive cancer progression.

---

## 论文详细总结（自动生成）

### 论文总结：基于图变换器的癌症预后通路嵌入 (PATH)

#### 1. 核心问题与整体含义
癌症分子组学数据（如突变、拷贝数变异）具有极高的**异质性**，导致准确预测癌症进展（如转移、分期）非常困难。现有的生物启发模型在构建“通路表示”时存在局限：要么直接映射原始数据，忽略了基因在群体中的稳定生物学身份；要么采用简单的统计聚合，丢失了细微的个体差异。
**PATH 模型**的核心动机是实现一种范式转变：为每个基因学习一个**共享的基础嵌入**（保持生物学一致性），再根据患者特有的组学信号进行**动态调制**（捕捉个体差异），最后通过图变换器建模通路间的复杂相互作用（Crosstalk）。

#### 2. 方法论
PATH 框架分为四个阶段：
*   **阶段 1：基于调制的基因嵌入 (FiLM Modulation)**
    *   每个基因 $g$ 拥有一个在所有患者间共享的初始向量 $e_g$。
    *   利用患者 $i$ 的突变和拷贝数变异 (CNV) 数据，通过一个 MLP 生成缩放因子 $\gamma$ 和偏移因子 $\beta$。
    *   公式：$h_{ig} = \gamma_{ig} \odot e_g + \beta_{ig}$。这种机制确保了模型既能识别基因的“身份”，又能反映其在特定患者体内的“状态”。
*   **阶段 2：通路令牌构建 (Attention Pooling)**
    *   根据 Reactome 数据库定义的基因集，利用注意力机制将基因表示聚合为通路令牌 (Pathway Tokens)，识别通路内贡献最大的关键基因。
*   **阶段 3：通路感知图变换器 (Pathway-aware Graph Transformer)**
    *   将通路视为图节点，Reactome 的通路间相互作用作为先验边。
    *   引入 **拉普拉斯位置编码 (LPE)** 捕捉通路在生物网络中的拓扑位置。
    *   采用**软结构掩码 (Soft Masking)**：不完全封死未连接的边，允许模型在训练中发现新的通路间相互作用（通路重连）。
*   **阶段 4：分类与解释**
    *   通过全局注意力池化生成患者表示，进行转移或分期预测。

#### 3. 实验设计
*   **数据集/场景：**
    1.  **泛癌 (Pan-cancer)：** 整合 TCGA 中 33 种癌症类型，预测原发 vs 转移。
    2.  **前列腺癌 (Prostate Cancer)：** SU2C/PCF 队列，预测原发 vs 转移。
    3.  **膀胱癌 (BLCA)：** TCGA 队列，预测早期 vs 晚期。
*   **Benchmark 与对比方法：**
    *   对比了 **PathCNN**（基于 CNN 的通路模型）、**MOGONET/MOGAT**（基于图注意力的多组学集成模型）、**eiCNN/liCNN** 等 SOTA 方法。

#### 4. 资源与算力
*   论文提到使用 **PyTorch** 框架实现。
*   **参数量：** 约 400K 至 500K，相比直接处理全基因组的深度模型更为轻量。
*   **算力细节：** 文中**未明确说明**具体的 GPU 型号、数量或总训练时长。但提到使用了 AdamW 优化器，Batch Size 为 16，最大训练 200 轮。

#### 5. 实验数量与充分性
*   **实验规模：** 进行了 5 折分层交叉验证，并使用 2 个独立随机种子重复（共 10 次运行），确保了结果的统计稳定性。
*   **消融实验：** 针对前列腺癌任务，对比了仅使用突变、仅使用 CNV 以及两者结合的效果，证明了多组学集成的必要性。
*   **充分性评价：** 实验覆盖了泛癌和单癌种任务，并结合 SHAP 值进行了深入的生物学解释分析（如识别 AR 基因在前列腺癌转移中的主导作用），实验设计较为客观、公平且论证充分。

#### 6. 主要结论与发现
*   **性能提升：** 在泛癌转移预测中，PATH 的 F1 分数达到 0.8766，比现有 SOTA 提升了 **8.8%**。
*   **生物学发现：**
    *   识别出 **FGFR/MAPK** 信号通路是驱动转移的核心。
    *   发现了新的通路间联系，例如**细胞外基质组织 (ECM) 与昼夜节律 (Circadian Clock)** 之间的潜在关联。
    *   揭示了转移过程中通路权重的“重连”现象，证明癌症进展是系统性的通路协同变化。

#### 7. 优点
*   **范式创新：** 引入 FiLM 调制机制平衡了基因的“群体一致性”与“个体异质性”。
*   **可解释性强：** 提供基因、通路、通路间相互作用三个层面的解释，能够生成可验证的生物学假设。
*   **灵活性：** 软掩码机制克服了生物学先验知识不完整的问题，允许模型从数据中学习新的相互作用。

#### 8. 不足与局限
*   **数据局限：** 实验基于回顾性数据集，缺乏前瞻性临床队列的外部验证。
*   **组学覆盖：** 目前仅整合了突变和 CNV，尚未纳入转录组（RNA-seq）、表观遗传组等对通路活性影响巨大的数据。
*   **因果性：** 模型识别的是相关性而非因果性，发现的新通路连接仍需湿实验进一步验证。
*   **先验依赖：** 模型性能在一定程度上受限于 Reactome 数据库的准确性和完整性。

（完）
