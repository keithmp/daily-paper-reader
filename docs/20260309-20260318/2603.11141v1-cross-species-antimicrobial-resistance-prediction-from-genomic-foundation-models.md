---
title: Cross-Species Antimicrobial Resistance Prediction from Genomic Foundation Models
title_zh: 基于基因组基础模型的跨物种抗生素耐药性预测
authors: Huilin Tai
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11141v1"
tags: ["query:q3"]
score: 9.0
evidence: 跨物种基因组基础模型用于耐药性预测
tldr: 本研究探讨了跨物种抗生素耐药性（AMR）预测中的分布外泛化问题。研究发现传统k-mer方法在跨物种评估时性能大幅下降，因此引入了基于基因组基础模型Evo-1-8k-base的表示学习方法。通过分析嵌入层的稳定性并采用MiniRocket算法捕捉局部多尺度激活模式，而非传统的全局池化，有效保留了水平转移基因等局部耐药信号。该方法显著提升了模型在未见物种上的预测准确性，为跨物种AMR监测提供了新思路。
selection_source: fresh_fetch
motivation: 跨物种抗生素耐药性预测面临严重的分布外泛化挑战，传统方法难以在具有不同耐药机制的系统发育差异物种间有效转移。
method: 利用基因组基础模型Evo-1-8k-base的嵌入表示，通过层稳定性诊断选择最优层，并应用MiniRocket聚合多尺度局部激活模式。
result: 实验证明，保留局部激活信号的聚合策略在跨物种评估中显著优于全局池化，有效克服了传统方法在处理局部耐药机制时的局限性。
conclusion: 基因组基础模型的层选择与局部信号聚合策略是提升跨物种耐药性预测泛化性能的关键因素。
---

## 摘要
跨物种抗生素耐药性（AMR）预测本质上是一个分布外（OOD）泛化问题：在某一组细菌分类群上训练的模型必须迁移到系统发育不同的基因组上，而这些基因组可能依赖于不同的耐药机制。在不同物种之间，耐药性源于局部的水平转移基因盒与弥散的物种特异性基因组背景的异质混合，这使得成功的迁移本质上取决于具体的机制。采用严格的物种留出协议，我们首先使用 Kover 建立了一个可解释的 k-mer 基准，并证明了强大的物种内性能在真实的跨物种评估下会发生崩溃。这促使我们采用表示层面的方法，以保留可迁移的生物信号，而不是放大系统发育捷径。我们研究了源自 Evo-1-8k-base 的基因组基础模型嵌入，并引入了基于激活尺度、各向同性、有效秩以及原生 bfloat16 推理下跨种子稳定性的层选择诊断方法。这些分析确定了深层中的稳定性边界，并揭示了在该边界附近提取的嵌入为下游预测提供了更稳健的表示。为了保留局部耐药信号，我们将每个窗口的嵌入视为有序的多变量信号，并应用 MiniRocket 来总结多尺度局部激活模式，而不是依赖全局池化。我们的结果表明，聚合策略在跨物种 AMR 预测中起着核心作用，并且当耐药机制具有局部性时，保留局部激活模式能显著提高泛化能力。

## 速览
**TLDR**：本研究针对跨物种抗生素耐药性（AMR）预测中的分布外泛化难题，提出了一种基于基因组基础模型的方法。研究发现传统k-mer模型在跨物种评估时性能骤降，进而引入Evo-1-8k-base模型的嵌入表示，并通过层选择诊断和MiniRocket局部激活模式聚合技术，有效保留了关键的耐药生物信号，显著提升了模型在异质基因组背景下的泛化表现。 \
**Motivation**：跨物种耐药性预测受限于物种特异性的基因背景和水平转移基因的复杂性，导致传统模型在处理未见物种时泛化能力不足。 \
**Method**：研究采用了基因组基础模型的嵌入表示，通过层稳定性诊断选择最佳层，并利用MiniRocket捕捉多尺度局部激活模式而非全局池化。 \
**Result**：实验表明，保留局部特征的聚合策略在跨物种评估中显著优于传统基准，能够更有效地识别跨物种转移的耐药机制。 \
**Conclusion**：特征聚合策略是提升跨物种AMR预测性能的关键，通过保留局部基因组信号可以克服系统发育差异带来的预测障碍。

---

## Abstract
Cross-species antimicrobial resistance (AMR) prediction is fundamentally an out-of-distribution (OOD) generalization problem: models trained on one set of bacterial taxa must transfer to phylogenetically distinct genomes that may rely on different resistance mechanisms. Across species, resistance arises from a heterogeneous mixture of localized, horizontally transferred gene cassettes and diffuse species-specific genomic backgrounds, making successful transfer inherently mechanism-dependent. Using a strict species holdout protocol, we first establish an interpretable k-mer baseline with Kover and show that strong within-species performance collapses under true cross-species evaluation. This motivates representation-level approaches that preserve transferable biological signals rather than amplify phylogenetic shortcuts.  We investigate genomic foundation model embeddings derived from Evo-1-8k-base and introduce diagnostics for layer selection based on activation scale, isotropy, effective rank, and cross-seed stability under native bfloat16 inference. These analyses identify a stability boundary in deeper layers and reveal that embeddings extracted near this boundary provide more robust representations for downstream prediction. To preserve localized resistance signals, we treat per-window embeddings as an ordered multivariate signal and apply MiniRocket to summarize multi-scale local activation patterns instead of relying on global pooling. Our results show that aggregation strategy plays a central role in cross-species AMR prediction and that preserving local activation patterns substantially improves generalization when resistance mechanisms are localized.

---

## 论文详细总结（自动生成）

这篇论文针对跨物种抗生素耐药性（AMR）预测这一挑战，提出了一种基于基因组基础模型（Genomic Foundation Models）的创新框架。以下是详细的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：跨物种 AMR 预测本质上是一个**分布外（OOD）泛化问题**。模型在已知物种上训练后，往往会捕捉到与物种身份相关的“系统发育捷径”（如 GC 含量、密码子偏好），而非真正的耐药机制。当应用于新物种时，这些捷径失效，导致预测性能崩溃。
*   **生物学背景**：耐药机制具有异质性。有些是局部的、通过水平转移获得的“基因盒”（Cassettes），有些则是弥散在整个染色体上的突变。
*   **研究目的**：探索如何利用大规模预训练基因组模型（如 Evo）的嵌入表示，通过保留局部生物信号而非全局统计平均，来实现更稳健的跨物种迁移。

### 2. 方法论：核心思想与关键技术
论文提出了两个关键创新点：
*   **诊断驱动的层选择（Layer Selection）**：
    *   **核心思想**：并非越深层越好。在 `bfloat16` 原生推理下，深层模型会出现数值不稳定和几何坍缩。
    *   **技术细节**：通过监测激活尺度、各向同性（Isotropy）、有效秩（Effective Rank）和跨种子稳定性，发现 Evo-1-8k-base 在第 11 层（L11）存在明显的稳定性边界。
    *   **结论**：选择**第 10 层（L10）**作为嵌入提取点，因为它在保持丰富几何结构的同时，数值最稳健。
*   **局部模式保留聚合（MiniRocket Aggregation）**：
    *   **核心思想**：传统的全局池化（Global Pooling）会稀释掉仅占基因组 0.1% 的局部耐药信号。
    *   **技术细节**：将基因组窗口嵌入视为有序的多变量信号，应用 **MiniRocket** 算法。该算法利用大量随机卷积核捕捉多尺度局部激活模式，并通过正值比例（PPV）进行池化。
    *   **流程**：提取 L10 嵌入 -> PCA 降维 -> MiniRocket 变换 -> 训练简单分类器（如 k-NN 或线性模型）。

### 3. 实验设计
*   **数据集**：涵盖 126 个物种、3,388 个基因组，针对 6 种抗生素（氨苄西林、环丙沙星、红霉素、庆大霉素、四环素、替加环素）。
*   **评估协议**：采用严格的**物种留出（Species Holdout）**协议，确保训练集和测试集之间没有任何系统发育重叠。
*   **基准方法（Benchmark）**：
    *   **Kover**：基于 k-mer 的规则学习方法（传统基准）。
    *   **Global Pooling**：基于基础模型嵌入的全局统计聚合（均值、标准差等）。
*   **对比维度**：同物种评估 vs. 跨物种评估；不同耐药机制（质粒介导 vs. 染色体介导）下的表现。

### 4. 资源与算力
*   **算力说明**：论文提到了使用哥伦比亚大学 AlQuraishi 实验室的计算资源。
*   **具体细节**：文中**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **优化技术**：作者提到实现了一种“流式提取器”（Streaming Extractor），通过固定提取深度，将 GPU 显存需求降低了约一个数量级，使得处理 4Mb 大小的细菌基因组变得可行。

### 5. 实验数量与充分性
*   **实验规模**：进行了 3 次独立随机划分的重复实验（Replicates v1-1, v1-2, v1-3），以减少分区偏差。
*   **覆盖范围**：测试了多种分类器（k-NN, LightGBM, Random Forest, SVM, MLP 等）。
*   **充分性评价**：实验设计非常严谨。不仅有性能指标（MCC, F1, AUROC），还包含了深入的几何分析（邻居审计、系统发育距离分析）和层诊断实验。通过对 6 种不同机制的抗生素进行测试，验证了“机制混合假说”，实验结果具有高度的客观性和说服力。

### 6. 主要结论与发现
*   **层选择至关重要**：证实了基础模型的中层（L10）比末层更适合迁移学习。
*   **聚合策略决定成败**：MiniRocket 在处理由**水平转移基因盒**介导的耐药性（如氨苄西林）时显著优于全局池化；而全局池化在处理弥散的染色体机制时仍具竞争力。
*   **k-NN 的崛起**：在 MiniRocket 变换后的特征空间中，简单的 k-NN 表现极佳，说明该方法成功地将特征空间重组为按“耐药机制”而非“物种分类”聚类的结构。
*   **超越 k-mer**：基于基础模型的方法在跨物种场景下全面超越了传统的 Kover 方法。

### 7. 优点与亮点
*   **可解释性**：通过邻居审计（Neighbor Auditing）解释了模型为何能跨物种预测，将黑盒模型与生物学机制（如质粒共享）联系起来。
*   **诊断驱动**：不仅给出了结果，还通过层诊断实验解释了模型内部表示在不同深度下的演变规律。
*   **现实意义**：严格的物种留出协议模拟了临床中面对新发病原体的真实场景。

### 8. 不足与局限
*   **机制依赖性**：该方法并非“万灵丹”，对于非局部、依赖全基因组背景的耐药机制提升有限。
*   **数据回顾性**：所有分析均基于回顾性数据，尚需前瞻性临床验证。
*   **模型单一性**：主要基于 Evo 模型，尚未在其他基因组基础模型（如 Nucleotide Transformer）上广泛验证。
*   **计算开销**：MiniRocket 生成的特征维度较高（约 12,000 维），相比全局池化增加了下游任务的存储和计算负担。

（完）
