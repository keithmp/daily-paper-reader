---
title: "EvoFlows: Evolutionary Edit-Based Flow-Matching for Protein Engineering"
title_zh: EvoFlows：用于蛋白质工程的基于演化编辑的流匹配
authors: "Nicolas Deutschmann, Constance Ferragu, Jonathan D. Ziegler, Shayan Aziznejad, Eli Bixby"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11703v1"
tags: ["query:q7"]
score: 9.0
evidence: AI驱动的蛋白质工程与突变轨迹研究
tldr: EvoFlows 是一种面向蛋白质工程的变长序列到序列建模方法，通过引入编辑流（Edit Flows）来学习进化相关蛋白质之间的突变轨迹。与传统的自回归或掩码语言模型不同，它能对模板序列执行可控的插入、删除和替换操作，不仅决定突变内容还决定位置。实验证明其在捕捉蛋白质分布方面与领先模型相当，且在生成非平凡且类天然突变体方面表现更优。
selection_source: fresh_fetch
motivation: 现有的蛋白质模型在处理变长序列编辑和精确控制突变位置与类型方面存在局限，难以有效模拟进化路径。
method: 提出基于编辑流的流匹配方法，通过学习进化相关序列间的突变轨迹，同时对蛋白质分布和连接它们的编辑路径进行建模。
result: 在 UNIREF 和 OAS 数据集上的评估显示，该模型在序列分布捕捉上媲美主流掩码语言模型，并能从模板生成更高质量的自然突变体。
conclusion: EvoFlows 为蛋白质工程提供了一种灵活且可控的编辑工具，能够通过模拟进化过程实现高效的蛋白质序列优化与设计。
---

## 摘要
我们介绍了 EvoFlows，这是一种变长序列到序列的蛋白质建模方法，特别适用于蛋白质工程。与自回归和掩码语言模型不同，EvoFlows 在模板蛋白质序列上执行有限且可控数量的插入、删除和替换。换言之，EvoFlows 不仅预测执行哪种突变，还预测突变应发生于何处。我们的方法利用编辑流（edit flows）来学习演化相关蛋白质序列之间的突变轨迹，同时对相关天然蛋白质的分布及其连接它们的突变路径进行建模。通过对来自 UNIREF 和 OAS 的多种蛋白质群落进行广泛的计算机模拟（in silico）评估，我们证明 EvoFlows 捕捉蛋白质序列分布的质量与蛋白质工程中常用的领先掩码语言模型相当，同时在从给定模板蛋白质生成非平凡且类天然突变体方面表现出更强的能力。

## 速览
**TLDR**：EvoFlows 是一种面向蛋白质工程的变长序列到序列建模方法。它通过编辑流学习进化相关蛋白质序列间的突变轨迹，能够对模板序列进行可控的插入、删除和替换操作。该方法不仅能预测突变内容，还能确定突变位置。在 UNIREF 和 OAS 数据集上的实验表明，EvoFlows 在捕捉序列分布方面可媲美主流掩码语言模型，且在生成非平凡且类天然的突变体方面表现更优。 \
**Motivation**：传统的自回归和掩码语言模型在处理变长蛋白质序列编辑及精确控制突变位置和类型方面存在局限。 \
**Method**：提出基于编辑流的流匹配方法，通过学习进化相关序列间的突变路径，实现对模板序列的插入、删除和替换建模。 \
**Result**：在 UNIREF 和 OAS 上的评估显示，该模型在序列分布建模质量上比肩领先的掩码语言模型，并能生成更高质量的自然突变体。 \
**Conclusion**：EvoFlows 为蛋白质工程提供了一种灵活且强大的工具，能够有效地在保持天然特性的同时探索蛋白质的突变空间。

---

## Abstract
We introduce EvoFlows, a variable-length sequence-to-sequence protein modeling approach uniquely suited to protein engineering. Unlike autoregressive and masked language models, EvoFlows perform a limited, controllable number of insertions, deletions, and substitutions on a template protein sequence. In other words, EvoFlows predict not only _which_ mutation to perform, but also _where_ it should occur. Our approach leverages edit flows to learn mutational trajectories between evolutionarily-related protein sequences, simultaneously modeling distributions of related natural proteins and the mutational paths connecting them. Through extensive _in silico_ evaluation on diverse protein communities from UNIREF and OAS, we demonstrate that EvoFlows capture protein sequence distributions with a quality comparable to leading masked language models commonly used in protein engineering, while showing improved ability to generate non-trivial yet natural-like mutants from a given template protein.

---

## 论文详细总结（自动生成）

### EvoFlows: 用于蛋白质工程的基于演化编辑的流匹配 —— 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：蛋白质优化（Protein Optimization）旨在通过对现有蛋白质序列进行局部编辑（替换、插入、删除）来改善其性质。
*   **研究背景**：现有的蛋白质语言模型（PLM）在优化任务中存在局限：
    *   **掩码语言模型（MLM）**：通常假设序列长度固定，且需要人工指定掩码位置，无法原生处理插入和删除（Indels）。
    *   **自回归模型（AR）**：生成局部变体效率低下，通常需要重新生成整个序列。
*   **动机**：开发一种能够模拟蛋白质自然演化过程（即通过离散编辑连接序列）的模型，使其不仅能决定“突变什么”，还能自动决定“在何处突变”，并支持变长序列生成。

#### 2. 方法论
*   **核心思想**：引入**离散编辑流（Discrete Edit Flows）**，将蛋白质序列之间的演化关系建模为连续时间马尔可夫链（CTMC）。模型学习在起始序列（源）和同源序列（目标）之间进行转换的编辑轨迹。
*   **关键技术细节**：
    *   **序列对齐**：利用生物信息学经典的 Needleman-Wunsch 算法对同源序列对进行全局对齐，引入空标记（$\epsilon$）来处理插入和删除，将变长问题转化为等长对齐空间的编辑问题。
    *   **流匹配目标**：通过最小化 Bregman 散度来训练模型，使其预测在给定时间 $t$ 下的编辑速率（Rate function）。
    *   **架构设计**：采用预训练的 **ESM-2** 编码器提取序列特征，通过 **FiLM（特征线性调制）** 层注入时间信息 $t$，最后使用浅层 MLP 预测头分别计算替换、插入和删除的概率及位置。
    *   **推理算法**：提出了一种**改进的无网格（Grid-free）采样程序**。通过数值积分采样“事件到达时间”，避免了传统欧拉法中可能出现的病态分布，且利用 FiLM 层的特性显著降低了推理时的计算开销。

#### 3. 实验设计
*   **数据集**：
    *   从 **UNIREF30** 和 **OAS**（抗体数据库）中选取了 6 种具有代表性的种子蛋白：抗 SARS-CoV-2 纳米抗体（VHH）、丝氨酸-丙酮酸转氨酶（SPAT）、卤代烷脱卤酶（DhaA）、抗 HER2 单链抗体（scFv）、鸡 FGF2 生长因子、抗 EphA2 纳米抗体。
    *   通过同源搜索构建了 6 个专门的同源序列数据集。
*   **Benchmark 与对比方法**：
    *   **确定性基准（Deterministic setup）**：在人工合成的规则数据集上验证模型学习复杂编辑逻辑的能力。
    *   **对比方法**：
        1.  **Random Pairing**：从训练集中随机抽取同源对（代表数据分布的上限）。
        2.  **Evotuned PLM**：在相同数据集上微调的 ESM-2-650M 模型。
        3.  **Evotune (forced)**：强制执行突变的微调模型。
        4.  **Random**：随机突变基准。
*   **评估指标**：Levenshtein 距离、ESM2 伪对数似然（PLL）、最大均值差异（MMD）、KL 散度、互信息（MIp）以及氨基酸频率分布等。

#### 4. 资源与算力
*   **模型规模**：使用了 **ESM-2-650M** 作为基础编码器。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但从模型参数量（6.5 亿）和涉及 6 个数据集的微调来看，该研究需要中等规模的计算集群支持。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 个不同家族的蛋白质，序列长度从约 100 到 400 个氨基酸不等，功能涵盖酶、抗体和生长因子。
*   **充分性**：实验设计较为全面，不仅验证了模型捕捉全局分布的能力（通过 MMD 和 KL 散度），还验证了捕捉协同进化信号的能力（通过协方差和 MIp）。
*   **客观性**：通过引入“确定性基准”排除了数据噪声对模型能力的干扰，对比实验中考虑了“强制突变”等变体，确保了与现有 MLM 模型的公平对比。

#### 6. 主要结论与发现
*   **分布捕捉能力**：EvoFlows 在捕捉天然蛋白质序列分布方面达到了与领先的掩码语言模型（MLM）相当的水平。
*   **编辑质量**：相比于 MLM，EvoFlows 能够生成更具“非平凡性”（即编辑距离更大但仍保持类天然特性）的突变体。
*   **变长支持**：成功实现了对插入和删除操作的原生建模，这是传统 PLM 难以直接完成的。
*   **确定性验证**：在合成任务中，模型能够以极高的精确度（F1 > 0.8）识别并执行复杂的条件编辑规则。

#### 7. 优点
*   **科学驱动的设计**：将蛋白质演化（Edit-based）与生成模型（Flow-matching）有机结合，符合生物学直觉。
*   **灵活性**：原生支持变长序列，且能通过“时钟归一化”（Clock normalization）超参数精确控制突变预算。
*   **推理效率**：FiLM 层的应用使得在不重新计算重型编码器的情况下更新时间步成为可能，大幅提升了采样速度。

#### 8. 不足与局限
*   **缺乏湿实验验证**：所有评估均基于计算机模拟（in silico），生成的序列是否具有生物学活性（如结合亲和力、酶促活性）尚待实验室验证。
*   **数据依赖性**：模型性能高度依赖于同源序列对齐的质量，对于缺乏已知同源物的孤儿蛋白（Orphan proteins）可能效果受限。
*   **应用限制**：目前主要展示了无条件生成能力，虽然提到了属性引导（Property-based guidance），但未在文中给出具体的条件生成实验结果。

（完）
